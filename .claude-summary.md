# VoxFlame Agent - 项目概述

> **燃言 - Ignite Your Voice**
>
> 专为2000万构音障碍者打造的 Agentic Voice 应用

## 产品定位

**核心理念**：不是"纠正"用户的声音，而是"理解"用户的意图。

**产品定义**：VoxFlame = Voice Gateway（语音网关）+ 沟通辅助模式 + 训练复盘模式 +（未来）多模态 Copilot

**核心问题**：构音障碍用户的**"认知差异"**——"我觉得我说得很清楚"，但"他人听到的是完全不同的声音"。产品要做的是让用户第一次真正"看见自己的声音"。

**核心流程**：用户说话 → ASR识别 → LLM纠错 → 清晰字幕 + TTS语音输出

---

## 产品蓝图（2026）

### 愿景与使命

- **愿景**：让每个声音都被听见，让每种表达都被理解
- **使命**：为构音障碍用户提供"实时可沟通 + 长期变好 + 温暖陪伴"的语音智能体生态
- **差异化**：把"沟通"从能力问题变成权限问题——用户授权系统完成"实时理解→清晰表达→可追溯复盘→长期变好"

### 目标用户 Persona

| Persona | 描述 | 核心需求 |
|---------|------|---------|
| **P1 患者用户** | 18-40岁，构音障碍（轻中重度） | "自己觉得说清楚了，但陌生人听不懂" |
| **P2 交流伙伴** | 陌生人/同事/商家/医护 | "快点懂 + 少尴尬" |
| **P3 康复专业人员** | 言语治疗师、康复师 | 结构化数据（趋势、训练建议、错误模式） |
| **P4 家属/同伴** | 家庭成员 | 一起用，语音无障碍 |

### 开发阶段规划

#### MVP（90天内）- 沟通桥
> 目标：能用在真实社交场景

| 功能 | 优先级 | 状态 |
|------|--------|------|
| 双行字幕镜（Raw ASR + LLM纠错 + 清晰度评分） | 必须 | ✅ 已有雏形 |
| 一键代播/全屏字幕 | 必须 | 🚧 待实现 |
| 常用短语板（TTS预缓存） | 高优 | ✅ 已完成 |
| 场景模板（就医/购物/点餐/打车） | 高优 | 🚧 待实现 |
| 可控录制与日志（local-first） | 高优 | 🚧 待实现 |
| 连接与身份（Token注入user_profile） | 中优 | ✅ 已实现 |

#### V1（180天内）- 个体化 + 端侧
> 目标：规模化可用

| 功能 | 描述 |
|------|------|
| 非标准语音识别个体化适配 | 个人热词、混淆模式、严重度分层 |
| LLM纠错升级为解码协同 | N-best重排序/语义约束 |
| 端到端低延迟（<2s）与可打断 | interrupt_threshold_ms=200 |
| 轻硬件原型 | 极简收音 + 提示灯（不做全能Agent设备） |

#### V2（365天内）- 康复闭环 + 商业化
> 目标：复盘与训练闭环、B2B2C打通

| 功能 | 描述 |
|------|------|
| 复盘与数据可视化 | 趋势（清晰度/流利度/停顿）+ 训练建议 |
| 伙伴模式正式化 | 对方端"懂/不懂"反馈 |
| 康复师工作台（Web） | 会话摘要、错误模式、训练处方导出 |
| 语音记忆模块产品化 | 可检索、可解释、可遗忘 |

#### V3（>365天）- 多模态 Copilot
> 目标：多模态 + 主动沟通提示（慢启动、强约束）

---

### 权限与同意模型（三层）

| 层级 | 范围 | 设计原则 |
|------|------|---------|
| **层级一** | 设备权限（OS层） | 麦克风、通知、蓝牙 |
| **层级二** | 会话同意（产品层） | 默认不连续录音，一键暂停/清空 |
| **层级三** | 第三方同意（社交层） | 公开提示机制、伙伴模式告知 |

### 数据授权边界（沟通权）

| 模式 | 描述 | 默认 |
|------|------|------|
| **Assist** | 仅建议与显示，用户确认后代播 | ✅ 默认 |
| **Relay** | 通话/社交中继，仍需确认式执行 | 需授权 |
| **Delegate** | 白名单工具+白名单对象+可撤销 | 需强授权 |

---

### KPI 与成功度量

**北极星指标（NSM）**：陌生人沟通成功率（Comprehension Rate）
- 在陌生人场景中，对方点击"懂了 ✅"或用户主观确认"对方理解"的比例

**辅助指标**：
| 指标 | 目标 |
|------|------|
| p95 端到端延迟 | <2s |
| TTS 打断成功率 | >95% |
| 短语板兜底率 | >80%（识别失败时） |

**成长指标（V2）**：
- 周留存（W4）、训练streak中位数、清晰度评分提升斜率
- 康复师端活跃（每周查看报告次数）

---

## 竞品对标与差异化

| 能力维度 | VoxFlame | PLAUD NOTE | Limitless | Ray-Ban Meta | Rabbit R1 |
|---------|----------|------------|-----------|--------------|-----------|
| 实时对话字幕 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 构音障碍纠错 | ✅ **核心差异** | ❌ | ❌ | ❌ | ❌ |
| 一键代播(TTS) | ✅ | ❌ | ❌ | ✅ | ✅ |
| 伙伴模式 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 训练复盘/康复 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 同意/可见性机制 | ✅ 设计要求 | 未明确 | ✅ | 争议较大 | 争议点 |

**结论**：VoxFlame 的差异化必须牢牢钉在"构音障碍沟通权"上——把"记录/转写"上升为"表达代理 + 训练飞轮 + 协作网络"。

---

## 技术栈

| 层级 | 技术 | 端口 | 说明 |
|------|------|------|------|
| 前端 | Next.js 14 + TypeScript + TailwindCSS + PWA | 3000 | Google风格简洁UI |
| 后端 | Express + TypeScript + WebSocket Proxy | 3001 | API + WS代理 |
| Agent | TEN Framework (Go Runtime + Python Extensions) | 8766 | ASR→LLM→TTS流水线 |
| ASR | 阿里云 funasr-nano (Paraformer-realtime-v2) | - | 容忍模糊识别 |
| LLM | QWEN3 Max via DashScope | - | 上下文推断纠错 |
| TTS | CosyVoice v3 (longxiaochun) | - | 温暖亲切音色 |
| 向量库 | Qdrant (可选) | 6333 | 记忆系统 |

---

## 目录结构

```
VoxFlame-Agent/
├── frontend/              # Next.js PWA 应用
│   ├── src/app/           # 页面 (ranyan/contribute)
│   ├── src/hooks/         # useAgent WebSocket hook
│   └── src/lib/           # 音频处理、WS客户端
├── backend/               # Express 服务器
│   └── src/index.ts       # HTTP API + WS代理到ten-agent
├── ten_agent/             # TEN Framework Agent
│   ├── extension_src/     # Python扩展
│   │   ├── voxflame_main_python/      # 主扩展
│   │   ├── websocket_server/          # WebSocket服务器
│   │   └── llm_correction_python/     # LLM纠错扩展
│   ├── manifest.json      # Agent配置
│   └── property.json      # 运行时参数
├── docs/                  # 技术文档
│   ├── TEN_*.md           # TEN框架研究
│   ├── LLM_CORRECTION_*.md # LLM纠错计划
│   └── PWA_*.md           # PWA实施指南
├── docker-compose.yml     # 容器编排
└── .env                   # 环境变量 (DASHSCOPE_API_KEY)
```

---

## 关键参数（构音障碍特异性）

| 模块 | 参数 | 推荐值 | 说明 |
|------|------|--------|------|
| VAD | threshold | 0.3-0.4 | 降低误触发（正常0.5） |
| VAD | silence_duration | 1500-2000ms | 延长等待（正常1000ms） |
| LLM | temperature | 0.3 | 降低随机性 |
| LLM | max_tokens | 100 | 限制输出长度 |
| TTS | voice | longxiaochun | 温暖亲切 |
| TTS | speed | 0.9 | 略微放慢 |

---

## 当前状态

### v1.0 - 基础语音助手 ✅
- TEN Framework集成
- 阿里云ASR实时识别
- 通义千问LLM对话
- CosyVoice TTS语音
- WebSocket实时通信
- PWA基础支持
- Docker部署

### v1.1 - WebSocket代理 ✅
- 后端代理WS请求到TEN Agent
- 解决VSCode Remote SSH端口转发问题

### v1.2 - UI改进 ✅
- Google风格简洁界面
- 点击式录音（空格键控制）
- 字幕实时显示

### v1.3 - 用户认证系统 ✅
- Supabase Auth 集成
- 注册/登录功能
- JWT Token 认证
- Agent 用户上下文感知

### v1.4 - 环境配置修复 ✅
- Docker Compose 构建参数修复
- NEXT_PUBLIC_* 环境变量正确传递
- 前端动态 URL 配置
- 完整消息链路验证通过

### v2.1 - 记忆系统 🚧
- [x] memory_layer_python TEN 扩展实现
- [x] LocalStore (SQLite + Markdown) 后端
- [x] 语音画像记忆 (混淆模式、热词、清晰度评分)
- [x] 会话事件处理 (session_start/session_end)
- [x] 热词自动学习机制
- [ ] Backend FAISS 语义检索集成
- [ ] Supabase 元数据同步
- [ ] 记忆检索 API 完善

### v2.0 - 构音障碍适配 🚧
- [x] Phase 0: 双行字幕镜 + 常用短语板 (P0)
- [ ] Phase 1: 一键代播/全屏字幕 (P0)
- [ ] Phase 2: 场景模板 + 伙伴模式 (P0)
- [ ] Phase 3: LLM 纠错 Prompt 优化 (P0)
- [ ] Phase 4: 音频流稳定性提升 (P1)
- [ ] Phase 5: 个性化纠错 (P2)

**详细计划**: [docs/LLM_CORRECTION_DEVELOPMENT_PLAN.md](docs/LLM_CORRECTION_DEVELOPMENT_PLAN.md)

---

## 记忆系统架构（2026 更新）

### 设计目标

基于 [OpenClaw 记忆架构](https://blog.csdn.net/tianyuanwo/article/details/158428045) 和 PRD 深研，VoxFlame 记忆系统需满足：

| 目标 | 描述 |
|------|------|
| **可控** | 默认本地保存，用户决定上传与分享 |
| **可检索** | 语义检索 + 关键词检索 + 时间检索 + 场景检索 |
| **可遗忘** | 选择性遗忘与自动 compaction |
| **可审计** | 写入/检索/导出/删除可追踪（GDPR/BIPA 合规） |

### 四层记忆架构（参考 OpenClaw）

```
┌─────────────────────────────────────────────────────────────────┐
│                    VOXFLAME MEMORY STACK                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐    │
│  │           TEN Framework Agent (PowerMem)                 │    │
│  │  • 对话记忆（短期上下文）                                  │    │
│  │  • 用户画像注入                                          │    │
│  │  • 个性化问候                                            │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                              ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              Local Store (本地优先)                       │    │
│  │  • MEMORY.md - 长期记忆（用户画像、发音特点、偏好）        │    │
│  │  • memory/YYYY-MM-DD.md - 每日日志（工作记忆）            │    │
│  │  • SQLite - 元数据索引                                   │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                              ▼ (可选云端同步)                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              Supabase PostgreSQL (元数据)                 │    │
│  │  • sessions, utterances, hotwords, corrections           │    │
│  │  • pgvector HNSW 索引（小规模语义检索）                    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                              ▼ (Phase 3: 音频向量)               │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              Qdrant (音频向量库)                          │    │
│  │  • text_episodic - 文本 embedding                        │    │
│  │  • audio_content - Wav2Vec/HuBERT/Whisper embedding      │    │
│  │  • speaker_voiceprint - 说话人 embedding                 │    │
│  │  • 量化压缩：Product Quantization（91%内存节省）          │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

### 四种记忆类型

| 类型 | 描述 | 存储位置 |
|------|------|---------|
| **Episodic（情景）** | 对话片段、当天事件 | SQLite + 每日日志 |
| **Semantic（语义）** | 长期事实（偏好、身份） | MEMORY.md |
| **Skill（技能）** | 短语模板、场景脚本 | Supabase |
| **Voice-profile（语音画像）** | 热词、混淆模式、清晰度趋势 | Qdrant + Supabase |

### 检索策略（避免 Context Explosion）

1. **永远不把全量 transcript 塞进 prompt**：通过 memory_search 工具检索
2. **默认只检索 top-k（8-20）片段**：带时间衰减
3. **混合检索优先**：关键词（BM25）+ 向量
4. **结构化 slot 优先**：热词/姓名/药名进 structured fields
5. **写入分层**：当天日志（append）与长期记忆（curated）分开
6. **定期 compaction**：接近 token 上限时触发静默写入
7. **选择性遗忘（TTL）**：敏感会话短保留期
8. **Embedding 缓存与去重**：控制成本

### Qdrant 性能指标（参考）

| 指标 | Qdrant + 量化 | pgvector |
|------|--------------|----------|
| 内存占用 | 减少 91% | 基准 |
| 搜索延迟 p95 | 28-32ms | 200ms+ |
| 召回损失 | 7-8% | - |

详见：[Qdrant vs pgvector](https://zilliz.com.cn/comparison/qdrant-vs-pgvector)

---

## 快速命令

### Docker管理
```bash
sudo docker-compose ps                  # 查看状态
sudo docker-compose logs -f             # 查看日志
sudo docker-compose restart <service>   # 重启服务
sudo docker-compose up -d --build       # 重新构建启动
```

### 服务访问
- 前端: http://localhost:3000
- 后端: http://localhost:3001
- TEN Agent: ws://localhost:8766

### 测试
```bash
cd frontend && npm test          # 前端测试
cd backend && npm test           # 后端测试
pytest                           # Python测试
```

---

## 工作日志

### 2026-03-02 (下午)
- **记忆系统实现**: memory_layer_python TEN 扩展完成
- **本地存储后端**: SQLite + Markdown LocalStore 实现
- **语音画像记忆**: 混淆模式、热词、清晰度评分功能
- **集成测试**: Playwright 登录测试 + TEN Agent 日志验证
- **关键验证**:
  - 用户注册/登录 ✅
  - WebSocket 连接 ✅
  - 记忆层初始化 ✅
  - 会话事件处理 ✅

### 2026-03-02 (上午)
- **PRD 深研交付**：完整的产品 PRD 与记忆系统前沿报告
- **产品蓝图更新**：基于 PRD 更新产品定位、阶段规划、权限模型
- **记忆系统架构升级**：参考 OpenClaw 四层架构 + Qdrant 量化方案
- **关键决策**：
  - 确认"沟通权系统"定位，非单纯语音转写
  - 三层同意模型（设备/会话/第三方）
  - Local-first + 可审计的记忆系统
  - 硬件定位为"连续性增强器"而非替代手机

### 2026-02-28
- **记忆系统深度调研**: 对比 EverMemOS / Qdrant / Supabase pgvector
- **TEN Framework 记忆研究**: 研究了 PowerMem、EverMemOS、memU 示例
- **关键发现**:
  - EverMemOS: 93% LoCoMo，需 4 个 Docker + LLM API，内测中
  - Qdrant: <10ms 延迟，最轻量 (1 Docker)，91% 内存压缩
  - pgvector: 已集成但 >200ms 延迟
- **下次计划**: 验证现有功能、选择记忆系统、部署上线

### 2025-01-23
- 重构AI编程指导体系
- 建立哲学化文档系统
- 简化工具使用规则

---

## 研究资料

### 论文
- [Bridging ASR and LLMs for Dysarthric Speech Recognition](https://arxiv.org/abs/2508.08027) - Interspeech 2025
- [Zero-shot MLLM for Dysarthric ASR](https://arxiv.org/abs/2406.00639) - Interspeech 2024
- [Generative Error Correction with LLMs](https://arxiv.org/abs/2409.09554) - 2024

### 相关项目
- [TEN-Agent](https://github.com/TEN-framework/TEN-Agent) - 实时语音Agent框架
- [CLEAR-VOX-MODEL](https://github.com/AIden-QiU1/CLEAR-VOX-MODEL) - 构音障碍语音研究

---

## 开发原则

1. **可理解性优先于准确性** - 容忍模糊，理解意图
2. **实践驱动** - 不过度设计，每步来自真实需求
3. **渐进增强** - 先可用再好用，最后优秀
4. **工具服务于目标** - 不炫技，不滥用工具

---

**让每个声音都被听见** 🎗️
