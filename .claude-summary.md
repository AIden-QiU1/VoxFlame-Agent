# VoxFlame Agent - 当前项目摘要

> 最后更新：2026-09-04
>
> 这里只保留启动会话必须知道的现役事实、近期决定和未完成边界。完成流水不在这里长期累积；历史以 Git、专项文档和研究登记为准。

- 2026-09-04 已建立四项采集需求总执行入口 `docs/VOICE_COLLECTION_FOUR_REQUIREMENTS_EXECUTION_2026-09-04.md`。LiveKit 扩容代码已补单机保护：active jobs/CPU/内存联合派单，当前 2 GiB Agent 容器默认 2 个 Job，ASR/LLM/TTS 各 2 个跨进程槽位，健康检查和 30 分钟 drain；精确版本升级为 Agents 1.7.1 / Server 1.13.6，并新增每周 GitHub Issue 升级提醒。8001 SSH 反向隧道已恢复，三名真实注册用户到 EXP-29/24/25 的路由及未知账户公共 fallback 已通过真实 WAV 审计；本机不再监听 8000/18000。集群总配额、双 Worker、ASR 容量/高可用和真实 RTC 压测仍未完成，不能承诺 1,000 路。阿里云扩容仍按控制面预发布、Backend 持久事件硬门、单节点 LiveKit、双 Agent Worker、证据驱动多节点推进；购买资源、provider 配额、生产 migration、正式 DNS/流量、释放旧资源和 App 发布必须由用户确认。

- 2026-09-04 已完成录音基础准入 Phase 1 与训练导出 Phase 2（尚未部署/未执行生产导出）：Backend 上传签名/完成登记核验当前 Auth 四项授权与 OSS 对象事实；现役 `export:audio-target` 又增加训练前 hard gate，只接受当前授权、服务端 `admitted`、训练 scope、DB receipt/OSS 活动 manifest 一致、对象未变化且质量未明确拒绝/重录的样本。导出创建不可覆盖的版本化 staging 快照，音频带 SHA-256，候选超 limit 整批失败，并按 contributor 做确定性 speaker-disjoint split。原始录音不因质量状态删除。仍需 DB/manifest fail-closed 状态机、历史 backfill dry-run、上传时内容哈希/音频头核验、重复检测和逐题来源回填。
- 2026-09-03 已完成 RO-015 方言配对采集研究登记：用户自报方言只作来源标签；普通话/方言共享语义题面并用 `utterance_pair_id` 关联；方言可跳过，ASR 不匹配不得作为拒收门槛；同一 contributor 的 pair 必须同 split。研究状态 `evidence_review`，仅支持内部原型，尚无 VoxFlame 方言模型收益或设备证据。报告：[RO-015](research/speech-health/RO-015-dialect-paired-speech-collection-2026-09-03.md)。
- 2026-09-03 注册授权已升级为版本化四项分开同意：隐私、语音/健康敏感信息、数据采集、商业用途（模型训练/评测/产品改进/服务运营）；不出售身份信息、不作违法用途，并明确撤回、删除、导出和匿名化后不可逆边界。Web/App UI 可各自适配但必须共享同一授权契约；旧账号不会静默补勾选，缺当前版本授权时训练上传需重新确认。
- 同轮修复训练元数据断链：前端与 Backend 白名单现保留 `speech_variant`、`dialect_name`、`utterance_pair_id`、`language_tag`、`dialect_name_user_reported`、`label_source` 等字段；注册方言标签写入 `label_source=user_reported`。尚未实现训练页配对状态机，尚未执行注册画像 migration 或部署。
- 2026-09-03 已删除原 60 条“概况 + 提纲”模板材料，替换为 81 篇经北京鲁迅博物馆全文页与固定版本第二底本互校的鲁迅完整作品；26 篇因乱码、缺底本、过短、正文差异或疑似残缺被拒绝。全文共 219991 个汉字，按逗号、分号、冒号、顿号、句末标点、换行、省略号和破折号等自然停顿生成 29415 个录音单元，其中 28574 个（97.14%）不超过 16 字；其余 2.86% 因原文没有更细自然停顿而原样保留，不硬切、不改写。每篇保存作者、刊载信息、来源链接、访问日期、原页哈希、公版依据、镜像 commit、互校覆盖率和正文哈希；没有可核验全文或权利依据的材料不得进入运行时。
- 本轮验证：重复执行全文导入后仍生成同一 81 篇目录且文件 SHA-256 不变；Frontend 130 项测试、TypeScript、Next 25/25 production build，Mobile typecheck/check/training tests 与 Android/iOS Expo export 均通过。API 实测返回 81 篇，`luxun-1477` 返回完整正文和 122 个录音单元，旧 `reading-001` 返回空；Playwright 本地无登录态时精确跳转登录页且 console 0 error/warning。全文目录尚未部署。
- 2026-09-03 已完成第 2 项 Web 真实注册验证：Playwright 通过真实注册页提交合成测试资料和四项授权，账号自动确认邮箱后跳转 `/contribute`；生产 Supabase 核对画像、方言与授权 metadata，证件类型/证件号未进入 Auth metadata。Mobile 仅完成生产配置 TypeScript/Android+iOS bundle，真实短信 OTP 与设备交互仍 pending。

- 2026-08-31 采集说明已按用户要求改为极简页面语言：只保留开始前准备、录音步骤、页面提示、注意事项、保留/重录判断、文章录音和陪同话术；移除接口、代码、内部编号、音频参数及验收等级。根目录 DOCX 已重新生成，A4 两页，AI docs harness 通过。
- 2026-09-01 根据 Android 真机录屏修复 Mobile 数据录入入口：原页面把必须完成的 3 项录音确认放在数屏之外的顶部，开始按钮只弹窗、不回到确认区，且“下一句”可以绕过确认，导致用户能切题却始终无法录音；共享 ScrollView 还会把长列表的旧滚动位置带入录音页。现已将确认直接放到目标句与录音按钮之间，未确认时主按钮明确显示“先完成上方确认”并同时锁定前后切题，路由切换自动回到顶部；录音页移除重复题库列表，可选年龄/性别下移，Android 顶部补安全区。Mobile typecheck、training/communication/memory tests、静态检查及 Android/iOS Expo export 均通过；未创建云构建、未发布，仍需 Android 真机验证确认、录音、连续下一句和顶部状态栏。
- 2026-09-03 已修复并部署 Web/Mobile 共用的训练录音撤回 Backend：8 月 31 日加入的 `PutObject + If-Match` 条件重写不受当前阿里云 OSS 支持，生产连续返回 `A header you provided implies functionality that is not implemented.`，导致撤回在 manifest scrub 阶段终止。现改为在既有“单账号 artifact 操作串行”边界内使用普通 PutObject 重写，同时修复重写后 Normal object 的再次追加。Backend OSS/撤回 22 项测试、TypeScript build、真实 OSS `追加 -> 重写 -> 再追加` 探针和部署容器内完整 discard flow 均通过；临时对象已删除，仅重建 Backend，回滚镜像为 `voxflame-agent-backend:pre-withdraw-fix-20260903`。此前失败的撤回保留 DB lookup 与 tombstone，可由客户端原操作安全重试。

## 产品与运行时

- 第一原则：不是纠正用户的声音，而是纠正系统对用户意图的理解；产品优先级是 `可理解性 > 低延迟 > 可打断 > UI 精修`。
- Web 当前任务结构：`/communicate` 负责快速表达与按需进入实时沟通，`/practice` 负责选择筛查或数据录入，`/assessment` 负责 20 词筛查，`/contribute` 与 `/contribute/topic/[topicId]` 负责训练数据录入。
- Web 数据录入入口已按用户任务重排为“马上录几句 / 用自己的内容 / 按沟通场景选择”；8 个公共主题直接展示题库真实可录句数。自定义材料可在训练页直接上传 `.txt/.md` 或粘贴原文，仍写入 workspace 材料唯一事实源，自动设为当前材料并切句进入录音。
- 面向用户的音系覆盖任务统一称为“系统易漏听字词”，不再使用“补齐声音 / 核心补音”；页面只解释系统为什么需要这些字词以及用户可以按平时方式说，研发覆盖台账留在内部证据层。
- Web 数据录入的“重录这一句”现在是严格替换：先撤回旧录音，再自动重录原句；保存中会等待完成后撤回，撤回失败不会开始新录音，也不会把新旧两版同时留在语料里。
- 2026-08-31 普通数据录入主流程已做标识减法：移除三步进度导航、本轮完成徽章、顶部时长/账号统计卡、“第 1/2 步”“目标句”“录音成功”、自动任务说明卡和重复质量摘要；保留当前任务、保存状态、题面、系统听到、回听与继续/替换/不收录。原 v1.0 SOP 未覆盖，新增可维护 Markdown、生成脚本及 A4 五页的 `燃言_构音障碍普通话语音采集标准化SOP_v1.1_Web对齐版.docx`，把同一标识原则固化为 Web/机构辅助采集标准。
- 2026-08-31 已修复“录完自动切到下一句后必须刷新才能继续录”：根因是页面把上一句后台上传的全局 `isUploading` 错当成下一句采集互斥锁。录音控制现只受当前采集的连接、整理和示例朗读状态约束；签名、OSS PUT、完成登记各有 20 秒上限，超时沿现有路径转本机待同步队列，并发上传使用活动计数避免状态提前清零。Frontend 121 项、TypeScript、production build、AI docs harness 与未登录 Playwright smoke 通过；未部署，真实账号麦克风连续两句及已登录标识视觉仍需设备验证。
- Web 长文朗读入口 `/contribute/readings` 只展示已通过完整正文、来源、权利与互校门禁的文章；列表显示 81 篇真实篇名和来源元数据，详情页先展示完整原文，再进入逐句录音。分句只移除作为边界的换行符，正文内汉字、标点和空格保持不变。原 `reading-001` 等模板文章已退出运行时，不能再通过详情页或训练目录 API 取回。
- Web/App 录音入口继续按低认知两层结构统一：第一层只保留“用自己的材料 / 选择已有材料”；第二层展示 8 个短句材料区和“完整文章 81 篇”。训练目录 API 返回已核验文章摘要，按文章 ID 定向返回完整原文、来源和版本化录音单元；Mobile 沿用同一事实源并在录音前显示全文预览。
- 录音区主统计改为“今日已录时长 / 累计已录时长”，云端 `voice_contributions` 与本机 IndexedDB 待同步录音合并。已录句、文章当前轮次和时长均为账户级全局状态；刷新、重新登录或换设备后恢复，断网录音先本机生效、同步后进入云端事实源。
- 2026-08-28 已完成活跃录音账号“像退出登录”的根因收口。生产证据为 852 次录音持久化成功、896 次 `/api/upload/progress` 因缺少 `reading_article_progress` 表失败、0 次 Backend JWT 校验失败；问题本质不是账号 308 的凭据失效，而是数据库发布不完整、失败请求风暴、上传 API 与 Frontend 发布耦合，以及多套前端 auth observer 把瞬时失败翻译成退出 UI。现已部署迁移，Supabase SDK 独占 session refresh，前端统一为单个 `AuthProvider`，录音队列改为账户绑定、single-flight、恢复事件触发和指数冷却，本机保存不再冒充云端成功。
- 生产请求边界已收口：Caddy 将除 Next-owned training/corpus-review API 外的 `/api/*` 直接路由 Backend，Frontend 重启不再中断上传 API；旧 `/api/session/*`、`/api/agent/*` 501 兼容壳已删除并在生产返回 404，唯一运行时会话事实源保持 `/api/rtc/session/*`。上传与 conversation webhook 日志不再记录目标正文、表达正文或账户存储路径。
- 训练进度现在是严格的账户级句子清单，而不是登录会话状态：云端 `voice_contributions` 提供已读 ID 集合，并推导每个普通主题/自定义材料最后成功句；页面显示全部/已读/未读并支持逐句筛选。默认进入下一条未读，全部读完后从上次位置之后继续复练。刷新、退出再登录和换设备读取同一份记录；本机待同步录音按 contributor ID 隔离，重新登录原账号恢复且不会串到其他账号。
- 2026-08-28 已继续收口“每录一句后反复卡在正在准备训练页”：直接原因是上传成功和本机队列清空会重复触发全量进度刷新，而普通短句页把后台刷新误当初次加载并整页遮挡；账号 308 的 2772 条记录使旧进度读取实测约 6.2 秒。现已部署数据库侧 `get_recording_progress` 汇总与组合索引，真实账号调用降至首次约 1.2–1.5 秒、后续约 0.3–0.5 秒；前端刷新 single-flight、8 秒可退出且不再遮住普通训练页，旧 memory 队列也改为 single-flight，避免服务恢复时整队列并发重放。问题不是“录了两小时所以系统容量耗尽”，而是全量历史读取与 loading/重试状态耦合。
- 2026-08-29 已完成“每个录音区录完又重复当前句”的五步现役风险收口。根因不是题库本身，而是题目游标、ASR、账号请求、录音快照和跨存储持久化缺少共同作用域：Web 现以完整 recording envelope 推进，ASR 只给建议；progress 按账号/generation 隔离；Mobile 冻结账号/题目/lineage，分页不重置且页末进入下一页或完成态，本机队列按账号串行；Backend complete/discard 同账号串行，撤回使用终态 tombstone + 单账号串行 scrub + 音频后删 + DB 最后删，并校验路径归属和撤回多标识一致；所有 manifest 消费者统一折叠 tombstone。
- 自动验证通过：Frontend 116 项、TypeScript、25/25 production build；Backend RTC、路径策略 2 项、artifact/撤回 19 项及 build；Mobile training、memory/account scope、typecheck、静态检查和 Android/iOS Expo export。尚未部署，也未完成账号 308、Android/iOS、双设备和生产 canary。完整共享 reducer/model-based 28 场景框架仍是后续工程目标，不能宣称已全部实现；Expo 发布环境还需移除 `NODE_TLS_REJECT_UNAUTHORIZED=0` 后重新打包。
- 匿名快速表达使用本机短语、文字发声和大字展示，不创建 RTC session；用户明确进入日常沟通后才登录并挂载实时 agent。
- 记忆页是画像、场景模板、热词、个人材料和短语的维护事实源；沟通页只消费 workspace 准备，不再维护第二套配置。
- 运行时唯一主链：`Frontend LiveKit RTC/Data -> Backend /api/rtc/session/* -> self-hosted livekit-server -> livekit_agent`。不得恢复 WebSocket 或 TEN/Agora 主链。
- 已认证的沟通/训练会话统一由 `livekit_agent` 调用 `http://127.0.0.1:8001/transcribe`，后端签发可信 `asr_account_id`，agent 仅在服务间请求头 `X-Account-ID` 中传递。模型服务独占账户注册、个性化模型选择、版本晋升与公共 fallback；VoxFlame 不维护逐用户白名单。历史数字 QQ 账号沿用邮箱数字前缀，其他账号使用稳定 Supabase UUID，因此新增用户模型不需要修改或重启应用。
- durable memory 由 `backend + workspace snapshot` 承担；LiveKit 只负责 session-local state 和会话原始材料。`dataset != memory`。
- 2026-08-27 Web App/PWA 已正式退役：Web 保留直接打开即用的沟通、练习和准备；Android / iPhone 原生内测统一从 `/download` 获取。已移除 manifest、service worker、安装/更新/离线提示及 next-pwa 依赖；首次打开新版 Web 会注销旧 service worker 并删除旧 Cache Storage，IndexedDB 录音待同步队列不受影响。浏览器已有桌面快捷方式无法由网站远程删除，需用户在系统中手动移除。
- 模型/provider 仍以 DashScope / Qwen-first 为现役约束。

## Mobile Workbench

- `apps/mobile-workbench` 是 Expo / React Native sibling client，不是 WebView；复用 backend 的 workspace、recording envelope、upload receipt 和 RTC orchestration contract。
- 一级 surface 保持 `communication / practice / memory / device`；信息架构以业务能力对等为目标，不做桌面页面像素复制。
- Mobile 沟通首页已经与 Web 产品任务对齐为“快速表达 / 语音助手”两条独立入口。快速表达未登录即可用，短句点击即由本机朗读，并支持自定义文字、复制和全屏展示；不创建 RTC、不上传声音。登录后合并 workspace 个人短句；只有进入语音助手并主动开始沟通才创建 RTC，文字可显式发送给 agent。
- Mobile 训练首页已直接展示“马上录 / 自己的材料 / 8 个主题”，题目数来自 `/training/catalog`；“现代文章朗读”作为明确主题出现。有材料时直接进入材料逐句录音，无材料时进入准备页添加；20 词筛查保留为次级任务。
- Mobile 材料页已支持 Android/iOS 原生 `.txt/.md` 导入与多材料选择；材料继续保存到 backend workspace 唯一事实源。准备材料切句已统一为 backend 返回的 `prepared_expression.practice_lines`，Web/Mobile 不再各自重新解析原文。
- Mobile 训练录音已改为录后显式确认：录音停止后先保存在本机，用户可回听、确认收录、严格替换重录或不收录；确认前不上传。替换操作只有在旧录音成功撤回后才开始新录音，撤回/上传失败都会保留当前录音供重试。
- Mobile 记忆页已覆盖场景模板、自定义重点词、完整沟通画像、材料和常用短句；这些能力都复用 backend workspace 唯一事实源。模板热词和用户自定义重点词分别持久化后合并消费，切换模板不会再覆盖用户重点词。
- Mobile 全流程真机验收已从“四页面打开”升级为 13 项证据门，Android/iPhone 必须分别提交真实设备 JSON 并通过校验。实际发布版本为 `0.1.7`、Android/iOS build `8`；Android build `463f705e-a125-43d6-a6dc-b5df98e74673` 已发布，SHA256 `821d0a3573c4738811bbe75085dd4eefab31d6c8f6cfe1f11941ed2ee8775762`。Linux 主机只能完成代码、bundle、环境和发布验证，实体设备结果在未提交前保持 pending。
- 第 6 项所需 Backend 已于 2026-08-27 从已验证发布提交 `d0a0525` 隔离构建并部署到生产，未带入当前工作树的 PWA/RTC 未提交改动；`https://voxember.com/api/rtc/health` 返回 200，`POST /api/memory/hotwords` 未登录返回 401（确认正式路由存在而非 404），容器健康。旧镜像保留为 `voxflame-agent-backend:pre-mobile-0.1.7`。
- 当前不能宣称 App 完全替代 Web。仍需 Android/iOS 真机验证：登录、RTC、训练录音与 LiveKit 并行、上传/重试/撤回、TTS/复制、档案完整编辑和断网恢复。
- iOS `0.1.7 (8)` Preview 构建已实际尝试：EAS 成功加载 Preview 环境并识别项目，但项目当前没有 iOS build credentials，也没有适用于 internal distribution 的 Provisioning Profile；必须由 Apple 账号持有人登录并登记设备/生成签名后才能产出可安装包。当前 Linux 主机也没有连接 Android/iPhone 设备、`adb` 或 iOS 工具链，因此两端真实设备证据仍保持 pending。

## 研究与硬件

- 应用研究统一进入 `research/` 五个主题；模型实验原始事实来自 `references/clear-vox-model` 固定 submodule。应用结论必须进入 `research/APPLICATION_FEEDBACK_REGISTRY.md`。
- 权威发布门禁、来源真实性、成果初审和反馈闭环以 `research/RESEARCH_HARNESS.md` 为准；论文、监管材料、专家意见或单次 demo 都不能单独证明产品有效。
- 燃言硬件当前路线：`P0 现成设备 A/B -> G1 随身音频桥` 是近期证据链；G2 是条件产品，G3—G7 是独立产品线期权，不构成自动订单、排期或预算。
- 两份最终外发 Word 位于仓库根目录：首家供应商修订版与通用供应商版。正文品牌仅为“燃言”，无研究元叙事、参考资料章节或内部谈判底牌。
- 参数内部事实源是 `research/product-engineering/evidence/ranyan-hardware-product-2026-08-17/`。原厂事实、候选配置、采购/测试目标和未知必须分开；芯片资料不等于整机性能，未实测数字不等于冻结 BOM。

## 安全与发布边界

- Supabase Auth 是身份事实源；服务端 privileged key 不进入前端、prompt 或日志。公开注册、邮箱确认、CAPTCHA、速率限制和 Mobile token 接入必须作为一组评估，不能单边切换导致登录中断。
- 手机短信登录已经接入腾讯云发送链路，但真实负责人手机号注册、验证码确认、再次登录与 Android 真机 smoke 仍需人工完成。
- 生产日志与移动诊断不得记录 OTP、完整手机号、表达正文、原始音频或 secrets；安全整改的长期事项仍以专项整改文档为准，不在启动摘要复制事件流水。
- Docker 部署使用 `scripts/docker-rebuild-core-fast.sh` 的最小影响模式；磁盘维护先跑 `scripts/docker_disk_maintenance.sh status`，再用 `prune-safe`，不默认执行 `docker compose down` 或广域 prune。
- 2026-09-03 Docker 自动磁盘维护阈值已从 75% 调整为 60%；50% 告警、60% 执行安全清理、85% 人工升级。已在根盘约 74% 时验证触发并回收约 2.018GB；修复 systemd `DOCKER_CONFIG` 只读问题后服务状态为 `0/SUCCESS`。清理全部 dangling images、7 天前停止容器、未使用网络和全部未使用 Build Cache，保留运行容器、卷、`latest` 与 `pre-*` 回滚镜像。
- 同日补充宿主日志轮转：`/etc/logrotate.d/voxflame-host-logs` 对 `logs/*.log` 每日轮转、50 MiB 提前轮转、保留 14 份压缩副本；JSONL 诊断/业务数据不纳入自动删除。

## cpu1 可靠性加固（2026-08-27）

- `cpu1` 的 2026-08-27 15:59—16:17 中断不是 Docker OOM：主机日志在 15:59:01 突然终止且没有正常关机、OOM、kernel panic、磁盘或网卡错误，16:17:41 冷启动；本机也没有 `sudo/systemctl reboot` 或无人值守自动重启记录。主机内只能确认宿主被突然切断；最终归因仍需在腾讯云核对实例 `ins-ehhkm5us`（`ap-guangzhou`）对应时段的实例事件、操作审计和宿主迁移记录。
- 当天公网 SSH 有 5,000+ 次未认证异常并触发 `MaxStartups` 节流，能解释部分“SSH 连不上”，但不能解释硬重启。现已关闭密码/交互认证并收紧 `LoginGraceTime=20`、`MaxAuthTries=3`、`MaxStartups=20:40:60`，启用 Fail2ban；受控重启后已实际封禁扫描来源，同时真实公钥登录成功。
- 系统安全包与内核已更新并受控重启，当前运行 `5.15.0-190-generic`。2 GiB `/swapfile` 已作为紧急缓冲自动挂载；重启后可用内存约 4.9 GiB，Swap 使用 0。
- Docker 26.1.3 本身无本次故障证据，因此没有冒险跨大版本升级到 29。五个容器已设置内存、PID 和 `10m × 3` 日志边界，均 `restart=unless-stopped`；受控重启后自动恢复，OOM/restart 计数均为 0，公网 `/api/rtc/health` 为 200。安全清理回收约 490.8 MB，保留运行镜像、卷与回滚镜像。
- Android fallback 同步已改为 GitHub API SHA 预检、15 分钟随机轮询和 `ssh.github.com:443` 拉取，并登记 cpu1 专用只读 deploy key；受限用户 `git ls-remote` 与 systemd 同步服务均验证成功。
- 每分钟主机健康探针已启用，记录 boot ID、内存/Swap、磁盘、负载、路由、GitHub/DashScope/公网 RTC 和容器状态；本次受控重启已正确记录 `boot_changed`，以后可区分主机重启、网络故障与应用故障。

## 当前未完成事项

- 2026-08-29 Harness 入口进一步收口：`AGENTS.md` 现在是第一跳，新增 `docs/aiprompts/HARNESS_ENTRY_CONTRACT.md` 定义任务分流和完成契约，新增 `research/HARNESS_RULES.yaml` 作为阈值/动作/人工确认的机器可读唯一事实源。研究闭环校验器现会验证 RO→证据包→成果审查→反馈→应用回流关联；所有相关检查已通过。

- 2026-08-29 已按 Research Harness 对照清理根 `AGENTS.md`：移除易过时的历史状态和重复说明，只保留长期协作规则、唯一运行时边界、研究闭环路由、验证底线和文档入口；闭环具体规则统一在 `research/RESEARCH_HARNESS.md` 与 `scripts/research/*`。

- 2026-08-29 已将三个工程问题正式纳入 Research Harness：`RO-012` 训练进度稳定性、`RO-013` Docker/宿主资源治理、`RO-014` LiveKit/ASR/Agent 并发容量；各自具备报告、证据包、成果初审、反馈和应用回流登记。新增研究触发器与闭环校验脚本：工程阈值可自动生成待审反馈，但真实设备/用户试点、扩容采购、数据删除、范围扩展和对外发布仍需人工确认。RO-014 仍无真实 RTC 容量结论。

- `reading_article_progress` 迁移、Backend、Frontend 和 Caddy 路由均已部署；部署后账号 308 继续产生并成功持久化新录音，缺表错误不再出现。仍需在具备真实登录态和麦克风设备时完成：整篇完成、重置后未录第一句即刷新、退出重登、跨设备恢复、离线补登、撤回后时长回算的端到端设备 smoke。当前材料为燃言原创内部产品材料，不是普通话水平测试官方作品；后续 PSC 作品必须在合法持有或明确授权后受控导入。
- Supabase migration history 仍有历史漂移：远端存在但本地缺少 SQL 的 `20260228000002`—`20260228000006`。在重建并核对这些迁移的实际 SQL/数据库状态前，不得盲目执行 `migration repair` 或 `db push --include-all`；本次 `20260828000000` 已单独登记 applied。

- 普通话采集已经切换为“语言学覆盖优先”：SOP 只作现场操作参考，覆盖事实源为 [research/speech-health/MANDARIN_LINGUISTIC_COVERAGE_AND_COLLECTION_BASELINE_2026-08-22.md](research/speech-health/MANDARIN_LINGUISTIC_COVERAGE_AND_COLLECTION_BASELINE_2026-08-22.md) 与可重复审计脚本。基础题库为 9107 条，另接入 263 条核心补音、291 条低频补强和 14 条开放研究补充录音就绪题面，索引共 9675 条；相对 9112 仅退出 5 条“学习包”商业污染，历史录音、manifest 和原始来源未删除。基线台账仍按 9107 条普通题面报告 `569 robust / 456 below_minimum / 217 missing`；录音就绪题面另有明确 `coverage_targets` 证据，不能冒充真实录音。审计现在同时输出通用字形注音与显式目标两条覆盖通道，避免阿胶/心脏/炸鱼等多音字被默认注音误报。
- 1242 项覆盖目标台账仍为 `569 robust / 456 below_minimum / 217 missing`；硬缺口按现代词、实际使用证据和用户负担收紧为 `88 core / 121 edge / 8 disputed`。88 个核心目标的原始审核包仍保留 263 条历史候选；新增 `mandarin-recording-core-gap-corpus.json` 按可复现证据 gate 放行 263 条录音题面（88 词、175 短句），已接入音系强化录音区，状态为 `recording_available_machine_checked`。录音区不再等待主观审核或人工 spoken_text。
- 基础题库的可执行口径为 `8771 外部生成子池 + 336 人工策划/固定评估项`；相对 9112 只删除 5 条学习包污染。核心候选已进一步把高负担锚点替换为同音中性词；175/175 条短句均附句内整词与整词拼音证据。另有 291 条低频补强题面通过同一机器门进入录音区，55 条因长度门拒绝并保留在候选证据中；263 条核心候选仍全部 pending、正式六项审核导出仍为 0。
- 456 个 `below_minimum` 目标仍按基础题库建立独立采集调度：455 个默认核心进入计划、1 个争议项保持下线；基础安全题库中选出 835 条题面并分配 2998 个未来槽位。另有 291 条机器校验新增补强题面、覆盖 116 个目标，已进入“低频补强·录音就绪”索引；有效音频 + 非空 target 即可计入采集覆盖，异常只做质量分层。
- 263 条核心候选已导出为 9 批 TSV 审核工作表，包含目标音、文本、来源、整词读音证据和六项审核栏；它只服务人工审核，不是生产语料入口。
- 263 条候选已同步生成站内 `/corpus-review` 审稿台：页面/API 需登录且审核邮箱必须在服务端 `VOXFLAME_CORPUS_REVIEWER_EMAILS` 白名单；草稿只存本机，导出 decision JSON 后还需 CLI 快照校验、合并和六项发布门，浏览器不能直接写生产语料。正式审核仍全 pending、生产导出仍为 0。
- 146 个安全题面稀少的低频目标已建立第二个互斥审稿任务：完整 Tatoeba 快照经低负担过滤后保留 346 条唯一待审句、354 个目标—句分配；118 个目标已有 3 条候选，28 个目标仍需补写。28 个 brief 均已结构化记录风险类别、允许证据、默认录音政策和下一动作，交由语言学专家确认中性承载词、转 edge 或保持下线；可搜索和导出独立 authoring worksheet，但不能直接写生产。全部候选仍 pending、生产 0，现役 9107 条题库和历史录音未因此删除。
- 2026-08-23 清理了前端生成题库中的 5 条“学习包”类课程/考试营销噪声（如“初级护师学习包”），仅更新题面生成副本并在 `mandarin-training-real.cleanup-audit.json` 留痕；历史录音、应用 manifest 和原始来源未删除。`rejection_reason` 已将学习包/课程包/培训包/资料包纳入逐句商业污染门。
- 2026-08-23 已新增人工 `spoken_text` 复核旁路：`build-mandarin-spoken-text-review-queue.mjs` 从应用 manifest 生成去重队列，ASR 仅作提示，队列不携带用户/设备字段或原始存储路径；当前 10 个 manifest 可生成 1185 条 `pending`。该旁路不阻塞录音或默认采集覆盖。
- 1185 条可选 spoken-text 诊断队列已保留；采集证据按 manifest 的有效音频 + 非空 target + 授权/上传契约计入 1180 条 collected，质量状态分为 `valid / suspected_misread / suspected_omission / long_silence / unusable_audio`。旧复核支线已从现役产品、脚本、统计路径和证据包移除，不参与录音或覆盖。
- 真实录音质量诊断已产品化为受保护的 `/corpus-review/spoken-text` 工作区：审核邮箱白名单控制访问，服务端按不透明 `recording_id` 提供受控音频，ASR 仅作非权威提示；人工 `spoken_text` 与 audio-text 对应确认只用于质量诊断，不阻塞采集覆盖，workspace `training_import_allowed=false`。
- 真实录音决定现有离线收口 CLI：`validate-mandarin-spoken-text-review` 校验精确队列快照、审核者/时间和批准条件，`merge-mandarin-spoken-text-review-decisions` 仅稀疏更新提交项，未提交项保持 pending，合并产物仍禁止训练导入。
- 历史旧复核归档不作为现役入口、覆盖门或训练导入依据；原始音频、manifest、普通题库和来源材料均保留。
- 双人复核支线已从工作树删除：页面、入口、脚本、测试、统计/证据包和生成工作区均不存在；Git 中显示为待提交的 `D` 变更。现役仅保留单人、受保护、可选的 spoken_text 质量诊断，且不阻塞录音、覆盖或训练导入。
- 沟通入口已拆成 `/communicate` 选择页、`/communicate/quick` 快速表达和受保护的 `/communicate/assistant` 日常沟通，两个任务不再相互嵌套；真实账号下的助手连接仍需设备 smoke。
- Web 数据录入已收成单列三步引导：主题入口默认只突出推荐主题和自定义材料，主题内按 `准备 -> 录一句 -> 确认结果` 推进；录音完成有独立正向反馈，并保留回听、重录、不收录和继续下一句。真实账号的麦克风、识别和上传仍需设备 smoke。
- 2026-08-24 已修复重录逻辑与反馈：替换期间明确显示撤回状态并锁住冲突动作；词项 2–6 字、短句 7–18 字规则分别测试。音系强化录音页默认打开“核心补音”，并新增独立“开放研究补充”组（14 条非教材、非训练导入批准），同时保留“低频补强”组；训练导入审核与录音就绪状态分开表达。产品状态唯一事实源记录录音就绪题面 `568 条 / 568 个唯一文本 / 206 个显式目标`，页面直接消费该总计。语料完整性/录音 gate/采集证据回归已更新；Playwright 未登录保护 smoke 通过，真实账号下的上传撤回、麦克风重录与新增题面的真实录音仍需设备验证。
- 2026-08-24 证据重算确认：10 个历史 manifest 共 1421 行、去重后 1185 条、采集资格 1180 条；历史 `prompt.target_focus` 实际是“补稳‘请’/收住‘对’”等反馈标签，不是音节—声调目标，已在采集覆盖审计中按 `^[a-züê]+[0-5]$` 过滤，避免把旧录音误报为显式音系覆盖。新增题面的前端 `coverage_targets -> pronunciation_targets`、后端 `pronunciation_targets -> prompt.target_focus` 链路已由回归测试固定；真实新增题面录音尚未产生，因此历史 manifest 的显式音节目标覆盖当前为 0，不能冒充 568 条已录音。
- 2026-08-24 新增 `frontend/scripts/rebuild-mandarin-recording-evidence.mjs` / `npm run rebuild:mandarin-recording-evidence`：从本地不可变 manifest 一键重生成采集证据、录音就绪覆盖、speaker-disjoint 切分和产品状态；当前输出稳定为 1421/1185/1180、显式音节目标 0、切分 555/429/196、说话人交集 0。该命令只写派生 JSON，不改音频、manifest、题库或来源。

- 语音采集产品化标准已落在 [docs/VOXFLAME_VOICE_COLLECTION_PRODUCT_SPEC_2026-08-18.md](docs/VOXFLAME_VOICE_COLLECTION_PRODUCT_SPEC_2026-08-18.md)：默认训练元数据收口为音频、目标文本、实际转写、严重程度、病种、年龄段、性别；设备/质量字段只用于质检或 A/B，不作默认训练特征。
- Web 与 Mobile 数据录入已加入采集前环境/距离/授权确认；Mobile 上传 metadata 已改为白名单，仅保留目标文本、实际转写、训练标签、结果摘要和必要追踪字段，并继续复用本机 queue -> upload receipt -> manifest。移动端录音先落本机队列，LiveKit 只负责尽力提供实时转写，网络不可用时仍可录音、回放和后续补登。Mobile collection 还可选择 baseline/anchor/reading/natural_speech 计划。
- 可执行验收模板见 [docs/VOXFLAME_VOICE_COLLECTION_DEVICE_ACCEPTANCE_CHECKLIST_2026-08-18.md](docs/VOXFLAME_VOICE_COLLECTION_DEVICE_ACCEPTANCE_CHECKLIST_2026-08-18.md)，并明确原始母版与训练副本不可混淆。
- Android preview 构建已提交 EAS：`f032fbea-6a23-424d-8585-3f7bed07d022`。Apple 组织 Team ID `J2QXFF775Q` 已确认，但 iOS credentials/TestFlight 仍需账号持有人在本机交互完成，命令见 [docs/VOXFLAME_APPLE_DEVELOPER_INPUT_CHECKLIST_2026-08-18.md](docs/VOXFLAME_APPLE_DEVELOPER_INPUT_CHECKLIST_2026-08-18.md)。
- Mobile parity 已通过 PR #17 合入 `origin/main`；版本为 `0.1.4`，Android `versionCode=5`、iOS `buildNumber=5`。新版 APK 已重新构建并发布到 `https://voxember.com/download/android`；GitHub Actions 因账号 billing lock 未启动，按用户授权使用本地完整验证后直接合并。第 2 项快速表达/文字沟通代码与双平台 bundle 已验证，真实 Android/iPhone 全流程仍待第 7 项。
- 本轮本地验证已完成：普通话相关定向测试 30 项、Web 92 项测试、录音就绪/采集证据/产品状态回归、TypeScript、Next production build（24/24 页面）、AI docs harness 与 `git diff --check` 均通过；Playwright 已确认未登录访问审核页面正确回跳并保留目标路径，受保护 API 返回 401、console 无错误/警告；登录后的可选质量诊断工作区和真实设备录音仍需授权账号/设备 smoke。
- 个性化 ASR 网关代码、账户和模型契约已完成当前环境验证：GPU1 主动建立的 SSH 反向隧道在 cpu1 `127.0.0.1:8001` 正常监听；生产 Backend 按“完整数字 QQ 邮箱取数字前缀、其他账号取不可变 Supabase UUID”签发。真实 Supabase 对账和 WAV 推理确认：注册用户 `2187054680/2307294809/3083029019` 分别命中 EXP-29/24/25 且不 fallback，随机未注册键命中公共 `exp16l3-lambda025`；8000/18000 均无监听。新代码进一步删除 participant metadata/attributes 和认证 ID 兜底路由，Agent 只信任 Backend dispatch 并拒绝响应账户错配；participant token 不再携带认证用户 ID。可重复审计命令为 `cd backend && env -u NODE_TLS_REJECT_UNAUTHORIZED npm run audit:asr-models`。仍未证明 ASR 单实例并发容量或高可用；GPU 注册表旧公共别名需在 GPU1 备份后清理。

1. 用真实账号和真实设备完成 Web `/communicate`：个人短语、麦克风、LiveKit、confirmed output、本机朗读和故障降级 smoke。
2. 用真实账号完成 20 词整组录音、评分与报告，验证同人、同任务、同设备复测边界。
3. 完成 Android/iOS 真机 parity；iOS 先解决签名与设备授权。
4. 冻结 P0 硬件真实任务 A/B 协议；通过 Gate 后再向至少两家供应商发 G1 同口径 RFI/RFQ。
5. 完成真实短信注册/再次登录与 Android smoke；CAPTCHA/邮箱确认等注册强化在 Web、Mobile token 和管理权限同时就绪后再切换。
6. 将 session-local memory、workspace durable owner、会后压缩和训练报告边界继续收口，避免平行事实源。

## 当前验证入口

- 纯文档与研究：`bash scripts/check_ai_docs.sh`
- Web：`cd frontend && npm test && npx tsc --noEmit && npm run build`
- Backend：`cd backend && npm run build`
- LiveKit agent：`cd livekit_agent && python3 -m unittest discover tests -v`
- Mobile：`cd apps/mobile-workbench && npm run check && npm run typecheck`
- 浏览器 smoke 使用 Playwright；截图、页面快照与 console dump 是短期产物，不进入长期事实源。

## 权威入口

- 产品边界：[docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md](docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md)
- 当前任务：[.tasks/current.md](.tasks/current.md)
- 工程规则：[docs/AI_ENGINEERING_SYSTEM.md](docs/AI_ENGINEERING_SYSTEM.md)
- 文档导航：[docs/README.md](docs/README.md)
- 研究入口：[research/README.md](research/README.md)
- 能力注册：[docs/capability-registry.md](docs/capability-registry.md)
# Codex VS Code 插件 / cpu1 连接诊断（2026-08-18）

- cpu1 当前没有监听本地 `127.0.0.1:7897` 代理；交互式 shell、`~/.bashrc` 和 `~/.vscode-server/server-env-setup` 均已清除代理变量。
- VS Code Codex 日志曾明确记录 app-server 通过 `127.0.0.1:7897` 访问 GitHub 失败；历史 settings 快照也包含该代理配置。2026-08-18 09:28 新日志确认仅设置 `http.proxySupport: off` 不够：Codex 扩展会直接读取 VS Code 应用级 `http.proxy`，将其注入 app-server 的 `HTTP_PROXY/HTTPS_PROXY`。已在 `/home/ubuntu/.vscode-server/data/User/settings.json` 固定 `http.proxy: ""`、`http.proxyStrictSSL: true`，并终止旧 VS Code Server；重新连接后需检查新日志是否彻底无 7897。
- CLI 与插件不是同一网络链路：CLI 使用 `~/.codex/config.toml` 的 `gateway`（`https://aicoding.aideb.me/v1`），而插件还访问 ChatGPT 官方账户/用量服务。cpu1 直连 gateway 与 GitHub 成功，但 `chatgpt.com:443` 直连超时，因此清除旧代理后，插件仍可能因 ChatGPT 官方域名不可达而显示 Reconnecting。
- 即使 7897 清除，`chatgpt.com` / `ab.chatgpt.com` 仍需官方服务可达；若新日志只剩这些域名 `fetch failed`，必须配置真实可用代理或把插件运行在可直连 ChatGPT 的机器，不能把 gateway 地址填成插件登录地址。
- 2026-08-18 日志中的次要问题：插件远端插件目录需要 ChatGPT 登录（API key 不支持）、`apps_mcp_path_override`/`thread_tools` 是当前 CLI 不识别的 feature、旧本地插件 manifest 有 prompt 长度警告；这些不是主链路网络根因。
