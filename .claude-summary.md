# VoxFlame Agent - 项目概述

> 这里只保留当前产品定义、现役事实源、最近 3 天仍有效的收口和下一步必须知道的状态。

## 产品定位

- 核心目标不是“纠正用户的声音”，而是“纠正系统对用户意图的理解”。
- 当前主产品链路是：`沟通工作台 -> 练习工作台 -> 沟通档案(memory/workspace)`。
- 当前迁移主线已经进入清退阶段：沟通与训练执行面都收口到 `self-hosted LiveKit + livekit_agent`。
- `ten_agent/` 目录已经从仓库物理删除，旧执行面不再是代码基座的一部分。
- `frontend / backend / livekit_agent` 代码层已经没有 `Agora/agora_ten` 运行时引用，残留的只是不影响运行的历史迁移文档。
- `docker-compose` 默认启动路径也已更符合最佳实践：
  - 默认核心栈是 `livekit-server / backend / frontend / livekit-agent`
  - `qdrant / redis` 改为 `extras` profile，不再拖主链启动
  - LiveKit server 默认镜像已钉到 `docker.m.daocloud.io/livekit/livekit-server:v1.10.1`
  - 当前容器实测已处于 `Up` 状态

## 现役事实源

- 2026-04-19 记忆页“自定义材料”已经不再是单文档壳子：
  - backend durable shape 已改成纯粹的 `prepared_expression_assets.active_asset_id + assets[]`
  - 不再维持旧 `prepared_expression_asset` 单份 owner
  - memory API 也已改成复数资源风格：
    - `GET /api/memory/workspace/:userId/prepared-expressions`
    - `PUT /api/memory/workspace/:userId/prepared-expressions`
    - `PUT /api/memory/workspace/:userId/prepared-expressions/active`
    - `DELETE /api/memory/workspace/:userId/prepared-expressions/:assetId`
    - `POST /api/memory/workspace/:userId/prepared-expressions/:assetId/summarize`
  - `workspace snapshot` 已新增 `prepared_expression_library`
  - `object_zones.custom_materials` 现在会列出整份材料库，而不是只暴露当前一份
  - [frontend/src/app/memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 已支持多份材料的新增、选择、加载、展开、编辑、删除
- 一个未接入主路由、但会污染构建的训练页拆页半成品已经删掉，不再把未完成组件留在 `src/` 里
- 2026-04-19 训练页已经真正拆成两层：
  - `/contribute` 只做主题选择
  - `/contribute/topic/[topicId]` 只做录音训练
  - 当前 recorder route 不再夹带主题选择逻辑
- 当前关于“有没有多套过时 / 平行逻辑同时在跑”的最新判断：
  - 训练页：之前确实存在“主页面里继续跑录音工作区”的并行逻辑，现已收口
  - 沟通页：之前有一块重复展示 `communicationLoadout` 的摘要卡，现已删掉
  - 记忆页：当前没有再发现前台并跑的第二套旧材料管理 UI
- 现役执行面已经收口为 `frontend -> backend -> self-hosted livekit-server -> docker livekit-agent`。
- 模型/provider 现役约束仍是 `DashScope / Qwen-first`。
- `dataset != memory`，记忆继续由 `workspace / profile bundle / session review / expression kit` 语义承接。
- `LiveKit != durable memory owner` 这个判断也已经重新收清：
  - `LiveKit` 负责 session-local state、room state 和 session report raw material
  - `backend + workspace snapshot` 继续是 durable memory owner
  - `Qdrant` 适合后续 semantic recall layer
  - `Redis` 只有在明确需要 ephemeral coordination/cache 时再引入
- docs 目录也继续收口：
  - 已删除一个无引用、已脱离主线的旧研究文档：`CORPUS_SOURCE_RESEARCH_2026-03-19.md`
  - 也已删除三份已经和现役主线打架的旧 LiveKit 迁移文档：
    - `VOXFLAME_LIVEKIT_TRANSITION_PLAN_2026-03-31.md`
    - `VOXFLAME_LIVEKIT_REPLACEMENT_ROADMAP_2026-04-02.md`
    - `VOXFLAME_LIVEKIT_COMMUNICATION_MINIMAL_LOOP_PLAN_2026-04-02.md`
  - 当前关于 LiveKit、memory 和数据录入的现状与下一步，只以：
    - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md)
    - [.tasks/current.md](/home/ubuntu/VoxFlame-Agent/.tasks/current.md)
    - [VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md)
    - [VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md)
    为准
  - `PRD / current task / 历史深参考` 的边界也重新写清了：
    - `PRD` 负责产品定义、页面职责和当前代码现状
    - `.tasks/current.md` 负责短期执行顺序、验证和优先级
    - `control-plane / unified memory` 不再代表当前代码真相，只保留为深参考

## 最近 3 天有效状态

### 2026-04-20

- 腾讯云中国大陆上线文档这轮也已从“端口/预览清单”改成真正可执行的正式上线 runbook：
  - [docs/TENCENT_CLOUD_MAINLAND_DEPLOY_CHECKLIST_2026-04-07.md](/home/ubuntu/VoxFlame-Agent/docs/TENCENT_CLOUD_MAINLAND_DEPLOY_CHECKLIST_2026-04-07.md) 已整篇重写为“买域名 -> 域名实名认证 -> ICP 备案 -> DNS 解析 -> Caddy 自动 HTTPS -> 正式域名部署 -> 公网 RTC smoke”的顺序步骤
  - 当前文档不再把“公网 IP 预览入口”当正式目标，也不再把“多端口是否已开”当主线
  - 这轮也把当前代码真实边界写清了：
    - 第一阶段正式上线推荐走单域名 `app.<domain>` + `Caddy` 自动证书
    - 当前仓库还不应把“独立 `turn` 域名 + TURN/TLS + 单 IP”写成现成步骤
  - [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md) 已新增该文档入口，避免后续继续从旧清单视角理解大陆上线流程

- 训练语料库这轮按“只保留高质量普通话短句”继续收了一次：
  - 账号 `2307294809@qq.com` 对应用户 `64758dee-5026-4b53-a063-1d02d0834f67` 的旧训练数据已再次清空：
    - `voice_contributions: 51 -> 0`
    - 相关 OSS 训练对象与 `dataset/{userId}` 导出对象共删除 `53` 个
  - frontend 新增 [curated-topics.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/curated-topics.ts)
  - 当前训练页已不再直接使用低质量的四组旧生成语料，而是改成：
    - `日常与出行`: `88` 条
    - `看病与求助`: `67` 条
    - `人群与角色`: `89` 条
    - `设备与数字`: `160` 条
  - 这些句子全部限制在 `6-15` 字，并在文件内直接校验去重和长度
  - `发音与朗读` 按用户要求保持原状，没有替换
  - [mandarin-training-data/index.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/index.ts) 现在会优先使用这四组精选语料，只保留 `发音与朗读` 继续走原朗读库
  - 当前旧 [mandarin-training-real.json](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/generated/mandarin-training-real.json) 也已继续收口：
    - 四组旧生成语料 `日常与出行 / 看病与求助 / 人群与角色 / 设备与数字` 已从文件物理删除
    - 旧库只保留 `发音与朗读` 作为 fallback
    - 前端分类顺序改成显式 `MANDARIN_TRAINING_CATEGORY_ORDER`，不再依赖旧 JSON 的 key 顺序
    - 删除旧 JSON 四类后，训练页主题卡、句子计数和录音页分类选择仍保持一致
  - 选材依据主要来自：
    - `12306` 重点旅客/出行服务说明
    - `120` 急救求助电话官方指引
    - 政务热线 / 公共服务普通话与文明用语规范
    - 常见手机操作与数字表达的高频普通话场景

- 训练录音上传链这轮已开始强制收口到 `wav only`：
  - frontend 新增 [recording-to-wav.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/audio/recording-to-wav.ts)
  - 当前会在真正落 OSS / 进入本地补传队列之前，把浏览器录音统一转成 `16k / mono / PCM16 WAV`
  - [useVoiceUpload.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useVoiceUpload.ts) 现在不再把原始 `webm` 直接上传或入队
  - 这意味着后续新录训练样本会只保存 `wav`，不再继续长新的 `webm` 训练资产

- 训练数据集导出链这轮也补了一个更贴近真实训练使用的出口：
  - [backend/scripts/export_audio_target_dataset.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/export_audio_target_dataset.ts) 现在默认只导出：
    - `audio/`
    - `samples.jsonl` 里的 `audio + target + 可选 speaker_profile`
  - 不再把 `raw_transcript / recognized_text` 暴露成训练数据集字段；这些字段仍留在线上训练总结与错配统计链路里
  - `speaker_profile` 当前会优先读取：
    - 样本 metadata 里的 `training_guidance_profile`
    - 用户 profile/preferences 里的结构化标签（若未来补上）
    - 旧 `user_profiles.condition`
  - 训练页 [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 也已开始把本地 `training_guidance_profile` 写进上传 metadata，给后续数据导出预留病种/严重程度标签入口
  - 对真实用户 `2307294809@qq.com` 的当前检查结果：
    - `user_profiles.condition = null`
    - `preferences.user_profile_memory` 目前没有结构化 `etiology / severity`
    - 所以导出的 `samples.jsonl` 目前只有 `audio + target`
  - 已验证：
    - `cd frontend && npm run build`
    - `cd backend && npm run build`
    - `sudo docker exec voxflame-backend sh -lc "cd /app && npx tsx scripts/export_audio_target_dataset.ts --email 2307294809@qq.com --limit 100 --output-dir /app/tmp/dataset_2307294809_20260420"`

- 文档入口也继续收口了一轮：
  - 已删除两份会误导当前实现判断的过时计划文档：
    - `docs/LLM_CORRECTION_DEVELOPMENT_PLAN.md`
    - `docs/VOXFLAME_MEMORY_CONTEXT_MODEL_COMPACTION_EXECUTION_PLAN_2026-04-14.md`
  - 这两份文档都还停留在旧执行路径或旧治理阶段：
    - 前者仍以 `TEN extension / websocket / 独立纠错扩展` 为主线
    - 后者仍把前端 compaction / server-side compaction 作为主推进项
  - [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md) 与 [AGENTS.md](/home/ubuntu/VoxFlame-Agent/AGENTS.md) 的引用入口已同步清理
  - 已验证：
    - `bash scripts/check_ai_docs.sh`
- 沟通页的 durable 用户画像更新触发边界这轮继续收了一层：
  - frontend [memory-service.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/memory/memory-service.ts) 不再只在 `disconnect/endSession` 时尝试 `session-close`
  - 现在收到带有效 `compaction_candidate` 的 `session_userdata_ack` 时，会先做一次去重后的增量画像写回
  - `disconnect/endSession` 仍保留最终兜底，不再是唯一触发点
  - 这让同一条 RTC 连接里持续沟通时，也能逐步更新 durable `user_profile_memory`
- 训练页 `training-report-maintenance` 的候选筛选也按最新口径收紧：
  - 仍然保持 `daily/weekly` 的定期窗口
  - 但只有在对应窗口内、且自上次 summary 生成后确实出现了新的 `training_result` 样本，才会进入自动补刷
  - 不再因为“summary 过期但没有新内容”就反复调用训练总结模型
- 已验证：
  - `cd frontend && npm test -- --test-name-pattern="session-runtime writes session userdata ack memory summary into current session metadata|buildSessionCloseUserProfileUpdate"`
  - `cd backend && npm run build`

### 2026-04-19

- 首页这轮也已经从“入口导航页”收成更产品化的主页面：
  - [frontend/src/components/home/HomeDashboard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/home/HomeDashboard.tsx) 不再把重点放在：
    - 资源链接
    - 压力场景说明卡
    - 三个入口的重复解释
  - 当前首页改成三段式结构：
    - `3 个核心功能`：沟通 / 练习 / 记忆
    - `怎样用出效果`：真实句子、上传授权、只带本次需要的上下文
    - `三页如何配合`：练习页产出真实信号，记忆页整理材料模板，沟通页按场景勾选后开口
  - 首页文案也重新明确了产品主线：
    - 不是“多几个页面”
    - 而是把 `沟通 -> 练习 -> 记忆` 连成一个闭环
  - 这样首页更贴近产品对象：
    - 先解释系统能解决什么问题
    - 再解释怎样用好它
    - 最后说明三页如何协同，而不是只给跳转按钮
  - 已验证：
    - `cd frontend && npm run build`

- 首页在这轮又继续往“价值先行”收了一次：
  - 用户最新反馈是对的：首页不能主要讲“页面怎么分”，而要先让用户快速明白：
    - 这个产品好在哪
    - 为什么会越来越有用
    - 自己应该怎么开始用
  - 当前 [frontend/src/components/home/HomeDashboard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/home/HomeDashboard.tsx) 已进一步改成：
    - hero 直接说明产品价值：`更好沟通、持续变准、提前准备`
    - `这个产品好在哪`：先讲 3 个用户能感知到的价值
    - `三个核心功能`：沟通 / 练习 / 记忆，各自回答“解决什么问题、怎么用更好”
    - `怎么用`：明确 `练真实句子 -> 记忆页收材料模板 -> 沟通页勾选后开口`
  - 当前首页口径比上一版更贴近产品认知，而不是信息架构说明
  - 已验证：
    - `cd frontend && npm run build`

- 首页与训练录音页这轮又继续往“更少字、更强主线、更像工作台”收了一层：
  - [frontend/src/components/home/HomeDashboard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/home/HomeDashboard.tsx) 现在已经去掉大段解释型信息块
  - 当前首页改成：
    - 一个更短的价值导向 hero
    - 三个核心能力卡：沟通 / 练习 / 记忆
    - 三个简短说明块：为什么会越来越准、什么时候去记忆页、最简单的开始方式
  - 首页不再把“为什么 / 功能 / 怎么用”拆成多层大标题和大段文字，当前更接近“用户一眼看懂产品好在哪、现在该点哪里”
  - [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 里的 `TrainingRecorderPage` 也从“上半准备、下半录音”的纵向堆叠，改成了真正的左右双栏：
    - 左栏：主题信息、准备内容、句子序列
    - 右栏：当前句、录音、实时识别、本次结果
  - 这让训练录音页更像工作台，而不是长页面滚动阅读
  - 已验证：
    - `cd frontend && npm run build`

- 这轮把“旧单材料字段导致参考材料凭空消失”的真实数据坑定位并收掉了：
  - 账号 `2307294809@qq.com` 的参考材料并没有丢，而是还留在旧 `preferences.prepared_expression_asset`
  - 前台/快照在 2026-04-19 切到纯 `prepared_expression_assets.active_asset_id + assets[]` 后，这类历史用户会被错误显示成“没有材料”
  - 当前 backend [supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 已新增迁移逻辑：
    - 读取 workspace snapshot / prepared-expression library 时，如果发现只有旧字段、没有新材料库
    - 会自动迁移到新 `prepared_expression_assets`
    - 并通过 `buildPreparedExpressionPreferences(...)` 直接删除旧 `prepared_expression_asset`
  - 同时已对该真实用户做了一次数据库迁移校验：
    - `has_legacy = false`
    - `has_library = true`
    - `library_count = 1`
    - `active_asset_id = 演讲-manual-input`

- 沟通页 `communication loadout` 这轮也继续朝“会话前准备区直接选材料/模板”收口：
  - backend `buildCommunicationLoadout(...)` 不再只暴露“已选模板 + 当前激活材料”
  - 现在会直接把：
    - 整个场景模板库
    - 整个参考材料库
    暴露给沟通页
  - 每个 loadout item 也新增了足够的会话装配信息：
    - `default_selected`
    - `document_content`
    - `reference_lines`
    - `hotwords`
    - `risky_terms`
    - `support_strategies`
  - frontend [ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 现在会：
    - 默认勾选 memory 页里已经选中的模板和当前激活材料
    - 允许直接在沟通页勾选任意模板/材料
    - 真正按所选材料/模板拼 `preparation_context_update`，而不是只会吃 active material

- 这轮把记忆页和训练页继续按“产品对象”收干净了一层：
  - [frontend/src/app/memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 已移除前台可见的 `CommunicationPreferenceCard`
  - 当前用户画像前台只保留：
    - 用户自己维护的画像文档
    - 系统观察到的稳定信号
  - 训练页 [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现在已改成：
    - 主题优先，而不是先挑句子
    - `自定义训练` 直接同步记忆区 `prepared_expression`
    - 选主题后自动滚到录音区
    - 每录完一条默认自动推进到当前主题的下一句
    - 句子列表降级成“需要时再手动换句”的辅助区
  - 这让当前产品对象更一致：
    - `自定义训练` = 一份参考文档 / 一个训练主题
    - `句子` = 该主题下自动推进的练习序列

- 用户画像的 durable 自动更新边界这轮也重新确认了：
  - 自动更新不是每次训练后都写
  - 当前 durable 更新只在 `沟通 session close` 时触发：
    - frontend [memory-service.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/memory/memory-service.ts)
    - backend [memory.controller.ts](/home/ubuntu/VoxFlame-Agent/backend/src/controllers/memory.controller.ts)
    - backend [memory-maintenance.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/memory-maintenance.service.ts)
  - 当前自动维护仍只做轻量稳定信号补充：
    - `summary`
    - `common_scenarios`
    - `risky_terms`
    - `support_strategies`
  - 也就是说：
    - `画像文档` 仍是用户 owner
    - 自动维护链只补稳定规律，不替代文档本体

- 训练页录音不可用的问题根因已经定位并修掉：
  - backend [rtc-orchestration.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/rtc-orchestration.service.ts) 与 frontend [livekit-transport.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/livekit-transport.ts) 之前都会在 loopback 场景下错误重写 LiveKit 地址
  - 结果是浏览器 origin 会把真实 LiveKit 端口覆盖掉，导致前端去连错地址
  - 现在已经改成：
    - 只在目标 host 是 `localhost / 127.0.0.1 / ::1 / livekit-server` 时替换主机名
    - 保留 LiveKit 真实端口，例如 `:7880`
  - 对应测试也已补上：
    - backend [livekit-config.service.test.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/livekit-config.service.test.ts)
    - frontend [livekit-transport.test.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/livekit-transport.test.ts)

- 这轮把记忆页里还不对产品对象的 3 处结构继续收口了：
  - [frontend/src/app/memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 现在已把：
    - `用户画像` 改成“单文档 + 首屏固定句辅助”的形态，不再把画像本体做成“三句话卡片”
    - `自定义材料` 改成“当前参考文档”标题卡，默认不再直接摊开全文编辑区
    - `场景 / 热词模板` 改成按标题切换查看，默认只看一套，不再展开整列模板详情
  - `用户画像` 也新增了真正的文档保存通路：
    - backend 已新增 `PUT /api/memory/workspace/:userId/profile-memory`
    - [backend/src/services/supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 的 `user_profile_memory` 现在已支持 `document`
    - workspace snapshot 也已把 `user_profile_memory` 直接暴露给前端
  - 当前口径也更清楚：
    - `用户画像文档` 是用户自己维护的单篇长期文档
    - `固定开场 / 配合方式 / 补救句` 只是从画像里提炼出来的首屏快捷表达
    - backend 维护链后续只应在这份画像文档基础上做小幅增量，不应再把画像 UI 退化回“三句话”
  - 已验证：
    - `cd backend && npm run build`
    - `cd frontend && npm run build`

- 这轮把沟通页里还没删干净的旧壳真正收掉了：
  - [frontend/src/components/chat/CommunicationStarterKit.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/CommunicationStarterKit.tsx) 已删除：
    - `更像你自己的第一句话`
    - `通用补救句`
  - 当前 starter kit 只保留：
    - 场景选择
    - 该场景下的开口句 / 关键内容句 / 补救句 / 对方配合句 / 切换句
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 也已删除前端用户可见的：
    - `沟通复盘`
    - `recommendedFocus / sessionReview` 对应卡片
  - 当前沟通页重新收成：
    - 先选场景和第一句话
    - 再选自定义材料 / 场景模板
    - 最后只展示“当前会送进助手的上下文”

- 这轮也把“沟通材料为什么会和训练绑在一起”继续收口了一层：
  - frontend `buildPreparationContextUpdate(...)` 已不再把 `training_pairs` 写进沟通页的实时 `preparation_context_update`
  - backend [rtc-orchestration.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/rtc-orchestration.service.ts) 也已不再把 `training_pairs` 放进沟通页初始 preparation context
  - 当前沟通链默认只围绕 4 块长期记忆装配：
    - 用户画像
    - 训练总结
    - 场景 / 热词模板
    - 用户手动选择的自定义材料
  - `training_pairs` 结构仍保留在数据 contract 里，作为内部可扩展位，但这轮不再默认进入沟通 agent 上下文

- 这轮也把训练页里会误导的产品表达压回来了：
  - [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 已改成：
    - `准备句数 -> 可练句数`
    - `准备内容与拆句训练 -> 准备内容与句子练习`
    - `按标点和拆句结果训练 -> 右侧会直接列出可练句子`
  - 当前产品表达重新明确：
    - 准备内容仍是完整材料
    - 句子练习只是从材料里提取句子练，不代表材料被“压缩成训练材料”

- 已验证：
  - `cd backend && npm test`
  - `cd frontend && npm run test:runtime`
  - `cd frontend && npm run build`
  - `cd backend && npm run build`

### 2026-04-17

- 这轮把之前明确标红的两条 gap 真正收掉了：
  - `训练总结定期更新` 不再主要靠训练页打开时 stale refresh：
    - backend 已新增 [training-report-maintenance.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/training-report-maintenance.service.ts)
    - server 启动后会定时扫描 stale 的 `prepared_expression training_reports`
    - [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 已删掉页面打开时的 `periodic_auto` 自动刷新副作用
  - `场景 / 热词模板` 也不再是“用户自由编辑 hotword profile”的旧壳：
    - backend 已新增模板库读写：
      - `GET /api/memory/workspace/:userId/scene-templates`
      - `PUT /api/memory/workspace/:userId/scene-templates`
    - [backend/src/services/scene-template.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/scene-template.service.ts) 已重写成开发者维护模板库
    - [frontend/src/app/memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 已改成模板选择 UI，不再让用户自己增删“自定义重点词”
    - `workspace snapshot / object_zones / communication_loadout / preparation` 现在都会直接消费已选模板
  - 这轮也按专业来源把沟通页 starter kit 做厚了一层：
    - [frontend/src/lib/communication/starter-kit.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/communication/starter-kit.ts) 现在已把每个场景扩成：
      - 开口句
      - 关键内容句
      - 补救句
      - 对方配合句
      - 说不顺时的切换句
    - [frontend/src/components/chat/CommunicationStarterKit.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/CommunicationStarterKit.tsx) 也已同步改成分组展示，不再只是一排短句
  - 模板与表达包主要参考：
    - ASHA dysarthria adults / AAC / communication tips
    - Patient Provider Communication 的 planning tools / communication tools
    - Tobii Dynavox emergency communication resources
  - 已验证：
    - `cd frontend && npm run build`
    - `cd backend && npm run build`

- 这轮把“4 类记忆到底谁维护”继续钉死了一层：
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 现在已明确：
    - `自定义材料` 只允许用户手动增删改
    - `训练总结` 由训练总结模型定期覆盖更新
    - `用户画像` 由后台维护链小幅更新
    - `场景 / 热词模板` 目标态是“开发者维护模板库，用户只负责选择”
  - [VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md) 也已同步去掉：
    - `compact` 长期对象
    - 模板可被后台自动改写
    这些会误导后续实现的旧口径
  - 当前 4 类记忆 owner 的代码口径现在已和 PRD 对齐：
    - `用户画像后台维护` 已经在 backend 里
    - `训练总结定期更新` 已切到 backend maintenance
    - `场景 / 热词模板` 已切到“模板库 + 选择加载”

- 这轮也继续把记忆页里的产品表达往单一事实源收了一层：
  - [frontend/src/app/memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 已去掉顶部重复总览区，不再“上半看四块、下半再编辑一遍”
  - [frontend/src/components/chat/CommunicationPreferenceCard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/CommunicationPreferenceCard.tsx) 已支持按页面语义改标题
  - 记忆页里原来的“我的沟通偏好”现在已并回 `用户画像` 语义，不再像独立第五块记忆
  - 记忆页四块内容现在也已改成“标题摘要 + 选中后展开”的收纳式结构：
    - 用户画像
    - 自定义材料
    - 训练总结
    - 场景模板
    默认不再把全文编辑器和整套模板明细全部摊在页面上
  - 场景模板列表也继续压成收纳式卡片，默认不再把整套模板内容全部展开
  - 这轮也再次确认了当前产品边界：
    - `用户画像` 仍由 backend memory maintenance 模型在沟通 session 结束后做小幅更新
    - `自定义材料` 当前仍是“单篇当前文档”，还没升级成多文档列表

- 这轮也把沟通页的“推荐短句”真正并回了“第一句话”入口：
  - 这一步已在 2026-04-19 继续收口，当前不再保留“推荐短句”这个单独产品概念
  - starter kit 只保留场景内的开口句和沟通表达包
  - 原来的大场景卡也已压成轻量标题按钮，不再用整块大卡占页面
  - 这样沟通页真正收成：
    - 场景入口
    - 第一话入口
    - 本次资料
    而不是多个相似模块并排竞争注意力

- 这轮也把沟通页的上下文装配 UI 和 backend owner 继续对齐了一层：
  - backend [supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 现在已收成：
    - `用户画像` 默认带上
    - `训练总结` 默认带上
    - `自定义材料` 用户手动选择
    - `场景模板` 用户手动选择
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 已删掉：
    - 沟通页里那套 mode 微调 / loadout 大解释
    - 对用户暴露“为什么这样装配”的大段说明
  - 当前沟通页只保留：
    - 自定义材料选择
    - 场景模板选择
    - 当前会送进 agent 的上下文结果摘要
  - 这样用户的点击动作和真正进入 agent 的上下文终于在页面上对齐了

- 这轮也把 `livekit-agent` 的 Docker 重建卡慢问题继续收口了：
  - 当前已明确：红色 `importing cache manifest` 不是主因，真正拖慢的是一旦没复用到依赖层就会重新在线 `pip install livekit-agents`
  - 仓库已新增 [livekit_agent/Dockerfile.localvenv](/home/ubuntu/VoxFlame-Agent/livekit_agent/Dockerfile.localvenv)
  - [scripts/docker-rebuild-core-fast.sh](/home/ubuntu/VoxFlame-Agent/scripts/docker-rebuild-core-fast.sh) 现在会优先检测本地 `livekit_agent/.venv`
  - 如果 `.venv` 里已具备：
    - `livekit-agents`
    - `websockets`
    - `python-dotenv`
    就直接切到 `Dockerfile.localvenv`，复用本机现成依赖层打镜像
  - `docker-compose.yml` 也已支持 `LIVEKIT_AGENT_DOCKERFILE`，同时去掉了会误导判断的 `cache_from`
  - 另外保留了 pip 镜像与官方 extra index 回退，作为 `.venv` 不可用时的兜底

- 这轮也把训练页顶部重新排了优先级：
  - [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 已把：
    - `每日目标`
    - `今日计划`
    - `今日总结`
    - `最近 7 天总结`
    提前到页面最前面
  - `设备状态 / SessionReadinessPanel` 已从训练页移除
  - `准备内容` 和 `拆句训练` 也已合成同一个训练工作区：
    - 左侧直接编辑要说的内容
    - 右侧立刻选择拆出的句子
    - 不再让用户在两个独立大块之间来回跳
  - 顶部说明文案也已明显压短，避免又回到长解释口吻
  - 已验证：
    - `cd frontend && npm run build`
    - `bash scripts/check_ai_docs.sh`

- 这轮也把沟通页里的“模式”再往后藏了一层：
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 当前已不再把：
    - `紧急沟通 / 长时间沟通`
    当成用户首先看到的产品概念
  - 页面现在更明确地围绕：
    - 当前场景
    - 第一条开口句
    - 本次会用到的资料
    来组织
  - `mode` 仍然保留给 backend / runtime 做装配，但前端已降成“需要时再手动微调装配策略”
  - 这更接近用户刚刚明确的产品目标：
    - 陌生场景下要 10 秒内能开口
    - 而不是先学会系统有哪两种状态
  - 已验证：
    - `cd frontend && npm run build`

- 这轮又把沟通页 / 记忆页里直接暴露给用户的工程语义收了一层：
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 已去掉用户可见层里的：
    - `默认常驻`
    - `workspace`
    这些工程词
  - 当前沟通页不再直接讲内部装配术语，而是只告诉用户：
    - 这类资料有什么用
    - 会不会自动带上
    - 去哪里编辑
  - [frontend/src/app/memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 也已不再用：
    - `durable workspace`
    - `workspace object zones`
    这类词
  - 记忆页对象卡当前已补成真正可操作入口：
    - 用户画像可直接打开编辑
    - 自定义材料可打开、编辑、删除
    - 自定义重点词可直接编辑和删除
    - 训练总结可直接查看
  - backend 也已补上：
    - `DELETE /api/memory/workspace/:userId/prepared-expression`
  - 已验证：
    - `cd backend && npm run build`
    - `cd frontend && npm run build`

- 这轮把沟通页上下文装配继续收到了“4 类 durable memory + session memory”的固定边界：
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 现在不再把：
    - `训练总结`
    - `场景 / 热词模板`
    弱化成只剩前端卡片标题
  - 当前固定口径是：
    - `用户画像 / 场景热词模板 / 训练总结` 默认直接以结构化字段进入 agent 上下文
    - `自定义材料` 仍按用户是否手动加载决定是否带全文
  - 当前 runtime 真正送进 agent 的关键字段已明确包含：
    - `hotwords`
    - `risky_terms`
    - `support_strategies`
    - `document_content`
  - backend 初始 session preparation contract 也已同步扩成：
    - `backend/src/services/rtc-orchestration.service.ts`
    - `backend/src/services/livekit-session.service.ts`
  - `livekit_agent` 的 typed `PreparationContextPack` 与 prompt 也已同步扩成可消费：
    - `hotwords`
    - `risky_terms`
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_session_userdata -v`
    - `python3 -m unittest livekit_agent.tests.test_data_contract -v`
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`
    - `cd backend && npm run build`
    - `cd frontend && npm run build`

- 这轮也把 `livekit-agent` 的快重建路径继续制度化了一层：
  - [docker-compose.yml](/home/ubuntu/VoxFlame-Agent/docker-compose.yml) 现在已显式给 `livekit-agent` 加上：
    - `image: voxflame-agent-livekit-agent:latest`
    - `cache_from: voxflame-agent-livekit-agent:latest`
  - 仓库已新增宿主机可直接执行的快重建脚本：
    - [docker-rebuild-core-fast.sh](/home/ubuntu/VoxFlame-Agent/scripts/docker-rebuild-core-fast.sh)
  - 这条脚本会优先：
    - 复用本地已有 `voxflame-agent-livekit-agent:latest`
    - 自动切到 `LIVEKIT_AGENT_BOOTSTRAP_DEPS=0`
    - 并以上一版 agent 镜像作为新的 base image，只覆盖最新代码
  - 只有本地完全没有 agent 镜像时，才会回退到全量依赖安装
  - 这意味着“复用上一个本地稳定镜像，只覆盖新代码”不再只是口头约定
  - 但当前这个沙箱会话里：
    - `sudo` 被 `no new privileges` 禁掉
    - 普通 `docker compose` 也没有 `/var/run/docker.sock` 权限
  - 所以这轮无法在当前环境里真实完成 Docker 重建；需要回到宿主机继续跑：
    - `bash scripts/docker-rebuild-core-fast.sh`

### 2026-04-14

- 已开始按 PRD 落第一刀代码：`workspace document model + memory page object zones`
  - backend `workspace snapshot` 已新增 `object_zones`
  - 当前先把现有对象正式收成 4 个区：
    - `自定义材料区`
    - `场景 / 热词模板`
    - `用户个人画像`
    - `训练总结`
  - 记忆页顶部已改成对象区视图，底下仍复用现有准备稿 / 热词 / 训练总结编辑能力
  - 当前这是骨架切片，不代表新对象存储已经全部做完；它主要服务后续 `loadout / context assembly / compaction`
  - 已验证：
    - `cd frontend && npm run build`
    - `cd backend && npm run build`
- [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 已从“大而全规划稿”收成精简版现状 PRD：
  - 只保留产品主线、基于代码的现状、上线缺口和执行顺序
  - 明确当前网站已经具备 `首页 -> 沟通工作台 -> 练习工作台 -> 沟通档案` 的主骨架
  - 明确现在离正式上线最近的 3 个 blocker 是：
    - `livekit_agent` 侧 typed session memory / context assembly 还没制度化
    - session-close compaction 仍主要依赖前端 `memoryService.endSession()` 过渡链，不是 server-side 稳态
    - dataset review / annotation 只有后端 contract，还没形成前台操作与运营闭环
- [VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md) 也已压缩成短版：
  - 明确 `backend workspace` 是 durable owner
  - 明确 `livekit_agent` 当前只有最小 session-local state，不应再被文档写成“完整记忆系统”
  - 明确 `dataset != memory`、`flush -> compact -> durable write` 仍是上线前待补 contract

### 2026-04-15

- 这轮把 `communication_loadout` 从“页面展示”推进成了“runtime 真正消费的上下文 contract”：
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 现在不只展示 loadout，还会把：
    - `loadout_mode`
    - `loadout_reason`
    - `loadout_items`
    - `reference_lines`
    连同准备稿和训练句对一起发进 `preparation_context_update`
  - [livekit_agent/session_userdata.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/session_userdata.py) 的 `PreparationContextPack` 已扩成真正可承接本次装配信息的 typed schema
  - [livekit_agent/assistant_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/assistant_runtime.py) 的稳定 prompt 和当前轮 prompt 都会明确消费：
    - 当前是“紧急沟通”还是“长时间沟通”
    - 本次为什么这样装配
    - 本次已经加载了哪些上下文对象
  - 这意味着当前记忆/上下文的运行时核心已经更清楚地落在：
    - backend 负责 durable owner
    - frontend 负责 loadout 可视化与装配入口
    - `assistant_runtime.py` 负责本轮真正如何使用这些上下文
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_session_userdata -v`
    - `python3 -m unittest livekit_agent.tests.test_data_contract -v`
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`
    - `cd frontend && npm run build`
    - `cd backend && npm run build`

### 2026-04-16

- 这轮把旧的 `session-close compaction` 长期写回链正式拆掉了：
  - [memory-service.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/memory/memory-service.ts) 不再生成或回写 `session_compaction`
  - backend `/api/memory/session-close` 不再新增 `session_compaction` memory
  - 当前会话结束后只允许更新 `用户个人画像`
  - 当前更新字段只保留：
    - `summary`
    - `common_scenarios`
    - `risky_terms`
    - `support_strategies`
  - [supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 已新增 `preferences.user_profile_memory` 合并写回
  - `workspace snapshot` 现在会直接读取这层用户画像记忆，进入：
    - `object_zones.user_profile`
    - `communication_loadout.always_on`
    - `preparation`
    - `profile_bundle`
  - 沟通 loadout 第四栏也已改回 `训练总结`
  - `training_summaries` 对象区不再混入“最近一次会话复盘”
  - `memory-growth` 已停止把 `session_compaction` 当成画像来源
  - 已验证：
    - `cd backend && npm run build`
    - `cd frontend && npm run build`
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types src/lib/memory/memory-service.test.ts`

- 这轮把 [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 再压成了真正的短版主文档：
  - 不再保留大段“已完成现状盘点”
  - 只保留：
    - 产品是什么
    - 当前固定边界是什么
    - 接下来只做什么
  - 当前 PRD 的固定锚点已经明确成：
    - `沟通 loadout`
    - `训练总结`
    - `durable workspace`

- 这轮把训练页产品口径继续收窄，避免主线又被“重型 AI 助手”带偏：
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 现在已明确：
    - 核心产品锚点改成 `沟通 loadout + 训练总结/计划 + durable workspace`
    - 训练页当前目标是 `舒服录音 + 明确目标 + 每日总结 / 7 天总结 + 训练计划 + 总结回写`
    - `训练页 TTS 对话助手 / 主动提醒人格 / 重型 coach` 不再作为上线前主线
    - 训练页和沟通页的个性化准备资料不默认共享
    - 训练侧产出的新材料建议默认只是 suggestion，不能自动进入沟通侧 loadout
    - dataset 也已改成最小原则：只保留音频、目标句和最小对句判断，不再继续长 review queue / export 审批流
    - 训练计划和总结都由同一个训练 room / 同一个模型顺手产出，重点放在前端 UI 自动化和简洁完成感
  - 当前产品判断进一步收清：
    - 训练页最重要的是留住用户录下去
    - 不是先做一个会说话的训练 companion
    - 等 `memory / compaction / 最小训练闭环` 稳住后，再决定是否加增强层

- 这轮又把训练总结这条链按新产品口径重写了一次：
  - backend / frontend 已不再沿用：
    - `50 句纠错总结`
    - `rehearsal_summary`
    - `correction_hints`
    这些旧壳
  - 当前训练总结正式收成：
    - `training_reports.daily_summary`
    - `training_reports.weekly_summary`
    - `training_reports.training_plan`
  - 当前模型链也已改窄：
    - 主要只看 `target_text / recognized_text`
    - 只生成今日总结、7 天总结和下一轮计划
    - 不再顺手改写 prepared material / section summary
  - 训练页和记忆页 UI 已同步改成：
    - 今日总结
    - 最近 7 天总结
    - 下一轮计划
  - 旧的“累计 50 句后自动更新纠错总结”文案和逻辑已删
  - 已验证：
    - `cd frontend && npm run build`
    - `cd backend && npm run build`
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types src/lib/training/prepared-expression-practice.test.ts`
    - `bash scripts/check_ai_docs.sh`

- 这轮又把“句子级准备资产 owner”继续收了一层，避免派生命名继续误导后续开发：
  - `workspace snapshot.expression_kit.personalized_phrases` 已改成 `recommended_phrases`
  - 当前边界变成：
    - `prepared_expression` = 用户自定义材料 owner
    - `hotword_profiles` = 场景 / 热词模板 owner
    - `quick_phrases` = 沟通开口短句 owner
    - `expression_kit.recommended_phrases` = 派生推荐视图，不再假装自己是 owner
  - 沟通页文案也同步收窄成“推荐短句”
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 已删掉这块旧待办
  - [VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md) 也同步删掉了旧的 review queue / export 审批口径，改回最小 dataset contract
  - [FOUNDER_COLLABORATION_LOOP_2026-03-25.md](/home/ubuntu/VoxFlame-Agent/docs/FOUNDER_COLLABORATION_LOOP_2026-03-25.md) 已纠正旧的 `TEN + Agora` 说法，补成当前真实架构与学习顺序
  - 已验证：
    - `cd backend && npm run build`
    - `cd frontend && npm run build`
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types src/lib/training/prepared-expression-practice.test.ts`
    - `bash scripts/check_ai_docs.sh`
    - `sudo docker compose build backend frontend`
    - `sudo docker compose up -d --force-recreate backend frontend`
    - Playwright smoke:
      - `/chat` 已确认显示 `本次已加载`、`推荐短句`
      - `/contribute` 已确认显示 `训练总结与计划`
      - `/memory` 已确认显示 `4 个正式对象区`
  - 当前还看到一个轻度残余信号：
    - 浏览器 console 有一次 `Failed to fetch RSC payload for /data-collection`，但实际页面可正常打开，更像部署切换时的 RSC/prefetch 噪音，不是主链路阻断

- 这轮开始真正往 `typed session memory` 落代码，而不只是继续讨论概念：
  - [session_userdata.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/session_userdata.py) 里已新增：
    - `SessionTurnRecord`
    - `SessionWorkingMemory`
  - 当前 `livekit_agent` 的 session-local state 已不再只是几个散字段，而是正式收成：
    - 当前轮状态
    - 当前 user / assistant 文本
    - 最近 turn 记录
    - `turn_count`
    - `context_revision`
    - `last_preparation_source`
    - `interruption_count / barge_in_count`
  - `assistant_runtime` 现在在回复完成后会把 turn 正式写进这层 working memory
  - `session_userdata_ack` 也开始把 `session_memory` 摘要回发，便于后续前端或调试面继续接入
  - [frontend/README.md](/home/ubuntu/VoxFlame-Agent/frontend/README.md) 已补“如何理解前端目录”，把 `app / components / hooks / lib` 解释成真正的分层，而不是零碎文件夹
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_session_userdata -v`
    - `python3 -m unittest livekit_agent.tests.test_data_contract -v`
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`

- 这轮又把训练总结模型配置收窄了一层：
  - [prepared-expression-summary.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/prepared-expression-summary.service.ts) 不再继续回退多层旧模型变量
  - 当前训练总结只优先读取：
    - `DASHSCOPE_TRAINING_REPORT_MODEL`
    - 无值时兜底 `qwen3.5-plus`

- 这轮把“记忆系统后台维护稳态 + 训练页舒服度”继续往上线前主线推进了一刀：
  - backend 已新增 [memory-maintenance.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/memory-maintenance.service.ts)
  - 当前 `/api/memory/session-close` 不再直接把前端猜的一份画像更新原样写库，而是会先用：
    - 现有 `user_profile_memory`
    - 本次 `session metadata / runtime signal`
    - 本次 `profile_update`
    - `DASHSCOPE_MEMORY_MAINTENANCE_MODEL`
    做一次后端维护，再落到 `用户个人画像`
  - DashScope 不可用时，也会回退到启发式维护，不会让会话结束写回失败
  - 训练页代码里已补上：
    - `SessionReadinessPanel`
    - `当前目标`
    - `重录这一句`
    - `这句判断`
  - 已验证：
    - `cd backend && npm run build`
    - `cd frontend && npm run build`
    - `cd backend && npx ts-node src/services/memory-maintenance.service.test.ts`
    - `sudo docker compose build backend frontend`
    - `sudo docker compose up -d --force-recreate backend frontend`
  - 当前剩下的主要不是“代码还没写”，而是：
    - Docker 前端实际吐出的 `/contribute` bundle 仍表现成旧页
    - 需要继续只在完整 Docker 环境里确认最新前端 bundle 真正生效
    - 然后再做训练页舒适度和沟通页准确率的真实测试
    - `DASHSCOPE_TRAINING_REPORT_MODEL`
    - 无值时兜底 `qwen3.5-plus`
  - 旧 prompt 里残留的 `correction hints` 口径也已删掉
  - [docker-compose.yml](/home/ubuntu/VoxFlame-Agent/docker-compose.yml) 和 [.env.example](/home/ubuntu/VoxFlame-Agent/.env.example) 已同步删除旧的 `DASHSCOPE_PREPARED_EXPRESSION_SUMMARY_MODEL` 配置位
  - 已验证：
    - `cd backend && npm run build`
    - `bash scripts/check_ai_docs.sh`

- 这轮把 `typed session memory -> compact candidate` 又往前接了一步：
  - [session_userdata.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/session_userdata.py) 已新增 `SessionCompactionCandidate`
  - `session_userdata_ack` 现在除了 `session_memory` 外，也会回一份很窄的 `compaction_candidate`
  - 当前这份 candidate 只包含：
    - 最近确认过的更稳表达
    - 最近风险原句
    - 支撑策略
    - 最近用户意图
    - 最近确认表达
    - `loadout_mode / turn_count / interruption_count`
  - [session-runtime.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-runtime.ts) 已开始把这层 server-side candidate 写进当前 session metadata，作为后续真正 server-side `session-close compact` 的过渡入口
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_session_userdata -v`
    - `python3 -m unittest livekit_agent.tests.test_data_contract -v`
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types src/lib/realtime-audio/session-runtime.test.ts`
    - `cd frontend && npm run build`
    - `cd backend && npm run build`

- 这轮也把 `livekit-agent` 的 Docker 重建真正打通了，但走的是“无降级快重建”路径，而不是慢速全量重装依赖：
  - [livekit_agent/Dockerfile](/home/ubuntu/VoxFlame-Agent/livekit_agent/Dockerfile) 现在已显式固定 `LIVEKIT_AGENT_PIP_INDEX_URL`
  - 由于外网 Python 依赖拉取极慢，直接全量 build 会长期卡住
  - 当前采用的生产安全做法是：
    - 先把正在稳定运行的旧 agent 镜像打成本地 base tag：`voxflame-livekit-agent-base:bootstrap-20260416`
    - 再用 `LIVEKIT_AGENT_BASE_IMAGE=voxflame-livekit-agent-base:bootstrap-20260416`
      和 `LIVEKIT_AGENT_BOOTSTRAP_DEPS=0`
      只覆盖最新代码重建新镜像
  - 当前新镜像已经成功产出并启用：
    - `voxflame-agent-livekit-agent:latest`
    - image id: `86834b88b9e5`
  - 这条路径的价值是：
    - 包含最新代码
    - 不重新拉慢依赖
    - 不降级当前稳定依赖层
    - 后续仍保留全量 clean rebuild 的扩展位

- 这轮把上线前最重要的 3 条 contract 正式收清了：
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 现在不再保留大而全待办，只保留：
    - agent 上下文包含什么
    - 三个模型如何分层
    - 哪些允许进入 durable memory
  - 当前明确：
    - 沟通页用户选中的 loadout 材料会自动进入 agent 当前轮上下文
    - durable memory 不能直接整库塞进 prompt，只能先经过 `workspace snapshot -> communication loadout -> selected preparation context`
    - correction / memory maintenance / training summary 已正式定义成 3 个模型层，不再混写成一个“LLM”
  - 代码同步完成：
    - [config.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/config.py) 已新增 `DASHSCOPE_CORRECTION_MODEL`
    - [assistant_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/assistant_runtime.py) 的实时 correction 已改由独立 correction model owner 驱动
    - [memory-service.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/memory/memory-service.ts) 的过渡实现现在会优先使用 agent 回发的 `serverCompaction*` 字段，减少前端对后台记忆更新信号的二次猜测
  - 当前仍待继续实现的下一刀已经固定：
    - 后端真正的 `DASHSCOPE_MEMORY_MAINTENANCE_MODEL -> 四块记忆后台更新`

- 这轮产品判断又进一步收紧了一层：
  - 不再把“会话后 compact”当成一个新的长期记忆体系
  - 长期记忆只允许继续维护现有 4 块：
    - 自定义材料区
    - 场景 / 热词模板
    - 用户个人画像
    - 训练总结
  - 当前代码里的 `compaction_candidate / session-close compaction` 只能算过渡实现或内部信号，不再代表产品上要新增一个第五类 memory object


## 下一步

1. 先把 `session-local typed memory / context assembly / session-close compaction` 补成稳定的 owner 链。
2. 训练页和记忆页继续围绕 `prepared-expression / important-expression / 高频句` 做统一句子录入入口，而不是继续长新 surface。
3. 数据录入和标注优先补齐 review queue、质量门槛和硬指标调查，不再只按样本数量推进。
   - 句子录入后默认自动跑“一句一音对应目标句”的校验，不先做独立 annotation UI
   - 只有自动校验命中风险的样本才进入复核
4. 记忆页继续收成“用户画像 + 当前准备 owner + 会后压缩结果”。
5. 再回到 10 分钟级别和公网条件下的真实沟通 smoke，验证这些沉淀是否真实影响现场纠错结果。
6. 本地验证入口继续收口到 `http://localhost:3000`：
   - 前端本地开发与 Docker 入口重新统一为单一 `3000` origin
   - 不再维护额外本地 HTTPS 入口，避免双 origin 带来的登录与跳转漂移
7. 本地登录和 RTC 鉴权稳定性已继续补强：
   - [frontend/src/lib/realtime-audio/session-bootstrap.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-bootstrap.ts) 不再优先发送可能过期的 `accessToken`，而是优先向 Supabase 取最新 token
   - [frontend/src/app/(auth)/login/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/(auth)/login/page.tsx) 登录成功后会先确认 fresh session，再做整页跳转，减少 client-only replace 和 server middleware 脱节

## 当前验证基线

- `cd frontend && npm run build`
- `cd backend && npm run build`
- `cd livekit_agent && python3 -m unittest discover tests -v`
- `python3 -m py_compile livekit_agent/app.py livekit_agent/asr_runtime.py livekit_agent/data_contract.py`
- `bash scripts/check_ai_docs.sh`
- `cd frontend && npm test`
- Docker 真实验证优先看：
  - `sudo docker compose logs -f livekit-agent`
  - 关键字：`LiveKit ASR first audio frame`、`LiveKit end_audio received`、`LiveKit ASR final transcript`

### 2026-04-07

- 沟通页展示又收口了一层：前端已移除 `表达对照`，用户界面只保留最终可直接说出去的结果。
- `livekit_agent` fallback 文案已收掉“现在先按当前沟通场景继续 / 我先帮你把这句话往前推进”这类铺垫，超时回退时也只回真正要说的话。
- 当前慢的主因已经查清：不是额度或磁盘问题，而是 `ASR final -> DashScope correction/reply -> TTS` 这段串行链路里，纠错/改写调用经常超时回退。
