# VoxFlame Agent - 项目概述

> 这里只保留当前产品定义、现役事实源、最近 3 天仍有效的收口和下一步必须知道的状态。

## 产品定位

- 核心目标不是“纠正用户的声音”，而是“纠正系统对用户意图的理解”。
- 当前主产品链路是：`沟通工作台 -> 练习工作台 -> 沟通档案(memory/workspace)`。
- 当前迁移主线已经进入清退阶段：沟通与训练执行面都收口到 `self-hosted LiveKit + livekit_agent`。
- `ten_agent/` 目录已经从仓库物理删除，旧执行面不再是代码基座的一部分。
- `frontend / backend / livekit_agent` 代码层已经没有 `Agora/agora_ten` 运行时引用，残留的只是不影响运行的历史迁移文档。
- `docker-compose` 默认启动路径也已更符合最佳实践：
  - 默认核心栈是 `livekit-server / backend / frontend / livekit-agent`
  - `qdrant / redis` 改为 `extras` profile，不再拖主链启动
  - LiveKit server 默认镜像已钉到 `docker.m.daocloud.io/livekit/livekit-server:v1.10.1`
  - 当前容器实测已处于 `Up` 状态

## 现役事实源

- 现役执行面已经收口为 `frontend -> backend -> self-hosted livekit-server -> docker livekit-agent`。
- 模型/provider 现役约束仍是 `DashScope / Qwen-first`。
- `dataset != memory`，记忆继续由 `workspace / profile bundle / session review / expression kit` 语义承接。

## 最近 3 天有效状态

### 2026-04-05

- 当前最重要的产品目标已经从“继续泛化替代 TEN”切到“5 天内验证 VoxFlame 是否真的能帮助构音障碍用户完成高压、长时、结构化表达”。
- 已新增专项文档：
  - [VOXFLAME_SPEECH_MODE_EXECUTION_PLAN_2026-04-05.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_SPEECH_MODE_EXECUTION_PLAN_2026-04-05.md)
- 外部参考分析已经并入这份专项计划，当前最值得借的长处已经写实：
  - `OpenClaw`: memory source-of-truth 与 context assembly 分离、自动 memory flush、hybrid recall
  - `VibeVoice`: 长时 transcript / hotword / 结构化长音频目标意识
  - `Lime`: 中文优先、信息优先、工作台按页面职责定宽度和层级
  - `CLEAR-VOX-MODEL`: 个体差异优先、LLM N-best rerank、规律/热词比流水更重要
- 一个关键产品边界也已经收清：
  - 所有页面都应该继续变得更直白、更简单
  - `记忆页` 做准备内容的 owner
  - `沟通页` 只显示当前场景的最小必要准备
  - 不把赌注压在“现场 agent 即兴发挥”上，而是先准备好规律、热词、保底句，再让 agent 稳定发挥
- 这份计划把当前主线重新收成：
  - `structured rehearsal mode`
  - `long-form live assist mode`
  - `review mode`
- 其中一个关键纠偏已经明确：
  - 记忆页不是训练复盘页
  - 它应该是 `用户全面画像 + 常见场景 + 即将面对场景的准备页`
  - training result 只是输入，不是页面本体
  - 这页未来应服务：
    - 用户自己上台前准备
    - agent runtime
    - LLM correction 的结构化上下文
- 关键判断已写实：
  - 这 5 天不能把成败押在“数据再录一点、微调马上显著变强”上
  - 数据录入和标注准确仍然重要，但定位是长期增益，不是短期关键路径
  - 短期更该依赖：
    - LiveKit self-hosted realtime loop
    - DashScope / Qwen-first 的现役 ASR / rewrite / TTS
    - 现有 `workspace snapshot / profile_bundle / session_review / expression_kit`
  - 记忆系统不应重做，而应继续沿现有 durable owner 做 `prepared-expression / pattern-extraction` 派生体验
  - 记忆最重要的是记住规律/特点，最高效地表达给：
    - 用户自己
    - agent runtime
    - LLM correction
- 一个新的执行判断已经固定：
  - 在今天这种 vibe coding 时代，`5 天做出一套真正够用的记忆系统和产品收口` 并不算离谱
  - 真正的难点不是编码速度，而是我们能不能把：
    - 模型链路如何分工
    - agent 在现场到底做什么
    - 记忆该记什么、怎么压缩、怎么被现场调用
    这几件事判断准
- 当前代码现状已经足以承接这条新主线：
  - 训练结果已能沿 `training_result -> training_profile_summary -> workspace snapshot` 链沉淀
  - 记忆页本身已经具备“上台前准备页”的雏形
  - 这轮又继续收口了一层：
    - backend `workspaceSnapshot.preparation` 新增 `profile_summary / listener_guidance`
    - memory 页顶部现在先展示“我的表达画像”，再展示“当前重要表达准备”和“最近复盘”
    - 页面本体更接近“用户画像 + 当前准备 owner”，而不是训练复盘页
  - 沟通页已有 LiveKit communication / TTS / interrupt / turn-taking 基线
  - 旧执行面的辅助工具链也在继续收尾：
    - `frontend/Dockerfile` 已删除 `NEXT_PUBLIC_ENABLE_LIVEKIT_TRANSPORT`
    - `frontend/README.md` 已切到 `livekit_agent/README.md`
    - `Qwen ASR/TTS live smoke` 现在默认直打 `voxflame-livekit-agent`
    - 两条 smoke 已在现役 `livekit-agent` 容器上通过
- 当前最值得做的第一刀也已明确：
  - 把 [speech.md](/home/ubuntu/VoxFlame-Agent/speech.md) 作为第一份验证样本结构化成 `prepared expression` 资产
  - 为训练页加 `pattern extraction + structured output`
  - 记忆页顶部新增“当前重要表达准备”模块，并突出热词/规律而不是复盘流水

### 2026-04-03

- `localhost:3000` 已成为 LiveKit communication 的单口入口：
  - [frontend/proxy-server.cjs](/home/ubuntu/VoxFlame-Agent/frontend/proxy-server.cjs) 统一承接页面、API 和 `/rtc/*` signaling websocket
  - 浏览器不再需要额外手动转发 `7880`
  - 真实请求已确认 `executionBackend: "livekit"`，不是 TEN 回退
- `self-hosted LiveKit RTC` 已经真实可用：
  - backend 会签发 LiveKit token/dispatch metadata
  - `livekit-agent` 已在 Docker 中注册并接 job
  - communication 文字改写与 TTS 音频回放已跑通
- 真实 voice smoke 的根因已查清一刀并修复：
  - 用户麦克风轨此前以 `source: UNKNOWN` 发布
  - worker 订阅的是 `SOURCE_MICROPHONE`
  - 已在 [frontend/src/lib/realtime-audio/session-audio.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-audio.ts) 改为显式 `publishTrack(..., { source: Track.Source.Microphone })`
- `livekit_agent` 现在已具备最小 `ASR -> correction-style transcript metadata -> assistant reply -> TTS` 骨架：
  - [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 已接入 `DashScope realtime ASR`
  - [livekit_agent/app.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/app.py) 现在会在语音路径上回发带 `metadata.type = "correction"` 和 `metadata.original` 的 assistant transcript
  - [livekit_agent/data_contract.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/data_contract.py) 已支持这套 correction metadata
- `livekit_agent` 已接入第一版 server-side `VAD / 自动收句`：
  - 当前在 [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 内使用轻量 RMS 检测
  - 持续静音后会自动 `commit`
  - 仍保留手动 `end_audio` 作为双保险
- `livekit_agent` 已开始发出最小 `voice_profile_updated` 信号：
  - correction reply 后会估算 `clarity_score`
  - [frontend/src/lib/realtime-audio/session-runtime.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-runtime.ts) 会把它并入当前 session metadata
  - 这一步先给后续 `session_review / memory parity` 铺真实数据面
- `livekit_agent` 已开始具备最小 turn-taking：
  - [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 会回调 `speech_started / speech_stopped`
  - [livekit_agent/tts_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/tts_runtime.py) 现支持 interrupt 当前 TTS
  - [frontend/src/lib/realtime-audio/session-messages.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-messages.ts) 会消费 `speech_activity`，更快反映用户打断
- LiveKit 沟通会话现在会正式走向 `session review / memory`：
  - [frontend/src/lib/realtime-audio/session-runtime.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-runtime.ts) 在连接时补齐 `surface / scene / executionBackend / transportProvider` 等 session metadata
  - [frontend/src/hooks/useRtcAgentSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useRtcAgentSession.ts) 在断开时会 `memoryService.endSession()`
  - 这意味着 LiveKit communication 不再只是当场对话，而会开始沉淀进现有 workspace owner
- 沟通页执行面现在也已显式切到 LiveKit：
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 已指定 `executionBackend: 'livekit'`
  - [frontend/src/lib/config.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/config.ts) 默认执行面已从 `agora_ten` 收口到 `livekit`
  - [frontend/src/lib/realtime-audio/session-execution.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-execution.ts) 的 LiveKit transport gate 已删除，不再要求 `NEXT_PUBLIC_ENABLE_LIVEKIT_TRANSPORT`
  - [backend/src/services/rtc-orchestration.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/rtc-orchestration.service.ts) 现已完全收口到 `livekit`，不再保留 `agora_ten` 分支
  - [frontend/src/lib/realtime-audio/agora-transport.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/agora-transport.ts) 已删除，前端 `Agora` npm 依赖也已移除
  - [docker-compose.yml](/home/ubuntu/VoxFlame-Agent/docker-compose.yml) 已移除 `ten-agent` service；旧 orphan 容器也已通过 `docker compose down --remove-orphans` 清掉
- LiveKit 训练反馈最小 contract 已接上：
  - [livekit_agent/app.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/app.py) 现在会消费 `training_feedback_request`
  - [livekit_agent/assistant_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/assistant_runtime.py) 会生成训练页当前可直接显示的 `summary / articulation_tip / next_step / clarity_score`
  - [livekit_agent/data_contract.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/data_contract.py) 也会同步补发训练侧 `voice_profile_updated`
  - [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 的过时标题 `TEN 训练反馈` 已改成 `训练反馈`
- LiveKit 训练反馈开始进入现有 `session review / memory` 过渡链：
  - [frontend/src/lib/realtime-audio/session-runtime.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-runtime.ts) 现在会把 `training_feedback` 的 `source / status / summary / next_step / exercise_id / clarity_score` 写进当前 session metadata
  - 同时也会写成现有 memory service 可识别的 `training_result`
  - 写入时也会显式标成 `kind=training / source=livekit_training_feedback`
  - 这让训练页的 LiveKit 反馈不再只是当场显示，而开始沿着现有 workspace owner / growth profile / session review 路径沉淀
  - 训练结果现在还会继续带上 `focus_syllables / articulation_tips / pronunciation_targets` 写进当前 session metadata，方便后续 `session_review_build`
  - [frontend/src/hooks/useMandarinTrainingSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useMandarinTrainingSession.ts) 也已显式指定 `executionBackend: 'livekit'`，训练页不再隐式依赖全局 `TEN/Agora` 默认执行面
- backend 的 `workspace snapshot` 现在开始更像真正的训练复盘：
  - [backend/src/services/supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 会把 `articulationTips / frequentConfusions / recentTraining summary` 写进 `profile_bundle`
  - `session_review` 在没有稳定沟通会话时，也会优先用最新训练摘要生成 headline/summary
  - 如果最近一次会话本身就是训练，也会直接用训练摘要覆盖更泛的“最近一次会话”描述
- 语义上已经不该再把 `livekit_agent` 描述成“只会 text stub”：
  - communication text rewrite 已真实使用 DashScope chat/completions
  - TTS 已在 LiveKit 房间发布音轨
  - ASR 已接线，但还需要继续拿真实麦克风 smoke 把 final transcript 完全打透

### 2026-04-02

- LiveKit migration 的判断已经重新写实：
  - 节点 1：部署好 LiveKit 服务，已完成第一版开发基座
  - 节点 2：补齐现役 TEN 执行链功能并跑通全链路，进行中
  - 节点 3：完全删除 TEN + Agora，尚未开始
- `livekit_agent` 不再走 OpenAI realtime stub，而是 `DashScope/Qwen-first` communication loop。
- worker 注册失败的旧 blocker 已解决，当前 blocker 已切到 `ASR / correction / training feedback / memory tooling parity`。

## 下一步

1. 先按 [VOXFLAME_SPEECH_MODE_EXECUTION_PLAN_2026-04-05.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_SPEECH_MODE_EXECUTION_PLAN_2026-04-05.md) 开始 `prepared expression asset ingestion`。
2. 训练页优先新增 `pattern extraction + structured output`，而不是继续泛化句库。
3. 记忆页优先变成“用户画像 + 场景准备页 + 台后复盘页”，不是单独训练复盘页。
4. 数据录入和标注继续优化，但不作为这次现场演讲成功的唯一保障。
5. 等长时表达专项体验跑稳后，再继续做更深的训练/记忆 parity 和最终 `ten_agent/` 目录清退。

## 当前验证基线

- `cd frontend && npm run build`
- `cd backend && npm run build`
- `cd livekit_agent && python3 -m unittest discover tests -v`
- `python3 -m py_compile livekit_agent/app.py livekit_agent/asr_runtime.py livekit_agent/data_contract.py`
- `bash scripts/check_ai_docs.sh`
- `cd frontend && npm test`
- Docker 真实验证优先看：
  - `sudo docker compose logs -f livekit-agent`
  - 关键字：`LiveKit ASR first audio frame`、`LiveKit end_audio received`、`LiveKit ASR final transcript`
