# VoxFlame Agent - 项目概述

> **燃言 - Ignite Your Voice**
>
> 专为构音障碍者打造的 Agentic Voice 应用

> 维护约定：
> 1. 这里只保留当前产品定义、现役事实源、最近 3 天的重要收口和下一轮协作必须知道的状态。
> 2. 旧 smoke、命令行过程、临时排障痕迹不长期保留；需要时回到 `docs/`、脚本、git 历史或执行计划。
> 3. 超过 3 天的动态记录默认压缩成文档结论，不继续堆在这里。

## 产品定位

- **核心理念**：不是“纠正”用户的声音，而是“纠正系统对用户意图的理解”。
- **产品定义**：`VoxFlame = 主动沟通助手 + 训练反馈页 + 个体记忆系统 +（后续）场景感知联动`
- **核心问题**：用户常常同时面对 `不敢先开口 / 开口后别人听不懂 / 练习后不知道有没有变好` 三个问题。
- **核心流程**：场景选择或直接说话 -> 系统辅助理解/代播 -> 对方更快理解 -> 训练与记忆持续沉淀

## 现役系统事实

- 当前运行时唯一事实源：`Frontend RTC/RTM -> Backend /api/rtc/session/* -> TEN rtc graph`
- 架构讨论统一按五层进行：`Control / Execution / Memory / Capability / Surface`
- `TEN + Agora` 仍是现役执行面，但应被视作过渡实现
- 训练数据与记忆已明确分层：`dataset != memory`
- 根 `AGENTS.md` 只服务仓库协作 agent，不等于产品运行时 prompt

## 任务型入口

- runtime / surface：
  [VOXFLAME_RUNTIME_AND_SURFACE_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_RUNTIME_AND_SURFACE_REFERENCE_2026-03-26.md)
  [control-plane.md](/home/ubuntu/VoxFlame-Agent/docs/control-plane.md)
  [capability-registry.md](/home/ubuntu/VoxFlame-Agent/docs/capability-registry.md)
- agent / memory：
  [VOXFLAME_UNIFIED_MEMORY_REPORT_2026-03-05.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_UNIFIED_MEMORY_REPORT_2026-03-05.md)
  [VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md)
- 训练数据与录音：
  [VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md)
- 协作与入口组织：
  [VOXFLAME_REFACTOR_COLLABORATION_PLAYBOOK_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_REFACTOR_COLLABORATION_PLAYBOOK_2026-03-24.md)
  [docs/aiprompts/README.md](/home/ubuntu/VoxFlame-Agent/docs/aiprompts/README.md)

## 最近 3 天更新

### 2026-03-28

- 训练页这轮已从“旧贡献页”正式收口成 `录音 -> 训练 -> 反馈` 的工作台：[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现已围绕真实的 5 个录音分类 `日常与出行 / 看病与求助 / 人群与角色 / 设备与数字 / 发音与朗读` 组织，不再把旧提交历史和说明式叙事放在主位。
- 登录授权边界已前置到 [login/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/(auth)/login/page.tsx)：当前登录前会默认确认《用户隐私》和《数据采集说明》，并通过 [legal-consent.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/auth/legal-consent.ts) 写入本地与 Supabase 用户元数据；训练页不再重复弹授权勾选，只在缺失授权记录时关闭“保存训练样本”入口。
- 授权说明页已补齐：[privacy/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/privacy/page.tsx) 与 [data-collection/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/data-collection/page.tsx) 现在可作为登录页的真实入口，其中数据采集说明明确把链路收口到 `recording envelope -> recorder queue -> upload receipt -> manifest`。
- 训练页的前端主链这轮已补齐：句子搜索、当前句切换、大录音区、即时本地反馈、训练资产状态、本地待同步队列、训练指导设置都已回到一个页面内；上传仍沿用现有 `recording envelope -> upload/sign -> upload/complete` 链路，并在成功后继续更新训练画像摘要。
- 这轮已完成真实浏览器 smoke：新版登录页可见双授权与真实文档链接；退出后重新登录能清掉训练页“需重新登录才能保存样本”的提示；训练页已确认存在 5 个分类、无重复授权勾选、分类切换与搜索可用、旧历史叙事已移除。`npm run build` 已通过；`npm run lint` 仅剩仓库里既有的 [WaveformVisualizer.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/WaveformVisualizer.tsx) `react-hooks/exhaustive-deps` warning。
- 录音按钮已做过浏览器探针：在当前无麦克风的 headless 环境里，会明确提示“当前设备未检测到可用麦克风”，而不是静默失败；但真实 `录音 -> transcript -> feedback -> 保存训练样本 -> manifest` 端到端仍需在有物理麦克风的浏览器里补最后一刀。

### 2026-03-29

- 已定位并修掉一处真实的 localhost 漂移：仓库文档一直把 `VSCode / SSH forwarded localhost` 白页归因到旧 `service worker / caches`，但现役 [layout.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/layout.tsx) 只有在 `PWA 关闭` 时才挂 [LocalRuntimeReset.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/pwa/LocalRuntimeReset.tsx)，而 docker 默认又是 `VOXFLAME_ENABLE_PWA=1`；结果最容易出问题的 `localhost:3000` 恰好绕开了自愈逻辑。
- 当前 localhost 修复已落地到代码和运行中的 3000 容器：`head` 里新增了前置 runtime reset bootstrap，会在 `localhost / 127.0.0.1` 上优先清掉旧 `service worker / caches` 并按需 reload；[LocalRuntimeReset.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/pwa/LocalRuntimeReset.tsx) 也从一次性 `localStorage` 标记改成 `sessionStorage` 级别，避免“曾经清过一次，后面再也不自愈”的状态卡死。
- [usePWA.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/usePWA.ts) 现在会显式跳过 localhost 上的 SW 注册与安装提示，避免“刚清完旧 SW，又在转发 localhost 上重新注册一份”的循环。已通过 `env VOXFLAME_ENABLE_PWA=1 npm run build`、`npm run lint` 和 `sudo docker compose up -d --build frontend` 验证，运行中的 `localhost:3000` 已吃到这轮修复。
- 训练页这轮继续收口到用户指出的两个真实痛点：[useMandarinTrainingSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useMandarinTrainingSession.ts) 现在在停录时会先发 `speech_stopped + auto_finalize`，再短等一拍后调用 RTC `end_audio`，同时不再拿“第一个冒出来的短 final”当最终答案，而是优先等待更稳定、信息量更高的 transcript，减少“一整句最后只剩一个‘嗯’”的情况。
- [contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 已去掉录音前那层大说明首屏，页面现在直接从 `分类 / 句子 / 当前句 / 录音` 进入；录后反馈卡已并到录音卡正下方，真实 `localhost:3000/contribute` smoke 已确认“反馈预留在录音旁边”就在首屏可见，不再需要往下翻很远。
- 这轮训练页验证已补到当前环境能做的边界：`frontend` 的 `npm run build` 通过，`npm run lint` 仍只剩仓库既有 [WaveformVisualizer.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/WaveformVisualizer.tsx) warning；`sudo docker compose up -d --build frontend` 已完成；Playwright 已确认 `/contribute` 首屏结构符合预期，且在无物理麦克风的 headless 环境里点击录音会明确提示“当前设备未检测到可用麦克风”。真实物理麦克风下“整句录入 -> 更完整 final transcript”仍需在你的浏览器里补最后一刀。
- 训练样本保存主路径这轮已进一步收口：[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现在在“登录授权已确认 + 录音资产完整”时会在停录后自动保存监督样本，不再把“保存训练样本”做成默认主操作；失败时才退回到“重新保存 / 同步本地队列”。
- 监督训练样本的 contract 也已明确拆开：[useVoiceUpload.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useVoiceUpload.ts)、[upload.controller.ts](/home/ubuntu/VoxFlame-Agent/backend/src/controllers/upload.controller.ts) 和 [upload-artifact.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/upload-artifact.service.ts) 现在显式传递 `recognizedText`，并把 manifest 的 canonical label 继续对齐为目标句；`recognized_text/raw_transcript` 只保留给前端反馈和样本诊断，不再和监督标签混写成一层语义。
- 训练资产后端这轮继续补了一个真实可靠性缺口：[upload-artifact.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/upload-artifact.service.ts) 现在会把 `manifest.jsonl` 视为第一事实源，即使 `voice_contributions` 写入临时异常，也优先避免样本 artifact 跟着整条失败；同一条录音在缺 `upload_receipt` 的情况下也会先检查 OSS，尽量不重复追加 manifest / transcripts。
- 已新增历史样本对账脚本 [reconcile_upload_artifacts.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/reconcile_upload_artifacts.ts) 并已实际执行：当前环境里缺 `upload_receipt` 的 6 条真实训练录音现已全部补齐到 `voice_contributions.metadata.upload_receipt + dataset/{user_id}/manifest.jsonl`，其中测试账号现有监督录音已确认写入自己的 `manifest.jsonl`，legacy `v_gv7fxwrp` 的 5 条样本也已全部回填。
- 训练 dataset 的去重粒度这轮也已写成显式 contract：同一句 prompt 允许保留多次练习样本，只有同一条录音的重试/补传才会去重；前端上传 metadata、训练页文案、数据采集说明和 backend manifest `lineage` 现在都统一按这条规则描述。
- dataset 这轮继续按主文推进到了“样本复核”层：[training-sample-review.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/training-sample-review.ts) 现已把上传后的训练样本 decision 收口成 `ready / sampled_for_review / retry_recommended`；即使转录和目标句差很多，只要音频完整，样本仍会进入 dataset，只是后续复核与导出优先级不同。[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 会把 `evaluation_status / review_queue / review_priority / review_reason_tags / review_summary` 带入 upload metadata，并在训练资产状态里说清当前是“自动纳入 / 进入复核 / 已上传但建议补录”。
- backend 这轮也把 review queue 推成了正式 contract：[dataset-review.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/dataset-review.service.ts) 会把 `voice_contributions.metadata` 规范化成统一 queue item；[upload.controller.ts](/home/ubuntu/VoxFlame-Agent/backend/src/controllers/upload.controller.ts) 已新增受认证保护的 `GET/PATCH /api/upload/review-queue*`，可继续写入 `reviewed_at / reviewer / rejection_reason / accepted_for_export`；[upload-artifact.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/upload-artifact.service.ts) 也会把这些 review/export 字段带进 manifest `evaluation.review` 语义。
- dataset 的“导出与复核层”这轮也补了第一版落点：[dataset-export.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/dataset-export.service.ts) 与 [export_dataset_manifest.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/export_dataset_manifest.ts) 现在已能按 `accepted_for_export` 生成统一字段语言的 export manifest；当前环境下由于还没有被人工标记为 `accepted_for_export` 的样本，所以导出结果暂时为 0 条。
- 为了继续补 review/export 闭环但不碰真实样本数据，[mark_dataset_review_decision.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/mark_dataset_review_decision.ts) 也已新增并默认 dry-run；现在可以先预览 `accepted_for_export / reviewer / reviewed_at / rejection_reason` 回写 payload，只有显式 `--write` 才会真正改库。
- 产品与数据主文这轮也真正做了减法：[VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 现已明确“同一句允许保留多次练习样本”；[VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md) 已删掉一整段已完成的“建议新增 recording_id / manifest”式旧改造项，只保留现役 contract 和剩余行动项。
- 这轮还把自动保存落地后的残余旧心智一并清掉了：[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现在只把手动动作保留给 `自动保存失败 / 本地待同步` 补救，不再对正常主路径展示“手动保存训练样本”；[data-collection/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/data-collection/page.tsx) 也已补上“登录同意后自动保存、目标句 vs 识别句如何分层”的说明。
- 训练页主叙事这轮也继续纠偏：[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 已把“样本质量 / 复核状态”降成数据侧备注，明确告诉用户“只要录音完整就会上传”；前台主路径继续围绕发声动作、肌肉运动和下一步怎么练来组织，而不是把 review/export 心智推到用户面前。
- 训练反馈本体这轮也继续往“发声教练”收口：[mandarin-feedback.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/mandarin-feedback.ts) 现在会优先产出更简单的嘴巴动作、舌位、气息和节奏建议；[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 的反馈卡标题也已改成更接近教练语言，而不是拼音纠错器语言。
- 03-29 晚些时候又补做了一轮文档治理：dataset 主文现已从“为 App 准备”改成“为多端可复用产品准备”，明确数据面只需要做到 `录音事实 / 样本治理 / 导出复核 / memory-safe 聚合` 四层，不再把目标写成抽象的大数据平台；PRD 也已把下一阶段收口成三条并行基础线：`runtime/surface/control`、`memory/agent tooling`、`dataset/review/export`。
- `Runtime And Surface Reference` 这轮已吸收产品运行时层的 control/capability 关键内容，成为 runtime 主参考；[control-plane.md](/home/ubuntu/VoxFlame-Agent/docs/control-plane.md) 现在只保留 backend 控制面实现深文档角色，[capability-registry.md](/home/ubuntu/VoxFlame-Agent/docs/capability-registry.md) 也已退回“仓库协作 capability 盘点表”，不再和平级产品主文竞争。

### 2026-03-24

- 已把 `lime` 的组织方式吸收进 [AGENTS.md](/home/ubuntu/VoxFlame-Agent/AGENTS.md)：根入口只放仓库级规则、文档索引和高频路由；长流程下沉到 `docs/`。
- 已把 `Context7` 前置成专业文档检索默认入口，把 `Playwright` 前置成浏览器验证默认入口。
- 已新增 [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md)，把当前产品定义正式收成“主动沟通助手 + 练习工作台 + 沟通档案”的单一主文档，并吸收旧 reset / strategy / roadmap 的有效结论。
- 已进一步把 PRD 吸收五层架构、Agora 替换研究、记忆机制、agent tooling 分层与 Google-like UI 方向；现役任务入口只保留 PRD 与 `.tasks/current.md`。
- 已补做一轮代码级复核：`CommunicationStarterKit` 已存在但未接入沟通页主路径；`useRtcAgentSession` 已接近“第二控制面”；长期状态当前分散在 frontend local memory、backend Supabase 与 TEN memory layer；TEN main control 近期应停长产品语义。
- 已补做一轮人机协作体系研究与收口：仓库协作现在明确采用“先本地，后官方；先文档，后联网；先验证，后结论”的升级梯度，并补充了 `docker compose` / `sudo docker compose` 的环境化验证规则，以及“自动吸收经验、自动清理失效入口、自动同步状态”的自演进机制。
- 已完成沟通工作台第一阶段首屏改造：[ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 现在把 `CommunicationStarterKit` 放到主首屏，把 `QuickPhrasesPanel` 收成右侧 `表达工具箱` 抽屉，并已在真实浏览器里验证 starter phrase 可自动连接并代播。
- 已完成沟通工作台第二阶段最小读模型：[supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 新增 `profile_bundle / session_review / expression_kit` 聚合输出，[ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 已开始消费它，在首屏展示“个体化表达建议 + 最近一次沟通复盘”。
- 已收掉两类运行噪音：[user-nav.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/ui/user-nav.tsx) 不再请求不存在的默认头像；沟通页未登录态下也不再提前请求 `workspace` 接口制造 401。
- 已完成“我的沟通偏好”最小闭环：[CommunicationPreferenceCard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/CommunicationPreferenceCard.tsx) 允许用户保存 3 句关键表达，backend 会把它们并入 `workspace` 聚合，且已在真实浏览器里验证保存后会立即回流到首屏个体化建议。
- 已把“用户功能先研究人、固定功能先查技术文档”写成正式协作规则，并新增 [USER_RESEARCH_HANDOFF_TEMPLATE.md](/home/ubuntu/VoxFlame-Agent/docs/aiprompts/USER_RESEARCH_HANDOFF_TEMPLATE.md)，方便后续直接把访谈、观察、问卷和 field notes 喂给 agent。
- 已把这套研究再收成“创始人即用户”模式：如果产品创建者本人就是目标用户，默认优先研究他的真实场景、失败瞬间、情绪成本和补救动作，再外推到更广泛的人群假设。
- 已新增 [VOXFLAME_REFACTOR_COLLABORATION_PLAYBOOK_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_REFACTOR_COLLABORATION_PLAYBOOK_2026-03-24.md)，明确 `研究在 /home/ubuntu，开发在 /home/ubuntu/VoxFlame-Agent`，以及 `gstack / superpowers / 设计专项 skill` 的协作定位。
- 已新增 [docs/aiprompts/README.md](/home/ubuntu/VoxFlame-Agent/docs/aiprompts/README.md) 与第一批 task-oriented 文档，后续根 `AGENTS.md` 继续保持短入口。
- 已新增 [SKILL_ROUTING_GUIDE.md](/home/ubuntu/VoxFlame-Agent/docs/aiprompts/SKILL_ROUTING_GUIDE.md)，把 `gstack`、工程纪律 skill、设计专项 skill 以及 `Context7 / Playwright / Linear` 的默认路由从根入口继续下沉。

### 2026-03-25

- 已把“创始人即用户”的一手研究正式吸收到当前产品判断：当前最关键的高压场景是 `求职 / 面试`、`工作协作`、`医疗沟通`、`陌生人求助`；最需要被产品解决的并不只是识别准确率，还包括被打断、被催促、被忽视和被替代发言的社交压力。
- 首页已开始按这个判断重排：[HomeDashboard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/home/HomeDashboard.tsx) 现在围绕 `开场 / 补救 / 准备` 组织首屏，并把高压场景卡和三个工作台入口前置，不再把首页做成说明页。
- 已把这套判断写回产品主文：[VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 现在明确要求首页先承接高压沟通场景，且强调“用户始终主导、产品不能增加社交压力”。
- 已补齐 UI 视觉规范入口：[design-language.md](/home/ubuntu/VoxFlame-Agent/docs/aiprompts/design-language.md) 现已存在，后续页面改版不再依赖失效路径或口头约定。
- 已新增 [FOUNDER_COLLABORATION_LOOP_2026-03-25.md](/home/ubuntu/VoxFlame-Agent/docs/FOUNDER_COLLABORATION_LOOP_2026-03-25.md)，把“继续开发 + 给创始人补短阅读 + 一起讨论产品/技术判断”的协作机制收成正式文档。
- 已补本地浏览器白屏第二层保护：[usePWA.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/usePWA.ts) 负责在 `localhost` 下清理旧 SW/cache；[useAuth.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useAuth.ts) 负责在 Supabase 鉴权初始化超过 3 秒时先退回游客态，避免 VSCode 端口转发场景因本机网络访问 Supabase 变慢而一直卡在首页 loading。
- 已把这层 auth 兜底进一步收口：只有首页使用 `timeoutBehavior = guest`，受保护页面仍保持严格 auth；同时 `useAuth` 已正确消费 `INITIAL_SESSION`，避免初始会话事件被忽略。
- 已开始把沟通页首屏做成场景感知的个体化 phrase rail：[CommunicationStarterKit.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/CommunicationStarterKit.tsx) 现在会把当前场景同步给父层；这层 starter 场景偏置最初以前端排序做过渡，已在 03-26 正式下沉到 backend。

### 2026-03-26

- 已把 `Expression Kit` 的场景排序正式下沉到 backend：[expression-kit.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/expression-kit.service.ts) 现在负责 scene-aware personalized phrase 排序，[memory.controller.ts](/home/ubuntu/VoxFlame-Agent/backend/src/controllers/memory.controller.ts) 已支持 `GET /api/memory/workspace/:userId?scene=...`。
- [supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 现在会在 `workspace.expression_kit` 中返回 `active_scene_id`，让 scene-aware `Expression Kit` 变成正式 contract，而不再只是前端局部排序。
- [ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 已去掉重复的 `workspace` 拉取实现，改成围绕当前 starter 场景显式刷新同一份 backend snapshot；临时的前端本地排序文件也已删除，避免双事实源。
- 已把 `workspace` 继续收口成前端共享能力：[useWorkspaceMemorySnapshot.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useWorkspaceMemorySnapshot.ts) 和 [workspace-snapshot.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/memory/workspace-snapshot.ts) 现在为沟通页和沟通档案页提供同一份类型与取数逻辑。
- [memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 已开始从“统计页”转成“准备页”：场景化准备区现在会展示最近复盘、画像摘要和按场景优先的个体化表达建议，使 `沟通工作台 -> 沟通档案` 第一次拥有连续数据流。
- [config.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/config.ts) 现在默认优先走同源 `/api` rewrite，而不是依赖浏览器直接访问 `:3001`；这是当前 VSCode / SSH 转发环境下更稳的前端 API 基线。
- 训练页上传链路这轮也被收紧了：[useMandarinTrainingSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useMandarinTrainingSession.ts) 现在会产出带 `recording_id / session_id / duration_ms / sample_rate` 的 recording envelope；[useVoiceUpload.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useVoiceUpload.ts) 会把这些字段强制带入上传 metadata。
- 已新增 [recorder-queue.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/recording/recorder-queue.ts)，本地降级录音现在优先走 IndexedDB recorder queue，而不是把 base64 塞进临时 `localStorage` 列表。
- [upload.controller.ts](/home/ubuntu/VoxFlame-Agent/backend/src/controllers/upload.controller.ts) 现在会为训练样本追加 `dataset/{user_id}/manifest.jsonl`，保留 `transcripts.txt` 兼容导出；[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 也开始把“本地待同步录音”显式展示出来，增强用户对“录音没有丢、后面还能补传”的信任感。
- 已新增 [upload-artifact.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/upload-artifact.service.ts)，backend 现在会尽量按 `contributor_id + audio_path` 复用已有 contribution，并用 `upload_receipt` 避免 `upload/complete` 在重试时重复追加 manifest。
- 已新增 [training-scenes.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/training-scenes.ts)，把 founder-user 的高压场景直接映射到训练页；[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现在会按当前训练场景拉取同一份 `workspace` snapshot，并展示最近沟通复盘、训练 focus 和值得记住的画像项。
- 训练页这轮还补了“训练资产状态”真相层：[useVoiceUpload.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useVoiceUpload.ts) 已显式区分 `uploaded` 与 `queued_locally`；[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 会展示 `recording_id / session_id / consent_scope / manifest`。仅本地待同步的录音不再被误算进训练画像累计。
- 训练页这轮继续把本地录音队列补成“可追踪资产”：IndexedDB `recorder queue` 现在会记录 `syncStatus / syncAttempts / lastAttemptAt / lastError`，训练页能直接看到哪条录音还在本机、最近一次同步有没有失败，以及后续还能不能继续重试。
- 沟通页这轮也修了一处明显的产品偏差：主路径里不再展示“把最重要的三句话固定下来”这种硬性三栏表单，[ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 现已改成继续围绕场景优先的可点击表达 rail 组织，并明确提示“点一下就会直接代播”。
- 已针对 `localhost / Edge` 白屏问题做了一轮架构级止血：确认首页 HTML、静态 chunk、正常 Chromium 渲染都没问题后，把 `next-pwa` 从“默认注入”改成“显式开关”。当前 [frontend/next.config.js](/home/ubuntu/VoxFlame-Agent/frontend/next.config.js) 和 [frontend/src/app/layout.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/layout.tsx) 仍以 `VOXFLAME_ENABLE_PWA=1` 启用 PWA bootstrap / PWA status center，但现役容器默认已重新开启；本地排障时再临时关闭。

### 2026-03-26

- 训练页这轮已修掉“场景显示和标准句不一致”的真实 bug：[contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现在默认按 `DEFAULT_TRAINING_SCENE_ID -> primaryCategory -> first exercise` 初始化；Playwright 已确认 `/contribute` 默认会从 `求职 / 面试 -> 人群与角色` 句库起步，不再错误落到“我想和朋友说话”。
- 训练页中段不再把大块说明文放在录音前面；现在已改成 `场景 -> 固定语料分类 -> 可点击句子列表 -> 当前标准句`，且点某句后右侧录音目标句会同步切换，真实浏览器里已确认“老师即时作答”等句子可直接切换。
- 训练页录音启动顺序已加一层麦克风预热：[useRtcAgentSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useRtcAgentSession.ts) 现在会先执行 `warmUpMicrophoneStream()` 再继续 RTC 训练会话，目的不是改权限文案，而是把训练页的实际启动链路拉得更接近沟通页，减少初始化阶段的假性麦克风报错。
- 训练页当前如果在“权限已给出”时仍拿不到麦克风流，会明确提示这是训练录音初始化问题，而不是继续笼统地说“请检查权限”；这样至少把责任边界说清楚了。
- 训练页首屏已进一步收口：顶部那层“左边解释 / 右边辅助卡”的说明式入口已经移除，现在页面一进来就直接是 `训练场景 -> 固定语料分类 -> 可点句子 -> 当前标准句 -> 录音`；辅助性内容则统一下沉到页面底部的 `更多设置与资源`。
- 训练语料分类现在固定对齐为：`全部语料 / 日常与出行 / 看病与求助 / 人群与角色 / 设备与数字 / 发音与朗读`，不再混用场景推荐分类和语料分类两套入口。
- 训练录音链路已收掉一个关键坏体验：如果本地录音初始化失败，页面现在会先终止这次录音尝试，而不是立刻把整个 RTC 训练会话断掉；同时本地录音会优先复用真实的麦克风 `MediaStream`，减少“沟通页能录、训练页却连上就断”的差异。
- 训练页这轮还修掉了一个真实生命周期 bug：`/contribute` 原先把 `disconnect` 放进依赖化 cleanup effect 里，而训练 hook 的 `disconnect` 又会随着 `sessionId` 变化重建；结果是一旦训练会话刚连上、`sessionId` 更新，旧 effect cleanup 就会把新会话自己断掉。现在页面只在真正 unmount 时才清理训练会话，`sessionId` 也已改走 ref，避免 callback identity 变化再次误触发断连。

### 2026-03-23

- 已围绕 `openclaw / agents / vixio / deerflow` 的研究先后写过多份仓库分析稿；这些结论现已在 2026-03-26 收口成 [VOXFLAME_RUNTIME_AND_SURFACE_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_RUNTIME_AND_SURFACE_REFERENCE_2026-03-26.md) 和 [VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md)。
- 已新增 [control-plane.md](/home/ubuntu/VoxFlame-Agent/docs/control-plane.md) 和 [capability-registry.md](/home/ubuntu/VoxFlame-Agent/docs/capability-registry.md)，控制面与能力治理进入正式文档层。
- 已新增 [VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md)，把 `ququ + vocotype-cli` 的经验收口成训练数据与 recorder 方案。

### 2026-03-22

- 这一天没有单独新增长期文档，但当前协作规范已按“只保留最近 3 天动态状态”的方式开始清理入口文件。

## 当前重点

- 把产品主路径正式收口成三块：`沟通工作台 -> 练习工作台 -> 沟通档案`，避免页面和文档继续漂在“说明书式产品”状态。
- 让首页真正承担高压场景入口，而不是继续停留在“解释产品做什么”的层面。
- 继续把首页高压场景入口和沟通页 starter kit 做成真实联动，而不是静态展示。
- 继续把产品 UI 方向收口成更接近 Google 的风格：干净、任务优先、低噪音、glanceable、可编辑，而不是依赖失效产品功能做参考。
- 沟通页前两步已经完成：先把首屏改成 `starter kit + live session + expression kit drawer`，再把 backend 的 `workspace` 聚合读模型接进来；下一步重点是让个体化表达真的长出更准的 personalized phrase rail，而不只是空状态和基础复盘。
- 现在已经有了第三步：用户可以先固定“最重要的三句话”；下一步重点是把这些偏好和 quick phrases / hotwords / recent wins 做更聪明的排序融合。
- `workspace` 现在已被沟通页和沟通档案页共用；下一步应继续把这份 contract 推到训练页和首页摘要，而不是再长出新的页面级取数分支。
- 训练页现在的即时反馈、上传 contract、本地队列和 `upload/complete` 幂等保护都比之前扎实得多，但 authenticated live upload、OSS manifest 真落盘和断网重试还需要继续补真实 smoke。
- 如果下一步继续做“更懂用户”的交互或文案功能，默认先等一手用户研究输入；你可以自己做调研，我来负责把材料翻译成 PRD / UI / 工程约束。
- 对当前项目，这个“一手用户研究输入”默认首先来自你本人，因为你不是旁观者，而是 `VoxFlame` 的第一核心用户。
- backend 下一步重点不是再加零散 memory API，而是补 `profile bundle / session review / expression kit merge` 正式读模型。
- 前端下一步重点是把 session hooks 变薄，避免会话 bootstrap、消息归并和长期画像同步继续堆在一个 hook 里。
- 继续深化训练链路，尤其是真实麦克风下的 `RTC 上行音频 -> transcript -> training_feedback -> voice_profile` 闭环。
- 继续把 `memory bundle / profile bundle / dataset-memory boundary` 落成更明确的 contract。
- 在 `light voice surface` 与现役 `heavy realtime` 之间补更明确的 `session_strategy` 控制面语言。
- 训练页的 UI 主路径、登录授权边界、真实前端 smoke、历史样本 receipt/manifest 对账和 review queue contract 已经收口；下一步重点应转到当前 2026-03-29 版本真实麦克风下的新样本字段验证，确认 `sample_quality_* / confidence / latency_ms / review_* + upload receipt + manifest + training profile sync` 一起跑通。
- 文档层下一步不再继续平铺更多平级参考，而是沿三份主参考推进：`PRD` 管产品主线，`Runtime And Surface Reference` 管多端/运行时主线，`Agent Memory And Tooling Reference` 管记忆与 agent/tooling 主线。

## 当前验证基线

- 文档一致性检查可用：`bash scripts/check_ai_docs.sh`
- 首页改版后需要补一次真实浏览器 smoke，重点确认高压场景卡、三个工作台入口和移动端布局
- Qwen ASR/TTS live smoke 已通过
- 沟通工作台新首屏已通过真实浏览器 smoke：starter kit 可见，`表达工具箱` 可开关，starter phrase 可自动触发连接与代播
- 沟通工作台的 `workspace` 个体化区已通过真实浏览器 smoke：会在登录态下展示“个体化表达建议 + 最近一次沟通复盘”
- 当前控制台已不再出现 `/avatars/01.png` 404，也不再在未登录态下出现 `workspace` 401
- “我的沟通偏好”功能已通过真实浏览器 smoke：3 句偏好可保存，且保存后会立即回流到首屏建议
- Playwright fake-mic smoke 已确认：沟通页与训练页的录音入口能进入稳定录音态
- 训练页当前已能按需建立 RTC + RTM 会话，但真实上行音频闭环仍需继续验证
- 训练页 2026-03-28 UI/授权 smoke 已确认：登录页双授权可见且可生效；训练页只保留 `录音 / 训练 / 反馈` 主叙事，5 个真实分类、句子搜索和分类切换都正常；当前 headless 环境下点击录音会明确暴露“无麦克风”错误，而不是静默失败
- 训练页现在已经具备“场景化准备 + 共享画像 + 训练资产回执”，而且当前环境里历史真实样本已补齐到 `voice_contributions.metadata.upload_receipt + manifest.jsonl`；后续应在真实登录态下继续确认 03-29 新增的 `sample_quality_* / latency_ms / confidence / review_*` 字段也会稳定进入新样本 manifest
- 数据录入与上传这条线下一步除了真实 smoke，还应继续补数据库唯一键 / upsert contract，降低并发重试时的重复写入风险
- dataset review queue 这轮已通过 backend build 收口成正式 API contract；但 `npm run review:queue -- --status retry_recommended --limit 1` 在当前环境仍被 Supabase DNS `EAI_AGAIN` 挡住，所以真实在线队列读取还要等网络恢复后补一刀
- `npm run export:manifest -- --limit 1` 已可正常生成第一版 dataset export manifest，当前输出为 0 条只是因为库里还没有 `accepted_for_export=true` 的样本，不是脚本链路没通
- `npm run review:mark -- --help` 已可正常展示默认 dry-run 的 review/export 标记入口；当前仍遵守“不改真实样本数据”的边界，尚未执行任何真实回写
- docker 日志这轮补了一处 dataset 可观测性收口：[upload.controller.ts](/home/ubuntu/VoxFlame-Agent/backend/src/controllers/upload.controller.ts) 现在会在 artifact persistence 完成后再打印上传日志，并显式带出 `reusedContribution / manifestAlreadySynced / transcriptAlreadySynced`；这让容器里重复出现的 `upload/complete` 更容易区分是正常幂等重试，还是异常重复写入。当前日志里能看到 `manifest/transcripts` 的 append position retry，但还没有看到因此导致的致命失败，dataset 线目前不需要继续阻塞主线。
- localhost / VSCode forwarded 浏览器白屏现在有两层代码防护：旧 SW/cache 会被主动清理；本地浏览器若连 Supabase/Auth 超时，首页会在 3 秒后先以游客态渲染
- localhost / VSCode forwarded 浏览器白屏现在额外补了“PWA 开着也能自愈”的前置修复：localhost 会在 hydration 前先清 runtime state，且 localhost 不再重新注册 SW
- 麦克风验证的默认路径已经明确：优先使用 VSCode/SSH 转发后的 `http://localhost:3000`；不要长期依赖 `--unsafely-treat-insecure-origin-as-secure=...`；公网地址若要直接验证麦克风，应补 HTTPS 与证书
- scene-aware `workspace` contract 这轮已通过 backend 编译、frontend lint 和 `sudo docker compose up -d --build backend frontend` 验证；但在线 Supabase smoke 暂时被当前环境的 DNS `EAI_AGAIN` 挡住，还没补到 authenticated API 的最后一刀
