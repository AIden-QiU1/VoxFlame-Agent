# VoxFlame Agent - 项目概述

> 这里只保留当前产品定义、现役事实源、最近 3 天仍有效的收口和下一步必须知道的状态。

## 产品定位

- 核心目标不是“纠正用户的声音”，而是“纠正系统对用户意图的理解”。
- 当前主产品链路是：`沟通工作台 -> 练习工作台 -> 沟通档案(memory/workspace)`。
- 当前迁移主线已经进入清退阶段：沟通与训练执行面都收口到 `self-hosted LiveKit + livekit_agent`。
- `ten_agent/` 目录已经从仓库物理删除，旧执行面不再是代码基座的一部分。
- `frontend / backend / livekit_agent` 代码层已经没有 `Agora/agora_ten` 运行时引用，残留的只是不影响运行的历史迁移文档。
- `docker-compose` 默认启动路径也已更符合最佳实践：
  - 默认核心栈是 `livekit-server / backend / frontend / livekit-agent`
  - `qdrant / redis` 改为 `extras` profile，不再拖主链启动
  - LiveKit server 默认镜像已钉到 `docker.m.daocloud.io/livekit/livekit-server:v1.10.1`
  - 当前容器实测已处于 `Up` 状态

## 现役事实源

- 2026-04-19 记忆页“自定义材料”已经不再是单文档壳子：
  - backend durable shape 已改成纯粹的 `prepared_expression_assets.active_asset_id + assets[]`
  - 不再维持旧 `prepared_expression_asset` 单份 owner
  - memory API 也已改成复数资源风格：
    - `GET /api/memory/workspace/:userId/prepared-expressions`
    - `PUT /api/memory/workspace/:userId/prepared-expressions`
    - `PUT /api/memory/workspace/:userId/prepared-expressions/active`
    - `DELETE /api/memory/workspace/:userId/prepared-expressions/:assetId`
    - `POST /api/memory/workspace/:userId/prepared-expressions/:assetId/summarize`
  - `workspace snapshot` 已新增 `prepared_expression_library`
  - `object_zones.custom_materials` 现在会列出整份材料库，而不是只暴露当前一份
  - [frontend/src/app/memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 已支持多份材料的新增、选择、加载、展开、编辑、删除
- 一个未接入主路由、但会污染构建的训练页拆页半成品已经删掉，不再把未完成组件留在 `src/` 里
- 2026-04-19 训练页已经真正拆成两层：
  - `/contribute` 只做主题选择
  - `/contribute/topic/[topicId]` 只做录音训练
  - 当前 recorder route 不再夹带主题选择逻辑
- 当前关于“有没有多套过时 / 平行逻辑同时在跑”的最新判断：
  - 训练页：之前确实存在“主页面里继续跑录音工作区”的并行逻辑，现已收口
  - 沟通页：之前有一块重复展示 `communicationLoadout` 的摘要卡，现已删掉
  - 记忆页：当前没有再发现前台并跑的第二套旧材料管理 UI
- 现役执行面已经收口为 `frontend -> backend -> self-hosted livekit-server -> docker livekit-agent`。
- 模型/provider 现役约束仍是 `DashScope / Qwen-first`。
- `dataset != memory`，记忆继续由 `workspace / profile bundle / session review / expression kit` 语义承接。
- `LiveKit != durable memory owner` 这个判断也已经重新收清：
  - `LiveKit` 负责 session-local state、room state 和 session report raw material
  - `backend + workspace snapshot` 继续是 durable memory owner
  - `Qdrant` 适合后续 semantic recall layer
  - `Redis` 只有在明确需要 ephemeral coordination/cache 时再引入
- docs 目录也继续收口：
  - 已删除一个无引用、已脱离主线的旧研究文档：`CORPUS_SOURCE_RESEARCH_2026-03-19.md`
  - 也已删除三份已经和现役主线打架的旧 LiveKit 迁移文档：
    - `VOXFLAME_LIVEKIT_TRANSITION_PLAN_2026-03-31.md`
    - `VOXFLAME_LIVEKIT_REPLACEMENT_ROADMAP_2026-04-02.md`
    - `VOXFLAME_LIVEKIT_COMMUNICATION_MINIMAL_LOOP_PLAN_2026-04-02.md`
  - 当前关于 LiveKit、memory 和数据录入的现状与下一步，只以：
    - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md)
    - [.tasks/current.md](/home/ubuntu/VoxFlame-Agent/.tasks/current.md)
    - [VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md)
    - [VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md)
    为准
  - `PRD / current task / 历史深参考` 的边界也重新写清了：
    - `PRD` 负责产品定义、页面职责和当前代码现状
    - `.tasks/current.md` 负责短期执行顺序、验证和优先级
    - `control-plane / unified memory` 不再代表当前代码真相，只保留为深参考

## 最近 3 天有效状态

### 2026-08-05

- 已按沪浦网信安通〔2026〕267号附件 2 的格式生成正式网络安全整改报告 Word / PDF：
  - 法定代表人、网络安全主要负责人按邱生峰表述，报告采用营业执照信息和当前生产架构
  - 2026-08-05 公网复测确认五张用户数据表 anon 均为 `401`，活动系统预设只读为 `200`，service role 正常；官网与 RTC health 为 `200`，未登录 workspace 为 `401`
  - `test1@poc.com` 仍停用至 2126 年；公开注册仍开启、邮箱仍自动确认、CAPTCHA 尚未启用
  - 正式报告未把未完成事项写成已完成：日志六个月留存、等保定级咨询、publishable key 迁移、注册强化、EdgeOne/WAF 接管和源站收口均列入限期后续计划
  - 报告正文源文件为 [docs/上海生声不息科技有限公司网络安全整改报告_沪浦网信安通2026_267号.md](/home/ubuntu/VoxFlame-Agent/docs/上海生声不息科技有限公司网络安全整改报告_沪浦网信安通2026_267号.md)

### 2026-08-01

- 独立手机号注册 / 登录已部署到 `voxember.com` 与 Mobile Workbench，邮箱登录保持现状：
  - backend/frontend 已切到包含 Phone Auth 与腾讯云 SMS Hook 的新镜像，两个容器均为 healthy
  - 腾讯云 `SecretId / SecretKey`、SDK AppID、签名和模板均已正确注入 backend；Supabase HTTPS Send SMS Hook 与 `SUPABASE_SEND_SMS_HOOK_SECRET` 已配置
  - 产品边界已改为邮箱 / 手机并列注册登录；手机号注册创建独立 phone-only Supabase 用户，不要求绑定邮箱账号或保持同一 UUID；注册 / 登录分别使用 `shouldCreateUser: true / false`
  - backend 当前为 `PHONE_AUTH_ENABLED=1`、`TENCENT_SMS_DRY_RUN=0`；Supabase Phone provider、HTTPS Hook、Web 和 EAS 三套 Mobile 公共开关均已开启
  - backend 已兼容 Supabase 生产 Hook 的无加号 `861...` 号码并规范化为 E.164；真实请求已到腾讯云，但返回 `FailedOperation.SignatureIncorrectOrUnapproved`
  - 当前签名配置与原计划的公司全称一致；CAM 子用户没有 `sms:DescribeSmsSignList`，需从腾讯云“国内短信 -> 签名管理”核对实际签名内容、审核 / 运营商报备状态和应用关联后重试
  - 公网 `/`、`/login`、`/api/rtc/health` 均返回 `200`；Playwright 已验证邮箱 / 手机登录、邮箱 / 手机注册和手机号注册表单
  - 回滚镜像标签为 `voxflame-agent-backend:pre-phone-auth-20260801` 与 `voxflame-agent-frontend:pre-phone-auth-20260801`
  - Android `0.1.2`（build 3）EAS preview `b47cd371-ef9f-409f-a45d-1e1d180a26dd` 已完成，网站下载页已切到该构建
  - 下一步只剩确认签名状态与应用关联、单条真实短信、Web 手机注册 / 再次登录和 Android 真机 smoke
- Docker 运维经验已收进 harness：
  - `scripts/docker-rebuild-core-fast.sh` 支持 `env-backend / backend / frontend / core`，环境变量更新只 recreate backend，不再默认先执行 `docker compose down`
  - `scripts/docker_disk_maintenance.sh prune-safe` 只删除 7 天前 dangling images 与 build cache，保留运行镜像、卷、`latest` 与 `pre-*` 回滚镜像
  - 本次清理释放约 `11.5GB`，根盘从 `87%` 降至 `65%`，可用空间由 `7.8GB` 增至 `21GB`；5 个生产容器与两个 Phone Auth 回滚镜像均保留

### 2026-07-31

- 手机号登录最初按绑定账号实现，已在 2026-08-01 根据产品决定改为独立手机号注册 / 登录：
  - Supabase Auth 继续是身份事实源；邮箱和手机号允许分别注册独立账号，不做自动合并
  - Web 与 Mobile 已区分注册 / 登录的 `shouldCreateUser`，账号绑定页仅作为可选能力保留
  - backend 已新增腾讯云 SMS adapter 和 Supabase Send SMS Hook，包含 Standard Webhooks 签名校验、号码 / OTP 校验、幂等、单号码限流和脱敏日志
  - Web 已新增邮箱 / 手机并列登录入口与 `/settings/account` 绑定页；Mobile Workbench 已在登录页与“我的”页提供同等流程；绑定完成后均守住原 UUID
  - 腾讯云 CAM 最小权限子用户凭据已安全保存到 `backend/.env`；轻量应用服务器不使用误建的 CVM 服务角色
  - 默认安全开关、dry-run 与 Hook 验证阶段已完成；当前已开启真实发送和公共入口
  - 验证通过：backend 针对性测试 / build / 签名 HTTP dry-run，Web 64 tests / typecheck / production build，Mobile check / typecheck / Android + iOS production export
  - 下一步：修正腾讯云短信签名 -> 一条真实短信 -> Web / Android 手机注册和再次登录 smoke

### 2026-07-30

- 沪浦网信安通〔2026〕267号所述 Supabase 未授权读取问题已完成生产封堵，完整证据见 [NETWORK_SECURITY_REMEDIATION_2026_267.md](/home/ubuntu/VoxFlame-Agent/docs/NETWORK_SECURITY_REMEDIATION_2026_267.md)。
- 生产六张 public 表全部启用并强制 RLS；五张用户数据表对 `anon / authenticated` 无直接权限，系统 `preset_phrases` 仅允许活动记录只读；`service_role` 为最小 DML、无 `TRUNCATE`。
- 生产回归脚本确认通知中公开的 anon key 访问 `user_profiles / sessions / memories / voice_contributions / quick_phrases` 均返回 `401`，service role 仍正常。
- `test1@poc.com` 已长期停用且业务表记录为 `0`；负责人已明确决定保留 2 条 Auth session 作取证，不执行撤销。
- 代码侧已删除前端未使用的 Supabase 数据直连函数，后端 service role 改为 fail-closed；backend/frontend 生产构建均通过。代码强化尚未单独部署，生产封堵不依赖此次代码发布。
- 负责人已选择保留公开注册，并要求启用邮箱验证、CAPTCHA 与速率限制；当前仍缺生产 SMTP、Turnstile site/secret key 和可用的 Supabase 管理 token，且全局 CAPTCHA 会影响尚未接 token 的 Mobile Workbench 登录，因此生产尚未强行切换。

### 2026-07-24

- Mobile Workbench 第一版已收口到 `0.1.0` 双平台可打包状态：
  - Expo / React Native App 现在是 `沟通 / 练习 / 准备 / 我的` 四个任务页，不再向用户暴露 RTC、token、Backend 或 contract 诊断说明
  - 仍复用现役唯一事实源：Supabase Auth、backend workspace/upload/RTC API、self-hosted LiveKit；没有新增移动端私有后端或 WebView 主链
  - RTC 已接齐 backend `start / ping / stop` lifecycle，练习页可管理整份本地录音队列
  - Android 与 iOS production Metro export 均通过；静态检查、TypeScript、Expo public config 均通过
  - Android preview APK 已构建成功；尚需 Android 真机完成登录、同步、录音、上传、LiveKit 与蓝牙耳机 smoke，iOS 仍需 Apple 签名/设备授权后生成可安装包并完成同等真机 smoke
  - EAS 命令已从错误的 `npx eas ...` 统一修成 `eas-cli`；仓库 `npm run eas:login` 会自动绕过服务器失效代理并恢复正常 TLS 校验，实测 `eas-cli/21.1.0` 可启动
  - VS Code / SSH 环境不再走随机 localhost OAuth callback；`npm run eas:login` 固定使用官方 `--no-browser` 终端登录
  - Expo token 认证已验证为账户 `qiud`，同时拥有 `qiuds-team` Owner；App owner 固定为团队，`npm run eas:configure` 负责安全创建/绑定项目并同步三套公共客户端环境
  - Expo Personal Access Token 现在可通过根目录 `npm run eas:save-token` 隐藏保存到当前用户的 `~/.config/voxflame/expo-token`；目录权限 `700`、文件权限 `600`，所有项目 EAS/构建命令自动加载，重新 SSH、重开终端或重启服务器后无需再次输入
  - 根目录已提供 `npm run eas:whoami`、`npm run build:android:preview` 与 `npm run build:ios:preview`，避免再次因在错误目录运行而出现 Missing script
  - Android EAS build `50814787-79bf-44aa-a8d5-1bb0296aa59a` 已完成；首次失败根因是 AsyncStorage `3.0.2` 引用了 Maven 不存在的 `storage-android:1.0.0`，已锁定到 Expo SDK 55 指定的 `2.2.0` 并加入版本守卫
  - Android 权限已按音频 V1 收口：保留麦克风、音频路由、唤醒锁与蓝牙耳机能力；Android 12+ 开始沟通时按需请求 Nearby devices，拒绝后回退手机音频；相机与悬浮窗从最终 Manifest 移除
  - iOS 最新 production Metro export 通过（`902 modules / 4.9 MB`）；已补 `ITSAppUsesNonExemptEncryption=false`，当前 EAS preview build 唯一阻塞是首次 Apple signing credentials / internal distribution provisioning，需 Apple Developer 账户在交互终端创建或导入
- Web 已提供 App 发布入口：
  - 新增 `/download`，Android 链接从 `NEXT_PUBLIC_ANDROID_APP_DOWNLOAD_URL` 读取，iOS/TestFlight 链接从 `NEXT_PUBLIC_IOS_APP_DOWNLOAD_URL` 读取
  - 首页已去掉重复说明和大面积渐变，收成一个主动作、三个任务入口与一个 App 内测入口
  - 前端生产构建和 `/download` HTTP smoke 通过；Playwright 当前被本机 Chrome `SIGSEGV` 阻断
  - 公网 `https://voxember.com/download` 已部署 Android 开放内测入口并指向成功构建页；Dockerfile/Compose 已接通 Android/iOS 下载 URL build args，本次因 Docker socket 权限使用 `sudo docker compose` 仅重建 frontend

### 2026-07-19

- “音系强化”已增加真实音系二级小组，不改变原主分类和上传 schema：
  - 使用本地 `pinyin-pro` 为清理后的 `2973` 条音系句离线建立声母、韵母、声调和变调索引
  - 小组包括双唇与唇齿音、舌尖中音、舌根音、舌面音、平舌与翘舌音、前后鼻韵母、复韵母、声调与变调，另保留全部音系句
  - 声母 / 韵母至少命中 2 个目标音节；声调只收四声覆盖、三声连读或“一 / 不”变调；每句最多保留 3 个最相关专项
  - 训练页支持小组筛选并显示当前句真实命中音位；录音中 / 处理中禁止切换，避免状态错位
  - 当前索引覆盖 `2973/2973`，8 个专项分别有 `252 / 725 / 196 / 518 / 1445 / 2041 / 1472 / 2235` 条
  - 验证通过：音系索引 4 项回归、前端 61 项测试、TypeScript、Next 生产构建；未登录浏览器按现有鉴权跳转登录页
- 训练区低质量语料已按最新边界完成第一轮逐句严重污染清理：
  - 不采用固定删除数量，也不按来源低质量比例整源退出；来源继续逐句评审
  - 普通新闻、财经 / 地产、影视对话、有效医疗与求助表达保留
  - 只删除明确色情、直接暴力、广告 / 考试站 / 订阅导流、确定 ASR 重复 / 填充 / 拼接残片和严重悬空句
  - 以原正式池做最小差异清理，不重生成补位；生成池从 `8980` 条变为 `8776` 条
  - 前端合并 curated 后共 `9112` 条，其中评估 `20`、非评估训练句 `9092`
  - 累计退出 `204` 条：`ASR 破损 152 / 广告导流 38 / 直接暴力 6 / 色情 5 / 严重悬空句 3`
  - [mandarin-training-real.cleanup-audit.json](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/generated/mandarin-training-real.cleanup-audit.json) 保存全部逐句理由
  - [clean_generated_training_corpus.py](/home/ubuntu/VoxFlame-Agent/scripts/corpus/clean_generated_training_corpus.py) 提供可重复执行的最小差异清理
  - 验证通过：Python 4 项回归、前端 58 项测试、`npx tsc --noEmit`；严重规则残留扫描为 `0`

### 2026-07-15

- 已完成普通话训练语料重构到 `7-18` 字、`8000+` 条：
  - 删除 `文言文节奏` 分类，新增 `会议与协作`、`车载与导航`、`音系强化`
  - 生成池 [mandarin-training-real.json](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/generated/mandarin-training-real.json) 当前为 `8980` 条：`现代文章朗读 5000 / 会议与协作 900 / 车载与导航 80 / 音系强化 3000`
  - 前端合并 curated 与 generated 后共有 `9316` 条训练项，其中 `评估筛查 20`、非评估训练句 `9296` 条
  - 主要来源为 AISHELL-1 transcript、AISHELL-3 content、AISHELL-4 TextGrid、WenetSpeech 修正版文本片段、普通话朗读作品；这轮生成不再使用 Tatoeba / 翻译例句
  - [export_frontend_source_corpus.py](/home/ubuntu/VoxFlame-Agent/scripts/corpus/export_frontend_source_corpus.py) 已支持 AISHELL-1、AISHELL-3、AISHELL-4 TextGrid、WenetSpeech id-prefixed transcript；WenetSpeech 解析按行流式处理，完整 `text.fix` 到位后可直接重刷
  - 导出规则已收紧：只收 `7-18` 个可见汉字，拒绝 ASCII/数字、网页 UI 噪声、古文/旧式语料、敏感新闻/政治/宗教/暴力/影视点歌/POI 片段、悬空虚词开头结尾、全局重复和高频近重复结构
  - 前端合并层已做全局 target text 去重，避免 curated 与 generated 跨分区重复
  - 已验证：`python3 -m py_compile scripts/corpus/export_frontend_source_corpus.py`、`cd frontend && npm test -- src/lib/corpus/mandarin-training-data/index.test.ts`（当前 npm script 实际跑 `55` 个测试，全部通过）、`cd frontend && npx tsc --noEmit`、`bash scripts/check_ai_docs.sh`

### 2026-07-14

- 已修复 `backend/scripts/manage_users.js` 里的硬编码 Supabase service role key，改为从 `backend/.env` 读取 `SUPABASE_URL` 和 `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_ROLE_KEY`，避免把管理密钥留在源码里。

### 2026-07-08

- 已完成 WAIC / 公安检查前的 Web 服务器侧安全收口，并把腾讯云 EdgeOne/WAF 配到等待 NS 切换的状态：
  - 交付文档：[docs/WAIC_SECURITY_CHECKLIST_2026-07-08.md](/home/ubuntu/VoxFlame-Agent/docs/WAIC_SECURITY_CHECKLIST_2026-07-08.md)
  - 运行态实测：`3000`、`3001`、`8081` 均只监听 `127.0.0.1`；公网只保留 Caddy `80/443`、SSH `22` 和 LiveKit `7880/7881`
  - [livekit_agent/app.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/app.py) 已把 LiveKit Agents SDK 健康 HTTP 服务绑定到 `127.0.0.1`；当前容器已热补 `/app/app.py` 并重启，`curl http://127.0.0.1:8081/` 返回 `OK`
  - 线上 `curl -I --noproxy '*' https://voxember.com` 已确认 CSP、Permissions-Policy、HSTS、X-Content-Type-Options、X-Frame-Options 等安全头存在，且不再看到 `X-Powered-By: Next.js / Express`
  - EdgeOne 站点已创建：ZoneId `zone-3sacn5q6g224`，PlanType `plan-personal`，目标 NS 为 `ns1.qeodns.com`、`ns2.qeodns.com`，当前仍 pending/inactive
  - EdgeOne 加速域名已创建：DomainId `edge-3sacy37kmnbh`，CNAME `voxember.com.eo.dnse2.com`，源站 `111.230.35.89`，HTTPS 回源，回源 Host `voxember.com`
  - EdgeOne DNS 当前根域 CNAME enabled，根域 A/CAA disabled；这是为了切 NS 后走 EdgeOne 加速/WAF，不继续直连源站。DNSPod 侧 CAA 已存在，但根域同名 CNAME 与 CAA 在 EdgeOne 侧冲突，暂不作为已完成项
  - EdgeOne 安全策略已设为 WAF 托管规则拦截：`ManagedRules.Enabled=on`、`DetectionOnly=off`、`wafgroup-free Action=Deny`；CC/DDoS、WebSocket、HTTPS 强跳、HSTS 已开启。Bot 当前仍是 off
  - 当前剩余人工/权限项：权威 NS 仍是 `eleven.dnspod.net`、`rich.dnspod.net`，公网 A 仍解析到 `111.230.35.89`；需要用户在腾讯云域名控制台把 NS 改为 `ns1.qeodns.com`、`ns2.qeodns.com`
  - NS 生效后继续：启用 EdgeOne HTTPS 证书，验证 EdgeOne 响应头，定位源站实例 / Lighthouse 防火墙并限制源站直连

### 2026-06-28

- 已把 `2307294809@qq.com` 对应 userId `64758dee-5026-4b53-a063-1d02d0834f67` 的沟通页与训练 / 评测页 ASR 路由接到 cpu1 本机 HTTP ASR 服务：
  - [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 的 `QWEN_HTTP_ASR_*` 命中范围已从仅 `communication` 放开到 `communication + training`
  - [docker-compose.yml](/home/ubuntu/VoxFlame-Agent/docker-compose.yml) 中 `livekit-agent` 改为 `network_mode: host`，默认 `QWEN_HTTP_ASR_URL=http://127.0.0.1:18000/transcribe`，默认 `QWEN_HTTP_ASR_USER_IDS=64758dee-5026-4b53-a063-1d02d0834f67`
  - `backend` 默认 `LIVEKIT_AGENT_HEALTH_URL` 已改为 `http://host.docker.internal:8081/`，用于 bridge 网络里的 backend 检查 host-network 的 livekit-agent
  - 注意：compose `environment` 会覆盖 `livekit_agent/.env`，所以 userId 白名单必须在 compose 默认值或根 `.env` 里存在；只改 `livekit_agent/.env` 不够
  - 延迟实测：本机 `/health` 约 `88-98ms`；3 秒评估筛查 WAV 单次 `/transcribe` 成功耗时约 `0.639s` 和 `1.850s`，返回 `发扬`
  - 风险：`127.0.0.1:18000/transcribe` 连续请求测试出现过瞬时 `connection refused`，随后 `/health` 又恢复；更像 ASR 服务自身连续请求 / worker 稳定性问题。HTTP ASR 失败时 livekit_agent 会回退 DashScope realtime ASR
  - 已验证：`python3 -m unittest livekit_agent.tests.test_asr_runtime`、`docker compose config | rg -n "QWEN_HTTP_ASR|network_mode|LIVEKIT_AGENT_HEALTH_URL|LIVEKIT_URL"`、本机 `curl /health` 与单条 `/transcribe`

### 2026-06-14

- 已重新拉取 `2026-05-24T00:00:00+08:00` 之后的 OSS 训练数据增量：
  - 输出目录：[artifacts/oss-by-account-after-20260524-refresh-20260614](/home/ubuntu/VoxFlame-Agent/artifacts/oss-by-account-after-20260524-refresh-20260614)
  - dry-run 与真实下载均为 `objects=720`、`listed=1216`、`bytes=221.6 MB`；本地目录约 `225M`
  - 账号分布：`2187054680__0983a35e` 517 个对象、`2307294809__64758dee` 195 个对象、`2440571672__77cab18e` 8 个对象
  - `_objects.jsonl` 共 `720` 行；lastModified 范围 `2026-05-31T03:22:41.000Z` 到 `2026-06-14T06:20:53.000Z`
- 已按用户新口径收口自定义材料 -> 训练语料切分规则：
  - 后端 [prepared-expression.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/prepared-expression.service.ts) 不再只把材料前 12 段生成 sections；所有材料内容都会进入结构化训练语料
  - 前端训练页 [prepared-expression-practice.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/prepared-expression-practice.ts) 与后端规则统一：目标 10-20 字，标点 / 自然停顿边界优先；短常用句或尾句可以保留；只有无标点超长文本才硬切
  - 训练页继续从 `document_content` 全量生成 exercises，并用测试保证 exercises 拼回去等于原材料全文，不跳过中间内容
  - 已新增 / 更新回归：
    - [backend/src/services/prepared-expression.service.test.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/prepared-expression.service.test.ts)
    - [frontend/src/lib/training/prepared-expression-practice.test.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/prepared-expression-practice.test.ts)
  - 已验证：`cd frontend && npm run build`、`cd backend && npm run build`、前后端 prepared-expression targeted tests、`bash scripts/check_ai_docs.sh`
- 已检查当前运行中的私有 HTTP ASR 云部署路由：
  - `livekit-agent` 容器内 `QWEN_HTTP_ASR_URL=`、`QWEN_HTTP_ASR_USER_IDS=` 均为空
  - 因此 `2307294809@qq.com` 对应 userId `64758dee-5026-4b53-a063-1d02d0834f67` 当前没有走私有 HTTP ASR 云部署；现役 ASR 回到默认 DashScope realtime ASR 路径

### 2026-06-05

- 已定位并修复沟通页录音 / 录音结束后出现大面积空白的问题：
  - 最终根因不是 LiveKit、ASR、voxember.com 独有部署，也不是单纯的波形大小，而是 [ChatInterface](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 自动滚动使用 `messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' })`
  - 录音、实时转写和 assistant streaming 更新触发滚动时，浏览器可能滚动 document/body，而不是只滚消息面板；结果是 footer/input 区被滚到视口中部，下面露出大块空白
  - 已新增 `messagesScrollRef` 指向消息 `main`，自动滚动改成直接设置消息区 `scrollTop = scrollHeight`，文字流会实时贴底滚动，但不再调用 `scrollIntoView` 或滚动 document/body
  - 沟通页外层已锁成 `h-dvh overflow-hidden`，主列 `min-h-0 flex-1 flex-col`，header/footer `shrink-0`，消息区 `min-h-0 flex-1 overflow-y-auto`，只允许消息区滚动
  - 进入沟通页时会重置 `window.scrollTo({ top: 0, left: 0 })`，避免从首页或旧状态带入 document scroll
  - 录音时消息流保留实时转写文字气泡；已恢复 footer 内收音波形展示，波形只占输入区上方轻量状态条，不再替代文字流或驱动页面滚动
  - 已验证：`cd frontend && npm run build`
  - 本地 Playwright 打开 `http://127.0.0.1:3220/?mode=communicate` 会按现有鉴权跳转登录页，未登录浏览器未做真实录音 UI smoke；代码层已确认 `scrollIntoView / messagesEndRef` 移除

### 2026-05-30

- 已把本地 OSS 导出目录加入 gitignore：
  - [artifacts/oss-by-account-after-20260524](/home/ubuntu/VoxFlame-Agent/artifacts/oss-by-account-after-20260524)
  - 该目录保留本地导出、`_objects.jsonl`、`_inventory.json` 与压缩包，不进入 Git
- 已按本地导出清单精确清理云端 OSS：
  - 新增 [delete_oss_objects_from_manifest.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/delete_oss_objects_from_manifest.ts)，默认 dry-run，只有 `--write` 才删除
  - 删除范围来自 `artifacts/oss-by-account-after-20260524/_objects.jsonl` 的 `objectName`，不按 bucket 通配或目录前缀扩散
  - 目标 bucket / region：`voxflame / oss-cn-shanghai`
  - 本次删除 `1258` 个 OSS 对象，`missing=0`，`failed=0`
  - 删除后用同一个 `--since 2026-05-24T03:24:01.898Z` 条件 dry-run 复查，匹配对象为 `0`
  - 本次只清理 OSS 对象；没有删除本地 artifact、压缩包、Supabase 账号或数据库记录

### 2026-05-26

- 已修复移动端训练录音“不能整理成标准 WAV”的兼容问题：
  - 根因：训练页本地保存录音原先使用 `MediaRecorder` 产出 `audio/webm` / `audio/mp4`，上传前再用 `AudioContext.decodeAudioData` 解码并转成 16k mono WAV；部分移动端内置浏览器 / WebView 会录出自己无法解码的 blob，导致上传前转 WAV 失败
  - 新增 [local-pcm-wav-recorder.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/audio/local-pcm-wav-recorder.ts)，训练页本地录音现在直接从麦克风轨道走 WebAudio PCM 收集并写标准 WAV
  - [useMandarinTrainingSession](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useMandarinTrainingSession.ts) 已从 `MediaRecorder` 本地保存改为 `LocalPcmWavRecorder`，实时 ASR 仍走原 LiveKit 链路
  - 上传前 `normalizeRecordingToWav` 会直接复用 `audio/wav + 16k + mono`，不再触发移动端浏览器解码 mp4/webm
  - 新增 [local-pcm-wav-recorder.test.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/audio/local-pcm-wav-recorder.test.ts)，验证 WAV header 和 normalize 复用路径
  - 已验证：`cd frontend && npm test -- src/lib/audio/local-pcm-wav-recorder.test.ts`、`cd frontend && npm run build`
- 已完成沟通页 confirmed output 本机输出 v0：
  - [ChatInterface](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 新增“确认输出”缓冲区，自动承接最新 assistant final transcript，也允许用户手动改写
  - 同一段确认文本现在可以大字给对方看、面对面反转、本机朗读、复制到剪贴板
  - 展示 / 复制 / 朗读会写入当前 session metadata 的 `latestConfirmedOutput*` 字段，先记录动作，不把文本另写长期记忆
  - 硬件外放本轮先不接伪接口；等真正做硬件时再决定走 BLE、串口、局域网、系统音频路由或厂商 SDK
  - 已验证：`cd frontend && npm run build`
  - 已按用户要求直接拉取 Playwright CLI，不再起本地 dev server；`bash /home/ubuntu/.codex/skills/playwright/scripts/playwright_cli.sh --help` 正常输出
  - 浏览器交互 smoke 待接入现成运行地址或用户本机已有服务后再跑
- 已完成 P0：训练总结退出长期记忆和沟通默认上下文
  - 后端 workspace snapshot 不再暴露 `training_summaries` object zone 和 `training_summary` loadout section
  - `preparation` 不再从训练报告注入 `immediate_goal / support_strategies / risky_terms / pronunciation_patterns / training_pairs`
  - `session_review` 不再用训练复盘兜底，只保留最近非训练沟通会话复盘
  - 记忆页已移除训练总结区；沟通页文案和上下文摘要也不再提示“训练总结默认进入”
  - 训练报告仍保留在训练页、dataset review 和未来专家复核材料里
  - 已验证：`cd backend && npm run build`、`cd frontend && npm run build`、`bash scripts/check_ai_docs.sh`
- 已按用户新的产品判断，重写两份当前主文档：
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md)
  - [VOXFLAME_REHAB_THERAPY_PRODUCT_MAPPING_BY_ETIOLOGY_2026-05-15.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_REHAB_THERAPY_PRODUCT_MAPPING_BY_ETIOLOGY_2026-05-15.md)
- 这次不是继续追加研究，而是删除两份文档里已经完成或不再作为未来计划的部分，改成“当前代码事实 + 下一步执行计划”：
  - 沟通页已有 LiveKit 主链、表达工具箱、starter kit、workspace loadout、字幕辅助和麦克风输入反馈
  - 训练页已拆成 `/contribute` 主题选择与 `/contribute/topic/[topicId]` 录音训练，支持评估筛查、自定义材料切句、训练上传 metadata、病因标签和质量指标
  - 记忆页已有用户画像、场景模板、多份自定义材料库和 active material
  - Mobile Workbench skeleton 已有四 surface、登录态、workspace snapshot 读取、native recorder queue 和 RTC intent 雏形
- 新的产品判断已经收口：
  - 固定句库只是缓兵之计，长期价值来自不同场景下高效沟通、稳定住表达自主权、可感知进步
  - 沟通页是主产品，也是后续连接硬件的页面
  - 第一句话 / 破冰材料库不应继续当人工句库扩展，而应升级为沟通转写 agent 的第一轮协议：让对方知道怎么听、保护用户表达权、建立补救规则，并把场景目标送入 agent
  - 训练总结暂时移出长期记忆和沟通默认上下文，只留在训练页、dataset review 和未来专家复核材料里
  - 训练页短期继续服务稳定采样和低风险练习；正式疗法必须有专家准入、专家复核和清晰医学边界
- 当前未完成代码事实也已写进文档：
  - 沟通页已补 confirmed output 本机输出 v0：大字展示、面对面反转、本机朗读、复制
  - 语音转文本、给对方看、文本发声都不应拆独立 agent；当前缺口是硬件外放接口选型、第三方接入状态流和保存为短语闭环
  - 沟通页还没有真正的 listener-facing 出口
- 下一步执行顺序：
  - P1：继续完善沟通页 confirmed output 呈现层，把保存为短语、第三方接入状态流和后续硬件接口选型补齐
  - P2：增强沟通页内高频输出出口；即使后续增加快捷入口，底层仍进入沟通页 / 沟通转写 agent
  - P3：训练页专家协作 protocol v0

### 2026-05-25

- 已把 ICP 备案信息挂到 Web 首页底部：
  - 备案 / 许可证编号：`沪ICP备2026020229号`
  - 审核通过日期：`2026-05-14`
  - 首页底部新增备案展示，链接到工信部备案首页 `https://beian.miit.gov.cn/`
  - 备案主体默认展示 `上海生声不息科技有限公司`
  - `docker-compose.yml`、`frontend/Dockerfile`、`frontend/next.config.js` 和 `.env.example` 已补齐 `NEXT_PUBLIC_ICP_BEIAN_*` 构建期配置
  - `.env` 的正式入口已切到 `https://voxember.com`，frontend metadata 默认域名已从旧 `ranyan.app` 切到 `voxember.com`
  - 新增 [set_dnspod_voxember_record.cjs](/home/ubuntu/VoxFlame-Agent/scripts/ops/set_dnspod_voxember_record.cjs)，读取本机 `.env.dnspod` 中的腾讯云 CAM 子用户密钥，创建 / 更新 DNSPod `voxember.com @ A -> 111.230.35.89`
  - 已通过 DNSPod 创建根域名 A 记录，记录 id 为 `2299420650`
  - 已重启 `caddy` HTTPS profile，Caddy 日志确认 Let's Encrypt `http-01` 验证通过并成功获取 `voxember.com` 证书
  - 已验证 `https://voxember.com` 与 `https://voxember.com/api/rtc/health` 均返回 `HTTP/2 200`
  - Headless Chrome hydration 后确认首页底部展示 `上海生声不息科技有限公司`、`沪ICP备2026020229号` 和工信部链接

### 2026-05-24

- 已下载并整理 `2307294809@qq.com` 与 `3083029019@qq.com` 的训练语料：
  - Supabase 复查：`230729489@qq.com` 不存在；按真实账号 `2307294809@qq.com` 合并处理
  - `2307294809@qq.com` userId：`64758dee-5026-4b53-a063-1d02d0834f67`
  - `3083029019@qq.com` userId：`3368b1cb-8014-4502-8b4d-6011c17371ce`
  - 已刷新 [artifacts/oss-by-account](/home/ubuntu/VoxFlame-Agent/artifacts/oss-by-account)：当前远端清单里 `2307294809__64758dee` 有 `237` 个对象，`3083029019__3368b1cb` 有 `80` 个对象
  - 新增 [prepare_training_corpus_artifact.py](/home/ubuntu/VoxFlame-Agent/scripts/audio/prepare_training_corpus_artifact.py)，从当前 `_objects.jsonl` 取指定账号，跳过 `manifest.jsonl`，输出 raw 与 trimmed 两套语料
  - 已输出 [artifacts/training-corpus-20260524](/home/ubuntu/VoxFlame-Agent/artifacts/training-corpus-20260524)：raw 有 `313` 条音频 + `2` 个 `transcripts.txt`，trimmed 有 `313` 条 WAV，目录内没有 `manifest.jsonl`
  - 静默处理策略：检测 `>500ms` 静默段，裁剪长静默并保留约 `120ms` 缓冲；本轮共处理 `320` 段长静默，累计裁掉约 `293.24s`
  - 已额外生成 [2307294809-all-merged](/home/ubuntu/VoxFlame-Agent/artifacts/training-corpus-20260524/2307294809-all-merged)：本地缓存 WAV 共 `519` 条，其中 `235` 条有当前 manifest/transcripts/DB 可验证 target，`284` 条是 2026-04-29 历史缓存残留，当前 OSS 远端清单、`dataset/<userId>/transcripts.txt` 和 `voice_contributions` 都没有对应 target
  - 可训练强对应包为 [2307294809-target-audio-verified](/home/ubuntu/VoxFlame-Agent/artifacts/training-corpus-20260524/2307294809-target-audio-verified)：`235` 条 raw WAV、`235` 条 trimmed WAV、`metadata.jsonl` `235` 行、`errors.json` 为空
  - 已验证用户查询、OSS 下载、合并裁剪脚本、target/audio 对齐、远端 prefix dry-run、数据库 review 导出和 `py_compile`

### 2026-05-22

- 已把原 `发音与朗读` 拆成 `现代文章朗读` 和 `文言文节奏`，并统一清洗为简体中文：
  - `scripts/corpus/export_frontend_source_corpus.py` 现在优先使用 OpenCC `t2s` 做繁转简；没有 `opencc-python-reimplemented` 时才用脚本内置兜底表
  - `现代文章朗读` 作为默认朗读入口，优先使用普通话水平测试现代白话朗读作品；`文言文节奏` 单独保留经典文言和声律材料作为进阶入口
  - 已重新生成 [mandarin-training-real.json](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/generated/mandarin-training-real.json)，当前源抽取语料总数 `1419` 条，其中 `现代文章朗读` `560` 条、`文言文节奏` `240` 条
  - 典型截图残留已变为简体：`咨臣以当世之事`、`遂许先帝以驱驰`、`愿陛下亲之信之`、`三顾臣于草庐之中`、`阿姊闻妹来当户理红妆`
  - 前端 corpus 测试新增繁体 / 旧字形拦截，并拦截现代文章池里的网页 / 培训站噪声；本机已安装 `opencc-python-reimplemented`
  - 已验证：
    - `cd frontend && npm test -- src/lib/corpus/mandarin-training-data/index.test.ts`
    - `cd frontend && npx tsc --noEmit --allowImportingTsExtensions`
    - `bash scripts/check_ai_docs.sh`
- 已给现有实用分类补充专业精选语料：
  - [curated-topics.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/curated-topics.ts) 已新增重点旅客 / 无障碍出行、急救与康复就医、照护 / 窗口 / 课堂角色、实时语音 / 字幕 / 辅助功能设备操作等短句
  - 当前 curated 计数：`日常与出行 104 / 看病与求助 85 / 人群与角色 107 / 设备与数字 178`
  - 原则：高风险 / 高价值场景不直接把网页正文灌进前端，先按官方 / 高可信来源的业务语义抽象成短、清楚、可录音的目标句
  - 已验证：
    - `cd frontend && npm test -- src/lib/corpus/mandarin-training-data/index.test.ts`
    - `cd frontend && npx tsc --noEmit --allowImportingTsExtensions`
    - `bash scripts/check_ai_docs.sh`

### 2026-05-21

- 已继续收紧训练页 transcript 绑定，针对“前两句对、后面又错配”的残余竞态：
  - 训练录音最终结果只按 `client_capture_id` 取自己的 final transcript，不再让下一轮录音吃到上一轮的 interim / bestObserved
  - `livekit_agent` 对没有稳定语音的 `manual_stop` 不再把 capture 放进 final transcript 队列，避免后续 final transcript 整体后移一格
  - 已验证：
    - `cd frontend && npx tsc --noEmit --allowImportingTsExtensions`
    - `cd frontend && npm test -- src/lib/training/final-transcript.test.ts src/lib/realtime-audio/session-runtime.test.ts`
    - `python3 -m unittest livekit_agent.tests.test_asr_runtime`

### 2026-05-20

- 已清空 `2307294809@qq.com` 的上传语料，不删除账号本身：
  - 账号 userId：`64758dee-5026-4b53-a063-1d02d0834f67`
  - 新增后台脚本 [clear_uploaded_training_corpus.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/clear_uploaded_training_corpus.ts)，默认 dry-run，`--write` 才删除
  - 本次删除 `voice_contributions` 训练上传记录 `317` 条，全部为 guided training
  - 同步删除 OSS 上传语料对象 `319` 个：`317` 个音频对象 + `dataset/<userId>/manifest.jsonl` + `dataset/<userId>/transcripts.txt`
  - 清空后复查：数据库剩余 `0`，OSS 剩余 `0`，删除失败 `0`
  - 已验证：
    - `cd backend && npm run clear:uploaded-training-corpus -- --email 2307294809@qq.com`
    - `cd backend && npm run clear:uploaded-training-corpus -- --email 2307294809@qq.com --write`
    - `cd backend && npx tsc --noEmit --skipLibCheck --esModuleInterop --module commonjs --target ES2020 --moduleResolution node scripts/clear_uploaded_training_corpus.ts`

- 已按 2026 LiveKit 官方降噪策略做 P0 降噪调参：
  - 官方方向：Web/self-host 先保留 WebRTC `noiseSuppression / echoCancellation`；更强方案是 LiveKit Krisp / ai-coustics，但这属于 P1 依赖与计费/授权层，不在本次 P0 引入
  - `livekit_agent` 默认 VAD 从 `0.018` 提高到 `0.032`
  - silence finalize 窗口从 `720ms` 提高到 `860ms`
  - barge-in 最短语音从 `220ms` 提高到 `360ms`
  - 新增 `QWEN_ASR_MIN_COMMIT_SPEECH_MS=420`，低于该时长的 manual stop 不再提交 ASR buffer
  - 新增 filler transcript 过滤：普通沟通模式会过滤短促 `嗯 / 呃 / 啊 / 哦 / 喔 / 额 / 唔 / 哼`，但训练短词 / 筛查模式保留短 utterance，避免误伤训练
- 已验证：
  - `python3 -m unittest discover livekit_agent/tests`
  - `python3 -m py_compile livekit_agent/config.py livekit_agent/asr_runtime.py livekit_agent/data_contract.py livekit_agent/app.py`

- 已定位并修复 Web 训练页“系统听到总显示目标句上一句”的竞态：
  - 根因是上一条 ASR final transcript 可能晚到，被下一条训练录音的 `waitForFinalTranscript` 当成本条结果消费
  - 前端现在为每次训练录音生成 `client_capture_id`，并只接受同一 capture 的 final transcript
  - `livekit_agent` 会把 `client_capture_id` 从 `speech_activity` 透传到 user transcript payload
  - 训练页在没有可信 final transcript 时不再用旧 interim/bestObserved 兜底保存结果，避免污染 `recognized_text / raw_transcript / feedback`
- 同时查明截图里的 `Error committing input audio buffer...` 不是撤回失败：
  - backend Docker 日志显示撤回接口已成功写出 `[Upload] Discarded ...`
  - 红错来自 `livekit_agent` 对无稳定语音的 manual stop 仍提交 ASR audio buffer
  - `livekit_agent` 现在会跳过无稳定语音的 manual commit，避免把这类情况冒成红色系统错误
- 已验证：
  - `cd frontend && npx tsc --noEmit --allowImportingTsExtensions`
  - `cd frontend && npm test -- src/lib/realtime-audio/session-actions.test.ts src/lib/realtime-audio/session-runtime.test.ts src/lib/training/final-transcript.test.ts`
  - `python3 -m unittest livekit_agent.tests.test_data_contract livekit_agent.tests.test_asr_runtime`

### 2026-05-16

- 已新增 [restsend Rust 通信栈与硬件音频桥研究（2026-05-16）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_RESTSEND_RUST_STACK_AND_HARDWARE_AUDIO_BRIDGE_RESEARCH_2026-05-16.md)
  - 基于 Context7 和 GitHub 公开资料分析 `rustpbx / rsipstack / rustrtc / audio-codec`
  - 核心判断：restsend 作者更适合作为 VoxFlame 未来 `SIP / PBX / RTP / WebRTC / audio codec` 通信网关层合作者，而不是第一版 ESP32-S3 固件外包
  - `rustpbx + audio-codec` 适合后续电话 / 医院分机 / 远程随访 / SIP trunk / 通话录音 / CDR / TTS 注入；`rsipstack / rustrtc` 更适合边缘网关或协议实验，不应替代当前 LiveKit 主链
  - 硬件形态收口为 `耳挂式近口麦克风 + 挂脖 / 胸前扬声器盒 + 手机 App brain`；ESP32-S3 只做低成本音频桥和交互外设，不承担 LiveKit/WebRTC/SIP/LLM/ASR/TTS 主链
  - 已同步 [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md) 和 [.tasks/current.md](/home/ubuntu/VoxFlame-Agent/.tasks/current.md)

### 2026-05-15

- 已新增 [Voiceitt 功能设置深度分析与 VoxFlame 启发（2026-05-15）](/home/ubuntu/VoxFlame-Agent/docs/VOICEITT_FEATURE_SETTINGS_ANALYSIS_AND_VOXFLAME_INSPIRATION_2026-05-15.md)
  - 文档拆解 Voiceitt 的 `Record / Speak / Dictate / Integrations`，以及 voice output、silence timeout、playback speed、preferred microphone、record validation、profanity、flip text、highlight words、streaming、shortcut phrases、personal vocabulary、voice commands、notes/history、account deletion 等设置项
  - 核心判断：Voiceitt 是 personalized speech access layer，不是单个 ASR 页面；VoxFlame 应吸收“设置影响真实链路、三种输出面分开、个人语音 profile 是中心资产、shortcut phrases 和 listener-facing UI 高价值”的原则
  - 已明确不应照搬：模式命名、过早做 Chrome/会议插件、把 voice commands 当默认交互、把训练 level 做成纯数量 gamification
  - 推荐路线：P1 做停顿时间设置、给对方看模式、confirmed text buffer、prepared expression 发声；P2 做 personal vocabulary / shortcut phrases / risky terms；P3 再考虑外部 integrations
  - 已同步 [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)

- 已新增 [构音障碍病因差异参考（2026-05-15）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_DYSARTHRIA_ETIOLOGY_DIFFERENCE_REFERENCE_2026-05-15.md)
  - 覆盖 CP、TBI、Down Syndrome、Stroke / nerve injury、MS、ALS / MND、ASD、Parkinson、Deafness / severe hearing loss
  - 核心判断：`diagnosis label` 只是线索，不能直接决定训练策略；真正要进入产品事实源的是 observable speech profile，包括 dysarthria 类型、apraxia / aphasia 可能性、听觉反馈、韵律、疲劳和进展性
  - 文档已整理不同病因的本质差异、VoxFlame 产品启发、profile schema 建议和 ASHA / Mayo / NINDS / MS Society / Parkinson Foundation / ALS Association / Down Syndrome Education / NIDCD 等参考资料
  - 已同步 [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)

- 已新增 [分病因言语康复与沟通产品化深度文档（2026-05-15）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_REHAB_THERAPY_PRODUCT_MAPPING_BY_ETIOLOGY_2026-05-15.md)
  - 以 ReTalk / 复言中风言语康复样板为参照，沉淀 `专家评估 -> 软件高频训练 -> AI 分析 -> 专家复核 -> 专家知识自动化` 工作流
  - 文档逐个覆盖中风 / 神经损伤、脑外伤、脑瘫、唐氏综合征、多发性硬化、肌萎缩侧索硬化 / 运动神经元病、帕金森病、孤独症谱系障碍、听力损失
  - 每个病因都拆出核心机制、成熟疗法 / 医院常用原则、VoxFlame 沟通功能、练习功能、AI 接轨方式和专家边界
  - 核心判断：VoxFlame 不应成为自动治疗师，而应成为专家知识自动化执行面；任务库应按机制和目标组织，不按病名硬编码
  - 已同步 [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)

- 已把 Voiceitt 前两个设置方向收成 VoxFlame 当前真正可用的音频设置链路：
  - 新增 [Web 音频设置页](/home/ubuntu/VoxFlame-Agent/frontend/src/app/settings/audio/page.tsx)，从用户头像菜单进入 `/settings/audio`
  - 设置页只做真实有用的麦克风能力：授权、列出输入设备、保存首选麦克风、现场收音电平测试
  - Web 沟通页和训练页共用 [microphone-preferences](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/audio/microphone-preferences.ts)；LiveKit 发布麦克风时会优先使用设置页保存的输入设备
  - Web 训练录音 envelope 新增 `inputDevice` 与 `quality`，上传 metadata 同步写入 `microphone_* / selected_microphone_* / speech_duration_ms / silence_ratio / input_level_* / audio_quality_*`
  - `assessTrainingSampleQuality` 已把 `low_confidence / review` 收音质量纳入样本分级，低质量样本默认保留为 attempt / 回看，不当作高置信样本
  - Mobile Workbench 练习队列已新增最小时长质量分级并写入本地 queue / 上传 metadata；设备 surface 的“检查麦克风”现在会触发真实权限检查
  - 已验证：
    - `cd frontend && npm test -- src/lib/audio/microphone-input-feedback.test.ts src/lib/realtime-audio/session-audio.test.ts`
    - `cd frontend && npx tsc --noEmit --allowImportingTsExtensions`
    - `cd apps/mobile-workbench && npm run check`
    - `cd apps/mobile-workbench && npm run typecheck`
  - 尚未完成：浏览器真实设备手动 smoke、Android / iPhone 真机录音 smoke、移动端真实 RMS/静音比例采集

### 2026-05-12

- 已把训练语料路线从“模板生成 prompts”改成“来源文本抽取”：
  - 已删除 `scripts/corpus/build_controlled_mandarin_training_corpus.py`，不再用模板批量造句
  - 新增 [source-based frontend exporter](/home/ubuntu/VoxFlame-Agent/scripts/corpus/export_frontend_source_corpus.py)，只从抓取/本地 manifest 里的真实文本切分、过滤、去重、导出
  - 新增 [普通话朗读来源清单](/home/ubuntu/VoxFlame-Agent/scripts/corpus/source_inventory_putonghua_reading_2026.json) 与 [开源中文例句来源清单](/home/ubuntu/VoxFlame-Agent/scripts/corpus/source_inventory_open_example_sentences_2026.json)
  - 已重新生成 [mandarin-training-real.json](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/generated/mandarin-training-real.json)：`1378` 条 source-extracted prompts，全部符合 `6-16` 个汉字策略
  - 前端语料入口现在会把 curated topic prompts 与 source-extracted prompts 合并去重；`评估筛查` 仍然使用独立 20 词筛查
  - 当前前端训练页总量 `1801` 条，其中非筛查训练句 `1781` 条；本地 AISHELL-1 / AISHELL-2 / 其他 transcript 后续通过 manifest 接入，大数据集不提交进仓库
  - [corpus README](/home/ubuntu/VoxFlame-Agent/scripts/corpus/README.md) 已写清来源抓取、离线导出和 `target_text -> supervised recording -> upload receipt / manifest` 流程
- 已更新 [训练评估开发文档](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_TRAINING_ASSESSMENT_FUNCTION_DEVELOPMENT_2026-05-08.md)：
  - 根据 CDSD、AISHELL-1、Qwen3-ASR、FunASR 和普通话构音障碍声学研究，当前判断前台 supervised recording 默认目标句应控制在 `6-16` 字；`1-5` 字保留给音系筛查、最小对立和单词复练
  - 推荐录音窗口 `2-8s`，硬上限先按 `12s`
  - 已记录现有录音质检现状：有过短录音、ASR 覆盖率、transcript latency 和麦克风输入电平；尚无 VAD、首尾静音、静音占比和录音过长判定
  - 下一步应给 recording envelope 增加 `speech_duration_ms / leading_silence_ms / trailing_silence_ms / silence_ratio / input_level_rms / input_level_peak`
- 已新增 [corpus index test](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/index.test.ts)，守住 `1000-2000` 条、`6-16` 字和同类去重；测试 runtime hook 也补了 JSON import 支持。
- 已验证：
  - `python3 scripts/corpus/export_frontend_source_corpus.py --phonology-corpus /tmp/voxflame-phonology-corpus-20260512.json --manifest /tmp/voxflame-putonghua-reading-fetch-20260512/_local_manifest.json --manifest /tmp/voxflame-open-example-sentences-fetch-20260512/_local_manifest.json --output frontend/src/lib/corpus/generated/mandarin-training-real.json --per-source-cap 3000 --cap 日常与出行=360 --cap 看病与求助=80 --cap 人群与角色=140 --cap 设备与数字=120 --cap 发音与朗读=760`
  - `cd frontend && npm test -- src/lib/corpus/mandarin-training-data/index.test.ts`
  - `cd frontend && npx tsc --noEmit --allowImportingTsExtensions`
  - `bash scripts/check_ai_docs.sh`

### 2026-05-10

- 已新增 VoxFlame 专家标准与用户反馈闭环计划：
  - 新增 [专家标准与协作手册（2026-05-10）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_EXPERT_STANDARD_AND_COLLABORATION_PLAYBOOK_2026-05-10.md)
  - 结论：后续 App / Web / 硬件 / 训练评估 / 记忆系统必须按 `专家标准 / 技术验证 / 用户反馈` 三角闭环推进，不能只看“技术能跑”
  - 当前最大缺口已写清：用户反馈还没有形成可追踪、可复盘、可反哺 prompt / UI / 语料 / memory / hardware 的闭环
  - 已沉淀 `VoxFlame Expert Standard v0.1`，覆盖证据等级 L0-L5、用户反馈等级 F0-F4、prompt registry、沟通技巧 registry、训练语料、memory schema、专家协作、材料下载与准入门槛
  - 已明确医学 / 沟通专家协作触发条件：临床 / 康复 / 构音障碍评估口径、训练语料、评测维度、长期记忆解释、对外医学表达和硬件安全 / 人因设计必须进入专家审核
  - 用户反馈闭环交付物已定为：`feedback_registry`、创始人自我观察模板、目标用户访谈模板、沟通伙伴反馈模板、每周反馈 triage、ship decision log
  - [App / Mobile Workbench 机会文档](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_APP_COMPANION_BEST_PRACTICES_AND_OPPORTUNITY_2026-05-04.md) 已新增“标准 / 技术 / 用户反馈闭环”章节，并给 `沟通 / 练习 / 记忆与准备 / 设备与同步 / 硬件桥` 定义最小反馈信号
  - [README](/home/ubuntu/VoxFlame-Agent/README.md) 与 [docs/README](/home/ubuntu/VoxFlame-Agent/docs/README.md) 已把该文档接入当前主入口
- 已把硬件桥接路线从“控制桥优先”升级成“发声 + 记录”双主线：
  - 更新 [硬件桥接开发手册（2026-05-05）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_HARDWARE_BRIDGE_DEVELOPMENT_GUIDE_2026-05-05.md)
  - 已补充现有录音 / 发音硬件形态调查：大屏 SGD / AAC 设备、简单录放音 AAC / 可穿戴消息器、随身语音扩音器
  - 已沉淀 `VoxFlame Communication Audio Bridge Standard v0.1`，覆盖形态、重量、麦克风、扬声器、按钮、状态、离线、上传、电池和隐私的 P0/P1/P2 指标
  - 已补充音频标准、人因标准、安全 / 合规 / 认证路线、P0 benchmark 测试清单和参考资料入口
  - 第一版硬件定义为便携音频桥：I2S 麦克风、小喇叭、实体按钮、状态灯、BLE 连接 App；App 仍然是 brain，硬件是 mouth / ear / button / status surface
  - 发声分为三层：ESP32-S3 本地状态提示音、预置短句 / 最近录音回放、App / Web / desktop companion 承接实时 TTS / 翻译器输出
  - 记录分为三类：ESP32-S3 I2S 训练样本记录、App / LiveKit 侧沟通现场记录、设备质量 telemetry
  - 明确 ESP32-S3 不能当普通蓝牙 A2DP 音箱，也不适合第一版扛 LiveKit、ASR、LLM 或实时 TTS；真实翻译器外放优先让 App 播放到手机、蓝牙音箱或桌面扬声器
  - 采购路线升级为 `I2S MEMS 麦克风 + MAX98357A + 小喇叭 + USB-C 领夹麦 + 便携蓝牙音箱`
  - 阶段路线新增 `H2.5 ESP32-S3 本地发声原型`，验收本地提示音、预置短句、最近录音回放和 App BLE command 中断
  - 已验证：`bash scripts/check_ai_docs.sh`

### 2026-05-08

- 已补齐 Mobile Workbench Android EAS 内测安装入口
  - 新增 [apps/mobile-workbench/eas.json](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/eas.json)，包含 `development` 和 `preview` Android APK profile
  - `apps/mobile-workbench` 新增 `expo-dev-client` 依赖，并新增 `npm run build:android:development` 与 `npm run build:android:preview`
  - [.env.example](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/.env.example) 已提醒 Android 真机必须使用电脑局域网 IP，不能用 `127.0.0.1`
  - 文档已补充 Android 安装说明：EAS build 页面会提供 Install 链接或二维码；Android 手机通常用系统相机扫码，再在浏览器下载 APK 并允许浏览器安装未知应用
  - EAS 云端构建不会自动读取本地 `.env`，需要用 `eas env:create` 或 Expo dashboard 配置 `EXPO_PUBLIC_API_BASE_URL / EXPO_PUBLIC_SUPABASE_URL / EXPO_PUBLIC_SUPABASE_ANON_KEY`，且不能放服务端 secret
  - 已验证 mobile check、typecheck 和 docs harness；尚未执行真实 EAS cloud build
  - 后续又补了国内 Android 商店 profile：`npm run build:android:china-store`，用于生成 release APK，区别于 development / preview 内测包
  - 文档已写清：小米等国内 Android 商店上传 APK；华为应用市场 Android 分发可用当前 React Native Android APK；HarmonyOS NEXT 原生鸿蒙应用不是当前 APK 直接覆盖的目标，需要后续单独原生鸿蒙版本或跨端支持确认
- 已新增 [第一功能：训练评估开发文档（2026-05-08）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_TRAINING_ASSESSMENT_FUNCTION_DEVELOPMENT_2026-05-08.md)
  - 当前判断：现有 `20 词筛查` 适合低压力 onboarding、训练页粗筛和“系统听懂分”最小闭环，但不能代表经典中文构音评估语料，也不能替代 Frenchay 汉语版或中国康复研究中心构音障碍检查
  - 第一功能产品定义收口为 `训练表现分 / 系统听懂分 / 趋势报告`，服务患者持续训练和第二功能翻译器理解能力提升；不做独立自主临床评估系统，不把医生减负作为第一阶段核心卖点
  - 文档已从资料汇总重写成开发判尺：North Star 是“训练后，用户在真实沟通中被正确理解的概率提高”，不是平均分更高
  - 已定义 100 分开发者评分标准与一票否决项，覆盖语料、评分、反馈、趋势、沟通反哺、安全边界和工程验证
  - 已补 Stage 0-6 分阶段目标：边界修正、普通话音系核心语料、可复现评分、反馈质量、反哺沟通翻译器、声学趋势、治疗师参考报告
  - 模型职责边界已写清：`qwen-flash` 可用于 ASR、实时沟通、轻量反馈和结构化指标解释；不能直接做临床分型、呼吸/发声/共鸣/韵律评分或疗效判定
  - 语料路线：保留现有 20 词为 `screening_v0_daily_onboarding`，后续新增 `phonology_core` 60-80 词、功能短句、朗读和持续发声任务，并给每条 prompt 标注 pinyin、声母、韵母、声调和语义可预测性
  - 数据路线：继续复用现有 `training_result` metadata，不新增第二套训练事实源；在其上扩展 `assessment_mode / assessment_prompt_version / initials / finals / tones / system_intelligibility_score / score_confidence / acoustic_feature_version` 等字段
  - 已同步入口：[docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)

### 2026-05-05

- 已补齐 Mobile Workbench 真机验证梯度与环境预检：
  - 新增 [Mobile Workbench 真机验证手册（2026-05-05）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_MOBILE_WORKBENCH_DEVICE_VERIFICATION_RUNBOOK_2026-05-05.md)
  - 当前判断：早期 App 验证不需要先上架应用商店；现在应先做真机 development build 和业务 smoke
  - 验证梯度：代码级检查 -> 真机 development build -> 登录 / workspace read / 录音 / 回放 / 上传 receipt / LiveKit quick talk smoke -> 小范围内测分发 -> 正式商店上架
  - 新增 [smoke-device-env.mjs](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/scripts/smoke-device-env.mjs)，并在 `apps/mobile-workbench` 挂载 `npm run smoke:device-env`
  - 预检脚本会检查 mobile public env、提醒真机不能直接访问电脑 `127.0.0.1`，并防止服务端 secret 进入 App 环境
  - 已同步入口：[apps/mobile-workbench/README.md](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/README.md)、[Mobile Workbench Phase 0 RFC](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_MOBILE_WORKBENCH_PHASE0_RFC_2026-05-04.md)、[docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)
  - 已验证 mobile check、typecheck、device-env smoke（占位 public env）、Android Metro bundle export 与 docs harness
  - 下一步需要真机 development build；正式 App Store / Google Play 上架不是当前 Phase 0 前置条件
- 已推进 Mobile Workbench LiveKit React Native 最小 room 连接切片：
  - 新增 [use-livekit-room-connection.ts](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/src/realtime/use-livekit-room-connection.ts)
  - [index.ts](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/index.ts) 已调用 `registerGlobals()`
  - 沟通 surface 现在在 backend `/api/rtc/session/start` 返回 session 后，可以启动 `AudioSession`、连接 LiveKit room、发布麦克风音频，并支持断开清理
  - UI 显示 room connection status 与麦克风发布状态；仍不渲染 participant token
  - 已验证 mobile check、typecheck、Android Metro bundle export
  - 尚未完成真机 LiveKit room smoke、中断 / 断网 / 切后台 UI
- 已推进 Mobile Workbench communication 的 backend RTC session orchestration 切片：
  - 新增 [use-mobile-rtc-session.ts](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/src/realtime/use-mobile-rtc-session.ts)
  - 沟通 surface 现在可以登录后调用 backend `/api/rtc/session/start` 请求 `quick_talk` session
  - App 只展示 room/readiness/blockers/warnings，不渲染 participant token
  - 当前已完成 backend-orchestrated token/readiness 半段；尚未启动 LiveKit React Native `AudioSession` 或进入真实 room
  - 已验证 mobile check、typecheck、Android Metro bundle export
- 已推进 Mobile Workbench native recorder queue 的上传回执切片：
  - 新增 [mobile-upload-client.ts](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench/src/api/mobile-upload-client.ts)
  - `apps/mobile-workbench` 练习 surface 现在可以把本地 queue item 走现有 backend `/api/upload/sign`、OSS signed URL PUT、`/api/upload/complete`
  - 上传成功后会把 `uploadReceipt` 写回本地 queue item，并将状态改为 `uploaded`
  - 上传失败会保留本地文件，记录 `lastError`，并将状态改为 `failed`，后续可重试或丢弃
  - App 练习 surface 已把“待补传”按钮收成 `上传 / 上传中 / 已上传`
  - 已验证 mobile check、typecheck、Android Metro bundle export
  - 尚未完成 Android / iPhone 真机录音 smoke、真实设备上传 receipt smoke、断网队列 UI smoke
- 已更新 Founder Collaboration Loop，明确继续 App 开发的下一刀：
  - 更新 [FOUNDER_COLLABORATION_LOOP_2026-03-25.md](/home/ubuntu/VoxFlame-Agent/docs/FOUNDER_COLLABORATION_LOOP_2026-03-25.md)
  - 当前 App 现状判断：`apps/mobile-workbench` 已完成 Expo skeleton、四 surface、Supabase mobile auth、workspace snapshot read、native recorder queue 本地闭环
  - 尚未完成真机录音 smoke、断网队列 UI smoke、upload receipt / retry 去重、Web/App active prepared expression 人工确认、LiveKit quick talk
  - 下一段顺序：真机录音 smoke -> upload sign/complete -> retry 去重和 upload receipt UI -> backend-orchestrated LiveKit quick talk -> BLE / USB / 外接麦事件
  - 创始人需要把控：录音显式性、本地未上传录音保存/删除、医疗表述边界、硬件事件必须先进 App 再映射动作
- 已新增初版硬件桥接开发手册：
  - [硬件桥接开发手册（2026-05-05）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_HARDWARE_BRIDGE_DEVELOPMENT_GUIDE_2026-05-05.md)
  - 结论：第一阶段不让 ESP32 直接承接 LiveKit 实时语音；ESP32-S3 先做 `BLE 控制桥`，事件进入 Mobile Workbench 后再映射到 recorder queue / LiveKit session
  - I2S 麦克风录音上传作为 P1 原型，先服务训练样本，不服务实时沟通
  - 文档已包含购买清单、官方文档入口、ESP-IDF 开发步骤、BLE GATT 协议、App 接入、LiveKit 边界、上传链路、安全隐私与验收清单
  - 已同步入口：[docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)、[README.md](/home/ubuntu/VoxFlame-Agent/README.md)
- 已清点当前应用账户注册与 OSS 分账户上传情况：
  - Supabase Auth 当前共 `22` 个注册账户，全部有邮箱、无手机号
  - 有 OSS 对象的注册账户为 `5` 个：`1137205964@qq.com`、`13818790456@139.com`、`2307294809@qq.com`、`874888410@qq.com`、`ltf.edgar@foxmail.com`
  - 注册但未匹配到 OSS 对象的账户为 `17` 个，其中包括 `voxflame.e2e.*@example.com` 和 `test@voxflame.com` 这类明显测试账户
  - 最新登录账户是 `2307294809@qq.com`，最后登录时间 `2026-05-04T09:59:55.95749Z`
- 已把 OSS bucket 当前全量对象按账户重新同步到本地：
  - 输出目录：[artifacts/oss-by-account](/home/ubuntu/VoxFlame-Agent/artifacts/oss-by-account)
  - `_inventory.json` 生成时间：`2026-05-05T04:33:51.061Z`
  - OSS 对象数：`469`
  - 下载体积：约 `64.3 MB`
  - 账户分组：
    - `1137205964__8a533bbe`: `120` 个对象
    - `13818790456__d01b4410`: `29` 个对象
    - `2307294809__64758dee`: `286` 个对象
    - `874888410__800f7d03`: `5` 个对象
    - `legacy__v_gv7fxwrp`: `7` 个对象
    - `ltf.edgar__53649c22`: `6` 个对象
    - `unassigned`: `16` 个对象
  - 相比 2026-04-29 旧清单，OSS 从 `463` 增至 `469`，新增 `6` 个对象都归到 `1137205964__8a533bbe`
  - 本轮只做只读账户查询和 OSS 本地同步；未删除账户、未删除 OSS 对象、未改数据库

### 2026-05-04

- 当前已确认：可以开始 `App / Mobile Workbench` Phase 0，路线从“薄 companion”升级为完整移动端工作台；代码量不是最大约束，关键是产品 IA、owner、contract、隐私承诺和移动端技术路线。
- 新增并更新 [VoxFlame App / Mobile Workbench Best Practices And Opportunity（2026-05-04）](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_APP_COMPANION_BEST_PRACTICES_AND_OPPORTUNITY_2026-05-04.md)
  - 推荐新建 `apps/mobile-workbench`，复用 `workspace snapshot / recording envelope / upload receipt / RTC session orchestration`
  - 默认技术路线：`Expo / React Native + LiveKit React Native`
  - 从 day one 规划 `沟通 / 练习 / 记忆与准备 / 设备与同步` 四个一级 surface
  - `Capacitor` 只作为 WebView 原型或过渡方案，不作为完整移动端工作台主线
  - 官方约束已纳入：Capacitor sync/native plugin、Expo audio/background task、Supabase React Native auth storage、LiveKit React Native audio session、iOS/Android 麦克风与后台限制
  - 已列出创始人需要把控的方向：完整移动端工作台承诺边界、后台录音与隐私、医疗/康复表述、硬件桥接优先级、移动端技术栈学习顺序
- [FOUNDER_COLLABORATION_LOOP_2026-03-25.md](/home/ubuntu/VoxFlame-Agent/docs/FOUNDER_COLLABORATION_LOOP_2026-03-25.md) 已新增 App / Mobile Workbench 学习入口和“技术把控与学习点持续更新约定”：
  - 后续如果用户说某个技术栈不熟，或某技术方向需要他把控，默认把 `你需要把控什么 / 为什么现在要懂 / 风险 / 推荐资料 / VoxFlame 默认判断` 写进合适文档
- App / Mobile Workbench 新文档已接入：
  - [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md)
  - [VOXFLAME_OPEN_SOURCE_COLLABORATION_DIRECTION_2026-04-21.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_OPEN_SOURCE_COLLABORATION_DIRECTION_2026-04-21.md)
- 已开始 `apps/mobile-workbench` Phase 0 skeleton：
  - 新增 [Mobile Workbench Phase 0 RFC](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_MOBILE_WORKBENCH_PHASE0_RFC_2026-05-04.md)
  - 新增 [apps/mobile-workbench](/home/ubuntu/VoxFlame-Agent/apps/mobile-workbench)，包含 Expo / React Native app config、四 surface shell、RTC intent、recording envelope、workspace read model、recorder queue policy
  - 现役 RTC / recording contract 已把移动端 surface/source 从旧口径收口为 `mobile_workbench`
  - 新增根命令 `npm run check:mobile-workbench`
  - 依赖版本按 npm registry / Expo SDK 55 模板查询过；mobile lockfile 已生成，完整安装重试后成功，`npm run typecheck` 已通过
  - Expo dev server 已用 `/tmp/voxflame-expo-home` 启动在 `http://localhost:8123`，`/status` 返回 `packager-status:running`
  - Docker 核心栈已按要求重新 build / up；`backend` 与 `frontend` 当前 compose health 均为 healthy，`/health` 与 `/api/rtc/health` 均返回 OK
  - 首次完整安装曾因 npm registry 下载 `@livekit/components-core` 出现 `ECONNRESET`；当前还有 LiveKit 依赖链里的 React peer warning，后续依赖治理要继续关注
- Mobile Workbench Step 2 / Step 3 已推进：
  - `apps/mobile-workbench` 已新增 Supabase React Native auth adapter，session storage 走官方推荐的 `AsyncStorage`，`expo-secure-store` 只保存 last email 这类小型提示
  - `use-mobile-auth` 提供 `signInWithPassword / signOut / MobileAuthTokenProvider`
  - `use-mobile-workspace` 登录后读取 `GET /api/memory/workspace/:userId`
  - App 壳子现在有登录/退出、workspace 同步状态、prepared expression、quick phrases 和 daily target 的只读展示
  - 已移除未配置依赖的 `web` script，native smoke 改走 `npm run export:android`
  - 已验证 `npm run check:mobile-workbench`、`cd apps/mobile-workbench && npm run check`、`cd apps/mobile-workbench && npm run typecheck`、Android Metro bundle export 和 Expo `/status`
  - 真实账号 backend smoke 已通过：取消 `NODE_TLS_REJECT_UNAUTHORIZED` 后，Supabase Auth 登录成功，backend workspace snapshot 返回 `200`、`hasPreparedExpression=true`、`dailyTarget=20`
  - workspace 无 token 返回 `401`，`/health` 与 `/api/rtc/health` 匿名返回 `200`
  - 架构判断：App 不依赖 Web / Next.js runtime；Web 与 App 是两个 sibling client，共同依赖 backend-owned contracts
  - 尚未完成真实账号真机 UI 登录 smoke 与 Native recorder queue
- Mobile Workbench Step 4 Native recorder queue 已推进：
  - 按 Expo 官方 `expo-audio` 路线接入 `useAudioRecorder / useAudioRecorderState / AudioModule.requestRecordingPermissionsAsync / setAudioModeAsync`
  - 按 Expo `expo-file-system` document storage 路线新增本地持久队列与音频目录：`Paths.document/voxflame-recorder-queue`
  - 新增 `native-recorder-storage.ts` 和 `use-native-recorder-queue.ts`
  - `App.tsx` 练习 surface 已有本次练习句、权限、录音 / 停止保存、回放、待补传、丢弃和队列统计
  - 已验证 mobile check、typecheck、Android Metro bundle export
  - 尚未完成 Android / iPhone 真机录音 smoke、断网队列 UI smoke 和 upload receipt / retry 去重接入
- 这轮也把 Web/PWA 稳定性实测里发现的 3 个不符合点直接修了：
  - `/api/rtc/health` 改为无认证最小健康信号；`graphs/session/start/ping/stop` 仍然需要认证
  - PWA manifest 引用的 `frontend/public/screenshots/home-wide.png` 从 12-byte 坏占位换成有效 `1280x720` PNG
  - 记忆页训练总结 fallback 修复：材料库 asset 的空 `training_reports` 不再遮住 workspace snapshot 中实际存在的 80 条全训练样本总结

### 2026-04-29

- 已新增一份面向“从需求到应用架构”的学习与实践文档：
  - [VOXFLAME_FULLSTACK_ARCHITECTURE_LEARNING_GUIDE_2026-04-29.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_FULLSTACK_ARCHITECTURE_LEARNING_GUIDE_2026-04-29.md)
  - 重点沉淀 `真实场景 -> surface -> 状态生命周期 -> owner -> contract -> flow -> failure -> verification` 的拆解方法
  - 文档内补了 React / Next.js / Express / Supabase / LiveKit / MDN / Stripe / 12-Factor / Sam Newman / Martin Fowler 等学习链接
  - 已把入口同步到 [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)、[FOUNDER_COLLABORATION_LOOP_2026-03-25.md](/home/ubuntu/VoxFlame-Agent/docs/FOUNDER_COLLABORATION_LOOP_2026-03-25.md) 和 [backend/README.md](/home/ubuntu/VoxFlame-Agent/backend/README.md)
- 已把 OSS bucket 当前全量对象按账户下载到本地；2026-05-05 已刷新：
  - 输出目录：[artifacts/oss-by-account](/home/ubuntu/VoxFlame-Agent/artifacts/oss-by-account)
  - OSS 对象数：`469`
  - 下载体积：约 `64.3 MB`
  - 账户分组：
    - `1137205964__8a533bbe`: `120` 个对象
    - `13818790456__d01b4410`: `29` 个对象
    - `2307294809__64758dee`: `286` 个对象
    - `874888410__800f7d03`: `5` 个对象
    - `legacy__v_gv7fxwrp`: `7` 个对象
    - `ltf.edgar__53649c22`: `6` 个对象
    - `unassigned`: `16` 个对象
  - 输出内含 `_inventory.json` 和 `_objects.jsonl`，可核对对象名、账户分组、大小和本地路径
  - 新增 backend 下载脚本 [download_oss_by_account.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/download_oss_by_account.ts)，并在 backend `package.json` 中挂为 `npm run download:oss-by-account`
  - [.gitignore](/home/ubuntu/VoxFlame-Agent/.gitignore) 已忽略 `artifacts/oss-by-account/`

### 2026-04-28

- 训练页计划位已改成固定练习目标与匿名昨日榜：
  - `prepared-expression-summary.service.ts` 现在只调用模型生成 `daily_summary / weekly_summary`，不再生成 `training_plan`
  - `supabase.service.ts` 的 workspace snapshot 新增 `training_activity`，固定返回 `每天先练 20 句` 和昨日 Top 3 匿名训练量
  - 昨日榜只暴露 `rank / recording_count`，不暴露邮箱、用户名或 user id
  - 训练页和记忆页已改成展示固定 20 句目标、匿名昨日训练榜、今日总结和 7 天总结
  - 训练总结取样按最近 7 天窗口分页读取 `voice_contributions`，不再只截最近 80 条
  - 训练录音页已关闭 agent TTS 回声，避免最终 ASR / 浏览器 autoplay 导致的时有时无；结果卡片压缩成目标、系统听到、回听、重录和保存状态的最小视图
  - 本次结果新增“不收录”按钮，默认仍自动上传；用户不满意时可撤回本地队列 / 云端登记 / OSS 训练索引
  - ASR 链路改成先 VAD 再送模型，并过滤重复字噪声 transcript，避免“我我我…”进入训练结果
  - 已验证：
    - `cd backend && npm run build`
    - `cd frontend && npm run build`
    - `python3 -m unittest livekit_agent.tests.test_asr_runtime livekit_agent.tests.test_session_userdata`
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types src/lib/training/final-transcript.test.ts`
    - `bash scripts/check_ai_docs.sh`
- [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 已新增“练习工作台核心场景：脑卒中后持续说话练习”：
  - 每日固定小目标仍是 `每天先练 20 句`
  - 后续每日 / 7 天总结要支持同句或同类句子的进步评估
  - 今日总结 / 7 天总结要输出简练规律性内容，具体字词错配放在结构化例子字段，不让总结只盯一两个字
  - 输出只能表达“训练表现 / 系统识别代理指标改善”，不能表达医学康复结论
  - 后续实现顺序：先做 backend progress feature builder，再改 daily / weekly summary prompt
  - 已验证：`cd backend && npm run build`、`bash scripts/check_ai_docs.sh`

### 2026-04-27

- 客户反馈训练录音页需要“录完后能播放自己的声音”，以及 iPhone Safari 上看不到麦克风入口；这轮已先做低风险前端收口：
  - [contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现在在手机窄屏把录音面板排在词表前面，桌面端继续保持左边句子准备、右边录音结果
  - 录完后结果区新增“回听自己的声音”，直接用当前录音 blob 生成临时 object URL，并通过浏览器原生 `<audio controls>` 播放
  - 已确认 iPhone 客户截图使用 `http://111.230.35.89` 且处于 Safari 无痕浏览；这不是稳定的移动端麦克风验证环境，正式测试应走 HTTPS 域名或 localhost secure context
  - 已验证：
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types src/lib/training/training-assessment.test.ts src/lib/training/final-transcript.test.ts`
    - `cd frontend && npm run build`

### 2026-04-23

- [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 这轮已从“上线收口版”改成“上线后规划版”：
  - 不再继续用“上线前最后要看什么”的口径组织主文档
  - 当前明确承认 Web 主产品已具备上线基线
  - PRD 现在主要承接 4 条长期规划主线：
    - `App / Mobile Workbench`
    - `硬件接入`
    - `自定义语音 agent 框架`
    - `记忆架构`
  - 同时保留现役主链、durable owner、dataset/memory 边界这些不应再反复争论的产品基线
- 相关入口描述也已同步：
  - [README.md](/home/ubuntu/VoxFlame-Agent/README.md)
  - [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md)
  - [AGENTS.md](/home/ubuntu/VoxFlame-Agent/AGENTS.md)

### 2026-04-22

- 针对账号 `2307294809@qq.com`（用户 `64758dee-5026-4b53-a063-1d02d0834f67`）这轮补了一条更适合人工听样本的本地数据审阅链：
  - 根目录 [`.gitignore`](/home/ubuntu/VoxFlame-Agent/.gitignore) 已新增忽略目录 `artifacts/dataset-review/`
  - 使用现役导出脚本 `backend/scripts/export_audio_target_dataset.ts` 已把该账号当前 `194` 条训练样本导出到：
    - [artifacts/dataset-review/2307294809/samples.jsonl](/home/ubuntu/VoxFlame-Agent/artifacts/dataset-review/2307294809/samples.jsonl)
    - [artifacts/dataset-review/2307294809/audio](/home/ubuntu/VoxFlame-Agent/artifacts/dataset-review/2307294809/audio)
  - backend 新增 [export_dataset_review_report.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/export_dataset_review_report.ts)，可按邮箱/用户导出 `target_text / recognized_text / confidence / coverage / alignment` 的本地审阅报告
  - 当前该账号的首轮复核结果：
    - `194` 条样本里 `88` 条可先视为“标注大概率合理”
    - `106` 条建议人工复核，但 `metadata_incomplete = 0`
    - 高风险只有 `5` 条，而且都是 `recognized_text == target_text`、但 `coverage_ratio = 0 / confidence = 0.16 / alignment_tier = retry` 的异常组合，更像质量信号或评估逻辑异常，不像监督标签本身填错
    - 中风险主要集中在文言句、长句和个别日常句，表现为 `recognized_text` 与 `target_text` 差异较大；这更接近“音频难 / ASR 偏差 / 发音偏差需要人工听音频确认”，而不是字段丢失

### 2026-04-21

- 开源前的三份主入口文档这轮已经按“去掉已完成旧待办 + 明确开源协作主线”重新收口：
  - [README.md](/home/ubuntu/VoxFlame-Agent/README.md) 已删除一批还停留在“当前重点 / 近期开发路径”的旧条目，改成更适合开源协作的 4 条主线：
    - `Web 主产品继续打磨`
    - `App / Mobile Workbench 接入`
    - `硬件接入`
    - `自主语音 agent 架构`
  - 新增开源协作文档 [VOXFLAME_OPEN_SOURCE_COLLABORATION_DIRECTION_2026-04-21.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_OPEN_SOURCE_COLLABORATION_DIRECTION_2026-04-21.md)，单独回答 `App / 硬件 / 自主语音 agent` 怎么拆协作、怎么避免和当前主链打架
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 已删除已经过时的“上线前 blocker / 最后测试”章节，继续只保留产品边界、上下文 contract、模型分层和 durable write 原则
  - [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md) 已把开源协作方向加入现役入口与快速查找
- 核心 5 个页面这轮开始按“成熟产品面”统一收口，而不再是一页一页单独修：
  - 首页、手册页、记忆页、训练页、沟通页已统一到同一套暖色背景、白卡片主表面和更短的首屏说明
  - 首页 [HomeDashboard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/home/HomeDashboard.tsx) 现在首屏更聚焦“马上开始”，并新增 `新用户先这样开始 / 录音只记 3 件事`
  - 首页训练相关说明也已补上明确门槛：至少录够 `100` 句，才开始训练模型
  - `发音与朗读` 语料 [mandarin-training-real.json](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/generated/mandarin-training-real.json) 这轮已按官方 OpenCC 字表/词表统一把繁体字转换为简体，训练首页示例 [mandarin-training-data/index.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/index.ts) 也同步改成简体
  - 手册页 [manual/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/manual/page.tsx) 已收成短手册：只保留 `先做什么 / 怎么录 / 什么时候去哪个页`
  - 记忆页 [memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 顶部口径、section shell 预览和用户画像引导已进一步去后台感，改成更明确的“记忆与准备”页面
  - 沟通页 [ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 顶部已改成更短、更稳的沟通主线：先说关键一句，再补充
- 评估区“录了但没有 transcript”这条问题已经查到并直接修到交互上：
  - 根因更像是“双字短词已录到音频，但 transcript 没收稳”，而不是单纯“没录到声音”
  - 当前 [contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 已改成：
    - 空 transcript 时不再误提示成收音失败
    - 空 transcript 不再自动跳到下一词
    - 空 transcript 不再算进评估结果和严重程度
    - 会明确提示“这次录到了音频，但短词 transcript 还没收稳”
- 训练页 TTS 这轮也按“转录和播报必须是同一句”重新收口：
  - 根因已经确认：训练页 final transcript 之前还会进入 correction queue，再把 correction reply 拿去播，所以会出现 ASR 转录和 TTS 内容不一致，甚至播出“大家好”这类回复
  - 当前 [livekit_agent/app.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/app.py) 已改成：
    - `training_workspace` 不再把 final transcript 送去 correction
    - 如果训练页开启 TTS，就直接播 ASR final transcript 本身
    - 训练页对前端发出的 assistant transcript 也与 raw ASR 保持一致，不再生成另一句不同回复
- 评估短词的真实 finalize 问题也继续补到了 agent 侧：
  - frontend [useMandarinTrainingSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useMandarinTrainingSession.ts) 现在会在评估筛查模式显式上送 `short_utterance_expected`
  - [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 已改成：
    - 评估短词模式下不再把 `<=2` 字 final transcript 当成停录尾巴误杀
    - 停录后会给短词 final 更多收敛时间
    - 仍保留普通训练句的短尾巴脏词保护
- 训练标签这轮也进一步收口：
  - 旧“从记忆页文档解析病种 / 严重程度”的函数和测试残留已删除
  - 当前训练链只认结构化 `etiology / severity`
  - 疾病标签来源已经明确收成：评估区选择并保存到 durable `user_profile_memory`
- 用户画像里的病因 / 严重程度这轮已经不再只是“代码里有枚举、前端没有入口”的悬空状态：
  - backend [supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 的 `user_profile_memory` 现在会真正持久化 `etiology / severity`
  - [memory.controller.ts](/home/ubuntu/VoxFlame-Agent/backend/src/controllers/memory.controller.ts) 的 `PUT /memory/workspace/:userId/profile-memory` 也已接收这两个字段
  - frontend 当前没有再额外长“训练标签”表单或训练页提示区；按用户要求，只在 [memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 的用户画像文档编辑区里直接预填模板，不再增加新的填写表面
  - 训练录音上传时也已改成优先读取 durable `user_profile_memory` 的病因 / 严重程度，再直接以 `etiology / severity` 写进样本 metadata
  - frontend [training-guidance-profile.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/training-guidance-profile.ts) 新增了持久标签合并与标签文案 helper，前端不会再出现“有默认枚举但没有统一读取口径”的问题
- 针对用户 `2307294809@qq.com` 的现状复查，这轮确认了几个关键事实：
  - backend `training-report-maintenance` 服务确实处于启动状态，但该用户当前 active prepared expression `演讲-manual-input` 上 `daily / weekly / training_plan` 仍然都是 `null`
  - 这位用户昨晚存在训练与沟通 session；训练样本当前能在 `voice_contributions` 和 OSS 里同时看到
  - OSS 里可直接列到该用户 `supervised/mandarin/日常与出行/<userId>/*.wav` 与 `dataset/<userId>/manifest.jsonl`，所以对象层不是“没文件”，真正断的是 `memories / training_reports / durable 标签` 这条链
  - 现场也再次确认：此前 `user_profiles.condition = null`，`preferences.user_profile_memory` 没有结构化 `etiology / severity` 的问题，主要不是后端没留字段，而是前端没有稳定入口
  - 训练总结仍然 `null` 的直接根因已经查清：
    - 现有 `training_reports` 只汇总带 `prepared_expression_id` 的“自定义材料切句训练”，不会消费 `日常与出行` 这种通用主题句库
    - 而 summary service 当前读的还是 `memories.kind=training_result`，不是 `voice_contributions`
    - 这位用户现在 `voice_contributions` 里有训练样本，但 `memories` 里没有对应 `training_result`，所以这条总结链没有事实源输入
- 2026-04-21 训练总结逻辑这轮已按“全训练样本”重新收口：
  - backend [supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 的 summary 取样源已改成 `voice_contributions`
  - 不再依赖 `memories.kind=training_result`
  - summary 取样不再卡死在 `prepared_expression_id`，而是按用户全部训练样本的 `target_text / recognized_text` 差异做日总结和周总结
  - [prepared-expression-summary.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/prepared-expression-summary.service.ts) 的 prompt 也已清理成：
    - 先写一般性规律
    - 再给 1 到 2 个最有代表性的例子
    - 不再把准备材料说明、场景标题或系统提示误写成训练结论
    - 不管样本来自哪个训练专区，都按同一训练视角总结
- 2026-04-21 训练页已新增“评估主题区”：
  - frontend 新增 [assessment-screening.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/assessment-screening.ts)，提供 20 条高频双字筛查词
  - [training-topic-route.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/training-topic-route.ts) 新增 `/contribute/topic/assessment-screening`
  - [contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 现在会在评估主题区按正确字数 / 总字数计算字符准确率，并显示初步等级与最值得回看的词
  - 评估区现在内置“训练资料标签”小卡：
    - 用户先手选疾病种类：脑卒中、帕金森、脑瘫、脑外伤、听力相关、肌肉退化 / 神经肌肉相关、其他
    - 严重程度由评估词的字符准确率生成训练用初步分层
    - 用户保存一次后，`etiology / severity` 会写入 durable `user_profile_memory`
  - 训练上传 metadata 现在直接写 `etiology / severity`，不再额外写 `training_guidance_profile / speaker_profile`
  - 评估样本会额外带 `assessment_mode=screening`
  - 记忆页用户画像模板新增 `病种分类 / 严重程度` 行，并会从文档里自动提取成结构化标签
- 2026-04-21 首页已新增独立产品使用手册入口：
  - [HomeDashboard.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/home/HomeDashboard.tsx) 新增“查看使用手册”按钮
  - 新增 [manual/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/manual/page.tsx)，说明如何用好现有功能、如何录好音、如何先做评估再训练
- 2026-04-21 mock 清理检查已完成：
  - 产品运行路径已重新扫描 `mock / fake / dummy / 假数据 / 占位实现`
  - 清理了 [oss.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/oss.service.ts) 里的旧 `dummy` OSS client，改为 OSS 未配置时显式禁用或报错
  - 已验证：
  - `cd backend && npm run build`
  - `python3 livekit_agent/tests/test_asr_runtime.py`
  - `python3 livekit_agent/tests/test_data_contract.py`
  - `python3 livekit_agent/tests/test_session_userdata.py`
  - `python3 -m py_compile livekit_agent/app.py livekit_agent/asr_runtime.py livekit_agent/data_contract.py livekit_agent/session_userdata.py`
  - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types src/lib/training/training-assessment.test.ts src/lib/training/final-transcript.test.ts`
  - `cd frontend && npm run build`
  - `bash scripts/check_ai_docs.sh`

### 2026-04-20

- 腾讯云中国大陆上线文档这轮也已从“端口/预览清单”改成真正可执行的正式上线 runbook：
  - [docs/TENCENT_CLOUD_MAINLAND_DEPLOY_CHECKLIST_2026-04-07.md](/home/ubuntu/VoxFlame-Agent/docs/TENCENT_CLOUD_MAINLAND_DEPLOY_CHECKLIST_2026-04-07.md) 已整篇重写为“买域名 -> 域名实名认证 -> ICP 备案 -> DNS 解析 -> Caddy 自动 HTTPS -> 正式域名部署 -> 公网 RTC smoke”的顺序步骤
  - 当前文档不再把“公网 IP 预览入口”当正式目标，也不再把“多端口是否已开”当主线
  - 这轮也把当前代码真实边界写清了：
    - 第一阶段正式上线推荐走单域名 `app.<domain>` + `Caddy` 自动证书
    - 当前仓库还不应把“独立 `turn` 域名 + TURN/TLS + 单 IP”写成现成步骤
  - [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md) 已新增该文档入口，避免后续继续从旧清单视角理解大陆上线流程

- 训练语料库这轮按“只保留高质量普通话短句”继续收了一次：
  - 账号 `2307294809@qq.com` 对应用户 `64758dee-5026-4b53-a063-1d02d0834f67` 的旧训练数据已再次清空：
    - `voice_contributions: 51 -> 0`
    - 相关 OSS 训练对象与 `dataset/{userId}` 导出对象共删除 `53` 个
  - frontend 新增 [curated-topics.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/curated-topics.ts)
  - 当前训练页已不再直接使用低质量的四组旧生成语料，而是改成：
    - `日常与出行`: `88` 条
    - `看病与求助`: `67` 条
    - `人群与角色`: `89` 条
    - `设备与数字`: `160` 条
  - 这些句子全部限制在 `6-16` 字，并在文件内直接校验去重和长度
  - `发音与朗读` 按用户要求保持原状，没有替换
  - [mandarin-training-data/index.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/mandarin-training-data/index.ts) 现在会优先使用这四组精选语料，只保留 `发音与朗读` 继续走原朗读库
  - 当前旧 [mandarin-training-real.json](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/corpus/generated/mandarin-training-real.json) 也已继续收口：
    - 四组旧生成语料 `日常与出行 / 看病与求助 / 人群与角色 / 设备与数字` 已从文件物理删除
    - 旧库只保留 `发音与朗读` 作为 fallback
    - 前端分类顺序改成显式 `MANDARIN_TRAINING_CATEGORY_ORDER`，不再依赖旧 JSON 的 key 顺序
    - 删除旧 JSON 四类后，训练页主题卡、句子计数和录音页分类选择仍保持一致
  - 选材依据主要来自：
    - `12306` 重点旅客/出行服务说明
    - `120` 急救求助电话官方指引
    - 政务热线 / 公共服务普通话与文明用语规范
    - 常见手机操作与数字表达的高频普通话场景

- 训练录音上传链这轮已开始强制收口到 `wav only`：
  - frontend 新增 [recording-to-wav.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/audio/recording-to-wav.ts)
  - 当前会在真正落 OSS / 进入本地补传队列之前，把浏览器录音统一转成 `16k / mono / PCM16 WAV`
  - [useVoiceUpload.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useVoiceUpload.ts) 现在不再把原始 `webm` 直接上传或入队
  - 这意味着后续新录训练样本会只保存 `wav`，不再继续长新的 `webm` 训练资产

- 训练录音 finalize 这轮也按“只允许一条真实结果进入保存链”收了一次：
  - 根因不是训练页默认加载了 `speech.md / 大家好` 材料；对应训练 room 的 livekit-agent 启动日志里 `document_chars=0`
  - 真正的问题是训练页停录时同时会走：
    - frontend `manual_stop`
    - server VAD `speech_stopped -> auto_finalize`
    两条 finalize 路，导致同一轮录音可能被重复 `commit_audio`
  - 这会把像 `系统 / system` 这样的短尾巴 transcript 继续送进 assistant + TTS，表现成训练页偶尔自己念出莫名其妙的词
  - 当前 [asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 已新增基于 client recording capture 的重复 commit 抑制，并在 client 已明确停录后短暂抑制 VAD auto finalize
  - [app.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/app.py) 现在会真正消费训练页发来的 `speech_activity`
  - frontend [useMandarinTrainingSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useMandarinTrainingSession.ts) 不再偏向“本轮见过的最长 interim transcript”，而是优先接受 final transcript；只有拿不到 final 时才回退到 best observed interim
  - 新增回归：
    - [livekit_agent/tests/test_asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/tests/test_asr_runtime.py)
    - [frontend/src/lib/training/final-transcript.test.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/training/final-transcript.test.ts)
  - 已验证：
    - `python3 livekit_agent/tests/test_asr_runtime.py`
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types "src/lib/training/final-transcript.test.ts"`
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types "src/lib/realtime-audio/session-runtime.test.ts"`
    - `python3 -m py_compile livekit_agent/app.py livekit_agent/asr_runtime.py livekit_agent/data_contract.py`

- 训练数据集导出链这轮也补了一个更贴近真实训练使用的出口：
  - [backend/scripts/export_audio_target_dataset.ts](/home/ubuntu/VoxFlame-Agent/backend/scripts/export_audio_target_dataset.ts) 现在默认只导出：
    - `audio/`
    - `samples.jsonl` 里的 `audio + target + 可选 etiology / severity`
  - 不再把 `raw_transcript / recognized_text` 暴露成训练数据集字段；这些字段仍留在线上训练总结与错配统计链路里
  - 训练页 [frontend/src/app/contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 会把 `etiology / severity` 直接写入样本 metadata，给后续导出保留疾病种类 / 严重程度标签
  - 对真实用户 `2307294809@qq.com` 的当前检查结果：
    - `user_profiles.condition = null`
    - `preferences.user_profile_memory` 目前没有结构化 `etiology / severity`
    - 所以导出的 `samples.jsonl` 目前只有 `audio + target`
  - 已验证：
    - `cd frontend && npm run build`
    - `cd backend && npm run build`
    - `sudo docker exec voxflame-backend sh -lc "cd /app && npx tsx scripts/export_audio_target_dataset.ts --email 2307294809@qq.com --limit 100 --output-dir /app/tmp/dataset_2307294809_20260420"`

- 文档入口也继续收口了一轮：
  - 已删除两份会误导当前实现判断的过时计划文档：
    - `docs/LLM_CORRECTION_DEVELOPMENT_PLAN.md`
    - `docs/VOXFLAME_MEMORY_CONTEXT_MODEL_COMPACTION_EXECUTION_PLAN_2026-04-14.md`
  - 这两份文档都还停留在旧执行路径或旧治理阶段：
    - 前者仍以 `TEN extension / websocket / 独立纠错扩展` 为主线
    - 后者仍把前端 compaction / server-side compaction 作为主推进项
  - [docs/README.md](/home/ubuntu/VoxFlame-Agent/docs/README.md) 与 [AGENTS.md](/home/ubuntu/VoxFlame-Agent/AGENTS.md) 的引用入口已同步清理
  - 已验证：
    - `bash scripts/check_ai_docs.sh`
- 沟通页的 durable 用户画像更新触发边界这轮继续收了一层：
  - frontend [memory-service.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/memory/memory-service.ts) 不再只在 `disconnect/endSession` 时尝试 `session-close`
  - 现在收到带有效 `compaction_candidate` 的 `session_userdata_ack` 时，会先做一次去重后的增量画像写回
  - `disconnect/endSession` 仍保留最终兜底，不再是唯一触发点
  - 这让同一条 RTC 连接里持续沟通时，也能逐步更新 durable `user_profile_memory`
- 训练页 `training-report-maintenance` 的候选筛选也按最新口径收紧：
  - 仍然保持 `daily/weekly` 的定期窗口
  - 但只有在对应窗口内、且自上次 summary 生成后确实出现了新的 `training_result` 样本，才会进入自动补刷
  - 不再因为“summary 过期但没有新内容”就反复调用训练总结模型
- 已验证：
  - `cd frontend && npm test -- --test-name-pattern="session-runtime writes session userdata ack memory summary into current session metadata|buildSessionCloseUserProfileUpdate"`
  - `cd backend && npm run build`

## 下一步

1. 先把 `session-local typed memory / context assembly / session-close compaction` 补成稳定的 owner 链。
2. 训练页和记忆页继续围绕 `prepared-expression / important-expression / 高频句` 做统一句子录入入口，而不是继续长新 surface。
3. 数据录入和标注优先补齐 review queue、质量门槛和硬指标调查，不再只按样本数量推进。
   - 句子录入后默认自动跑“一句一音对应目标句”的校验，不先做独立 annotation UI
   - 只有自动校验命中风险的样本才进入复核
4. 记忆页继续收成“用户画像 + 当前准备 owner + 会后压缩结果”。
5. 再回到 10 分钟级别和公网条件下的真实沟通 smoke，验证这些沉淀是否真实影响现场纠错结果。
6. 本地验证入口继续收口到 `http://localhost:3000`：
   - 前端本地开发与 Docker 入口重新统一为单一 `3000` origin
   - 不再维护额外本地 HTTPS 入口，避免双 origin 带来的登录与跳转漂移
7. 本地登录和 RTC 鉴权稳定性已继续补强：
   - [frontend/src/lib/realtime-audio/session-bootstrap.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-bootstrap.ts) 不再优先发送可能过期的 `accessToken`，而是优先向 Supabase 取最新 token
   - [frontend/src/app/(auth)/login/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/(auth)/login/page.tsx) 登录成功后会先确认 fresh session，再做整页跳转，减少 client-only replace 和 server middleware 脱节

## 当前验证基线

- `cd frontend && npm run build`
- `cd backend && npm run build`
- `cd livekit_agent && python3 -m unittest discover tests -v`
- `python3 -m py_compile livekit_agent/app.py livekit_agent/asr_runtime.py livekit_agent/data_contract.py`
- `bash scripts/check_ai_docs.sh`
- `cd frontend && npm test`
- Docker 真实验证优先看：
  - `sudo docker compose logs -f livekit-agent`
  - 关键字：`LiveKit ASR first audio frame`、`LiveKit end_audio received`、`LiveKit ASR final transcript`

### 2026-04-07

- 沟通页展示又收口了一层：前端已移除 `表达对照`，用户界面只保留最终可直接说出去的结果。
- `livekit_agent` fallback 文案已收掉“现在先按当前沟通场景继续 / 我先帮你把这句话往前推进”这类铺垫，超时回退时也只回真正要说的话。
- 当前慢的主因已经查清：不是额度或磁盘问题，而是 `ASR final -> DashScope correction/reply -> TTS` 这段串行链路里，纠错/改写调用经常超时回退。
