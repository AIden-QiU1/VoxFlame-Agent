# VoxFlame Agent - 项目概述

> 这里只保留当前产品定义、现役事实源、最近 3 天仍有效的收口和下一步必须知道的状态。

## 产品定位

- 核心目标不是“纠正用户的声音”，而是“纠正系统对用户意图的理解”。
- 当前主产品链路是：`沟通工作台 -> 练习工作台 -> 沟通档案(memory/workspace)`。
- 当前迁移主线已经进入清退阶段：沟通与训练执行面都收口到 `self-hosted LiveKit + livekit_agent`。
- `ten_agent/` 目录已经从仓库物理删除，旧执行面不再是代码基座的一部分。
- `frontend / backend / livekit_agent` 代码层已经没有 `Agora/agora_ten` 运行时引用，残留的只是不影响运行的历史迁移文档。
- `docker-compose` 默认启动路径也已更符合最佳实践：
  - 默认核心栈是 `livekit-server / backend / frontend / livekit-agent`
  - `qdrant / redis` 改为 `extras` profile，不再拖主链启动
  - LiveKit server 默认镜像已钉到 `docker.m.daocloud.io/livekit/livekit-server:v1.10.1`
  - 当前容器实测已处于 `Up` 状态

## 现役事实源

- 现役执行面已经收口为 `frontend -> backend -> self-hosted livekit-server -> docker livekit-agent`。
- 模型/provider 现役约束仍是 `DashScope / Qwen-first`。
- `dataset != memory`，记忆继续由 `workspace / profile bundle / session review / expression kit` 语义承接。
- `LiveKit != durable memory owner` 这个判断也已经重新收清：
  - `LiveKit` 负责 session-local state、room state 和 session report raw material
  - `backend + workspace snapshot` 继续是 durable memory owner
  - `Qdrant` 适合后续 semantic recall layer
  - `Redis` 只有在明确需要 ephemeral coordination/cache 时再引入
- docs 目录也继续收口：
  - 已删除一个无引用、已脱离主线的旧研究文档：`CORPUS_SOURCE_RESEARCH_2026-03-19.md`
  - 也已删除三份已经和现役主线打架的旧 LiveKit 迁移文档：
    - `VOXFLAME_LIVEKIT_TRANSITION_PLAN_2026-03-31.md`
    - `VOXFLAME_LIVEKIT_REPLACEMENT_ROADMAP_2026-04-02.md`
    - `VOXFLAME_LIVEKIT_COMMUNICATION_MINIMAL_LOOP_PLAN_2026-04-02.md`
  - 当前关于 LiveKit、memory 和数据录入的现状与下一步，只以：
    - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md)
    - [.tasks/current.md](/home/ubuntu/VoxFlame-Agent/.tasks/current.md)
    - [VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_DATASET_SCHEMA_AND_RECORDER_PIPELINE_IMPLEMENTATION_2026-03-23.md)
    - [VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md)
    为准
  - `PRD / current task / 历史深参考` 的边界也重新写清了：
    - `PRD` 负责产品定义、页面职责和当前代码现状
    - `.tasks/current.md` 负责短期执行顺序、验证和优先级
    - `control-plane / unified memory` 不再代表当前代码真相，只保留为深参考

## 最近 3 天有效状态

### 2026-04-14

- 已开始按 PRD 落第一刀代码：`workspace document model + memory page object zones`
  - backend `workspace snapshot` 已新增 `object_zones`
  - 当前先把现有对象正式收成 4 个区：
    - `自定义材料区`
    - `场景 / 热词模板`
    - `用户个人画像`
    - `训练总结`
  - 记忆页顶部已改成对象区视图，底下仍复用现有准备稿 / 热词 / 训练总结编辑能力
  - 当前这是骨架切片，不代表新对象存储已经全部做完；它主要服务后续 `loadout / context assembly / compaction`
  - 已验证：
    - `cd frontend && npm run build`
    - `cd backend && npm run build`
- [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 已从“大而全规划稿”收成精简版现状 PRD：
  - 只保留产品主线、基于代码的现状、上线缺口和执行顺序
  - 明确当前网站已经具备 `首页 -> 沟通工作台 -> 练习工作台 -> 沟通档案` 的主骨架
  - 明确现在离正式上线最近的 3 个 blocker 是：
    - `livekit_agent` 侧 typed session memory / context assembly 还没制度化
    - session-close compaction 仍主要依赖前端 `memoryService.endSession()` 过渡链，不是 server-side 稳态
    - dataset review / annotation 只有后端 contract，还没形成前台操作与运营闭环
- [VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_AGENT_MEMORY_AND_TOOLING_REFERENCE_2026-03-26.md) 也已压缩成短版：
  - 明确 `backend workspace` 是 durable owner
  - 明确 `livekit_agent` 当前只有最小 session-local state，不应再被文档写成“完整记忆系统”
  - 明确 `dataset != memory`、`flush -> compact -> durable write` 仍是上线前待补 contract

### 2026-04-12

- `speech mode` 5 天专项计划已经完成阶段使命，独立执行文档已从 `docs/` 删除：
  - 剩余有效判断已经并回 `PRD / .tasks/current.md / dataset contract / LiveKit memory best practices`
  - 后续不再单独维护“speech mode 执行计划”入口
- 当前短期主线也已明确收口：
  - 不再新增大功能
  - 先把 `session-local typed memory -> session-close compaction -> workspace snapshot` 做扎实
  - 再把 `句子级准备资产 / 数据录入 / review queue / annotation 指标` 收成统一 contract
- 当前最值得推进的 3 个方向已经固定：
  - 记忆系统 owner、typed state 和 context assembly
  - 句子录入与 prepared-expression / important-expression 的统一入口
  - 标注与复核的硬指标，而不是只看样本数量

### 2026-04-11

- 这轮把训练页“通用句库看起来没有结果”的问题查清并收掉了：
  - 根因不是通用句库数据为空，也不是后端接口问题
  - 真正原因是前端 [contribute/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/contribute/page.tsx) 里有一个 `useEffect`
  - 只要账号已经存在 prepared content，这个 effect 就会再次执行 `setPracticeMode('prepared_content')`
  - 所以用户手动点了“通用句库”后，页面会立刻被切回“准备内容”，表现上像“通用句库没有结果”或“按钮点了没反应”
- 当前已改成更符合直觉的行为：
  - 只在 prepared content 第一次变为可用时，默认切到 `prepared_content`
  - 后续用户手动切到“通用句库”时，不再被自动抢回
  - 同时保留“首次进入已有准备内容账号时，默认先展示拆句训练”的原体验
- 已验证：
  - `cd frontend && npm run build`
  - `sudo docker compose build frontend`
  - `sudo docker compose up -d --force-recreate frontend`
  - Playwright 复测训练页：点击“通用句库”后，分类卡、搜索占位符 `搜索当前句库`、句子列表都会稳定停留在通用句库
- 最新运行态已确认：
  - `frontend` 容器已重建并恢复 `healthy`

### 2026-04-10

- 这轮又把“字幕辅助页红字超时”和“停录后环境音尾巴”一起收了一次：
  - 用户明确指向的是沟通工作台里的全屏字幕辅助页，不是另一套独立页面
  - 这次根因查清了两层：
    - `livekit-agent` 确实存在真实 LLM 超时，日志里有精确证据：`code=correction_timeout`、`latency_ms=15198`，而容器配置当前是 `DASHSCOPE_TIMEOUT_SECONDS=15`
    - 同时 ASR 在手动停录后会偶发产出 `嗯。/。/空串` 这类尾音 final transcript，和用户说的“设备停录后收进环境音”一致
  - 当前已直接修成产品兜底，而不是继续只报错：
    - `assistant_runtime` 现在在 `caption_mode` 下如果 DashScope 超时、失败、空回复或未配置，会直接回退到当前轮 ASR，source=`caption_asr_fallback`
    - 所以前屏字幕辅助页不再因为 correction 失败弹出 `本句整理超时，请再说一次完整句子。` 这种红字，而是继续显示当前 ASR
    - `app.py` 也同步跳过了 fallback 场景下的 `voice_profile_updated`，避免把“仅回退 ASR”误记成有效纠错
    - `asr_runtime` 新增了 `manual_stop` 后的短尾音保护：手动停录后的首个 `1-2` 字 final transcript（如 `嗯。/。`）会直接忽略，不再进入后续整理
  - 已新增验证：
    - `caption_mode` 下超时会返回原始 ASR，不抛错
    - `manual_stop` 后短尾音不会再 publish / enqueue
    - 有意义的手动停录 transcript 仍然保留
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`
    - `python3 -m unittest livekit_agent.tests.test_asr_runtime -v`
    - `python3 -m unittest discover livekit_agent/tests -v`
    - `sudo docker compose build livekit-agent`
    - `sudo docker compose up -d --force-recreate livekit-agent`
  - 最新运行态已确认：
    - `livekit-agent` 新 worker：`AW_6nesnP7edfK8`

- 这轮又把 correction history 的边界收准了一次：
  - 用户最后明确收口成：recent history 里只要规范化后长度是 `1-2` 个字，就直接跳过，不只限于 `嗯 / 啊`
  - `assistant_runtime` 现在的 recent correction history 行为改成：
    - 只对 history 项做过滤，不动当前 ASR
    - 规范化后长度为 `1-2` 字的旧 reply 一律不进入 recent history
    - 像 `嗯。/啊/你好/好的。` 这类短句都不会再占用 history 窗口
    - 像 `不是不知道该怎么过，嗯。` 这种正常句子仍然原样保留
    - 仍然维持“过滤短句后保留最近 5 条有信息量的唯一 correction history”
  - prompt 也继续明确：
    - 最近历史里长度只有 `1-2` 个字的旧结果不算有效上下文
    - 最近历史只用于语义承接和避免重复，不可直接复述
  - 同时确认一个产品事实：
    - 沟通档案页的“参考文档可编辑并保存更新”能力其实已经存在
    - 当前前端 [memory/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/memory/page.tsx) 已能直接编辑 `textarea` 后调用 `PUT /memory/workspace/:userId/prepared-expression`
    - backend `savePreparedExpressionAsset(...)` 也会沿用现有 draft `id` 覆盖保存，不需要删掉旧稿再重传
  - 这轮也把记忆页交互收成了最小版：
    - 页面会明确提示“这里就是当前参考文档的编辑区”
    - 主按钮会按状态显示 `保存第一份参考文档 / 更新参考文档`
    - 不再加多余编辑控件，只把“可改、可覆盖保存”讲清楚
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`
    - `python3 -m unittest discover livekit_agent/tests -v`
    - `cd frontend && npm run build`
    - `sudo docker compose build livekit-agent`
    - `sudo docker compose up -d --force-recreate livekit-agent`
    - `sudo docker compose build frontend`
    - `sudo docker compose up -d --force-recreate frontend`
  - 最新运行态已确认：
    - `livekit-agent` 新 worker：`AW_DqEJatisfBvj`
    - `frontend` 容器已切到新镜像，当前 `health: starting`

### 2026-04-09

- 这轮又按用户最新判断把 `assistant_runtime` 的短期 history 收了一次：
  - 不再把前几轮原始 `ASR(user)` 当作 correction history 回灌给模型
  - 改成只保留最近几轮 `LLM correction reply`
  - 这些旧 reply 不再以原始 `assistant` 对话消息形式直接展开到 `messages`
  - 而是作为“更可信但禁止复述”的结构化上下文，拼进当前轮 prompt：
    - 可用于理解承接关系、代词、省略和语义延续
    - 不能直接复述
    - 如果候选与最近历史高度相同、但当前 ASR 没明确重复同一句，则视为 `history echo`
  - 同时继续保留：
    - `document_content` 原文全文
    - `training_pairs`
    - 输出长度尽量贴近当前 ASR、优先控制在前后 `2` 字以内的 prompt 约束
  - 这轮还顺手把 recent correction history 改成“最近 5 条唯一 reply”，避免相同旧输出在 prompt 里反复强化
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`
    - `python3 -m unittest discover livekit_agent/tests -v`
    - `sudo docker compose build livekit-agent`
    - `sudo docker compose up -d --force-recreate livekit-agent`
  - 最新运行态已确认：
    - `livekit-agent` 新 worker：`AW_JViK7B2q9hxR`

- 这轮又把“短句近似原文优先”收回到纯 prompt 约束，不再用本地规则兜底：
  - 用户明确要求“不要用规则”，所以 `assistant_runtime` 没有继续保留本地 reference match / 直返逻辑
  - 当前改成只在 prompt 里更明确约束：
    - 最终输出长度尽量贴近本轮 ASR
    - 优先控制在前后不超过 `2` 个字
    - 如果明显更长或更短，默认视为改写过度，应回到更贴近 ASR 的版本
    - 只有参考原文里存在非常明确且更准确的对应原句时，才允许突破这个范围
  - 同时继续保留：
    - 最近 `5` 轮上下文
    - `document_content` 原文全文
    - `training_pairs`
    - 专名 / 机构名 / 术语优先按参考原文写法保留
  - `reference_lines` 仍然不参与文章原句匹配；唯一文章事实源继续是 `document_content`
  - 顺手保留了一个工程性修正：
    - `assistant_runtime` 里真实 `DashScopeChatClient` 继续走 `asyncio.to_thread(...)`
    - 测试 / 本地 fake client 走同步调用，避免测试 stub 卡住
  - 已验证：
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`
    - `python3 -m unittest discover livekit_agent/tests -v`
    - `sudo docker compose build livekit-agent`
    - `sudo docker compose up -d --force-recreate livekit-agent`
  - 最新运行态已确认：
    - `livekit-agent` 新 worker：`AW_64HjkZfiGcFE`

- 这轮又把“为什么 `我是邱生峰` 没翻对”查清了一次，根因不是单纯 LLM 弱，而是线上实际跑的是旧 `frontend/backend`：
  - 新 `livekit-agent` 日志里明确看到某次沟通房间：
    - `document_chars=0`
    - `training_pairs=0`
    - 且房间内没有收到 `preparation_context_update`
  - 也就是说，那次 correction 虽然在跑，但模型根本没看到参考文章和训练句对，自然不可能稳定把 `邱生峰`、`生声不息科技` 这类词纠回来
  - 同时也查到另一个前端时序坑：
    - `preparation_context_update` 之前只要第一次发送失败，就会被 `syncKey` 锁死，不再重试

- 这轮已经继续按“参考文章优先、不要 prompt cache”收口：
  - `assistant_runtime` 现在不再使用 `prompt/session cache`
  - correction 稳定前缀已改成更聚焦的版本：
    - 参考原文全文
    - 从参考原文和训练句对里提取出的 `人名 / 地名 / 公司名 / 术语`
    - 最近 `5` 轮上下文
    - `training_pairs`
  - 当前 prompt 也明确要求：
    - 不要管额外场景包装
    - 尽量恢复到参考原文里的准确说法
    - 最终输出不能带任何提示词，例如 `纠正后：/ 参考原文：/ 最终答案：/ 字幕：`
  - `assistant_runtime` 还新增了结果清洗：
    - 如果模型漏出 `纠正后：` 这类标签，会在发布前去掉

- 这轮把会话 preparation 同步继续改稳：
  - `frontend ChatInterface` 现在构造 `preparation_context_update` 时会：
    - 优先用 `snapshot.preparation`
    - 必要时回退到 `snapshot.prepared_expression`
  - 同时修掉了“第一次发送失败后不再重试”的问题
  - 这样即使 session start metadata 里没带全，也还能在房间建立后补齐参考文章全文和训练句对

- Docker 部署链这轮也补完了：
  - `backend/Dockerfile` 已改成 multi-stage，修掉了之前 `npm ci --only=production` 后容器内没有 `tsc`、导致镜像不能重建的问题
  - 已重新构建并重启：
    - `backend`
    - `frontend`
    - `livekit-agent`
  - 当前新实例时间：
    - `backend`: `2026-04-09T11:39:28Z`
    - `frontend`: `2026-04-09T11:39:28Z`
    - `livekit-agent`: `2026-04-09T11:39:28Z`
  - 最新 worker 已注册：
    - `worker_id=AW_ASbV5xh9KMWF`
  - 已验证：
    - `python3 -m unittest discover livekit_agent/tests -v`
    - `cd backend && npm run build`
    - `cd frontend && npm run build`
    - `sudo docker compose build backend frontend livekit-agent`
    - `sudo docker compose up -d --force-recreate backend frontend livekit-agent`

- `assistant_runtime` 这轮又按用户反馈做了一次减法：
  - 不再在本地做 `prepared anchor / 重点词硬匹配 / 直接返回文章句` 这类规则化纠错
  - 现在 runtime 只负责把这些材料完整送进 LLM：
    - 当前 ASR
    - 最近 `5` 轮上下文
    - `document_content` 参考原文全文
    - `training_pairs(target/heard)` 训练句对
  - prompt 当前明确收成：
    - 参考原文是人名、机构名、产品名、数字和术语的唯一优先写法来源
    - `training_pairs` 只是常见误听线索，不是本地替换规则
    - `reference_lines` 不再作为文章原句来源参与纠错
    - 同时固定把这些专名写法放进 prompt：
      - `邱生峰 / 燃言 / 上海生声不息科技 / 生声不息科技 / 智能体 / AI / LLM / VoxFlame`
  - 同时又补了一层 runtime 接收面：
    - `build_preparation_context_pack_from_payload(...)` 现在即使只有 `document_content + training_pairs` 也会接受，不会因为缺少 `immediate_goal/profile_summary` 被静默丢掉
  - 已验证：
    - `python3 -m unittest discover livekit_agent/tests -v`

- `assistant_runtime` 这轮又按“ASR 为主、文章锚点优先”收了一层：
  - 当前 correction prompt 不再把自己当“泛助手改写器”，而是改成：
    - 先看当前 ASR
    - 再结合最近上下文
    - 再用 `training_pairs(target/heard)` 做 alias 线索
    - 最后尽量回 `document_content` 里找对应原句
  - 这轮特别收清了一个上游 contract：
    - `document_content` 才是“保存的文章全文”，可用于精确文章原句匹配
    - `training_pairs` 只是帮助把 `heard -> target`，再去文章里找原句
    - `reference_lines` 在 backend 里混有 `fallback_phrases / personalized_phrases / quick_phrases`，不能再被当成“文章原句候选”直接返回
  - 当前 runtime 行为已改成：
    - 高置信命中 `training_pair + article sentence` 时，可直接返回文章里的精确原句
    - 只有 pair 命中但没文章原句时，pair 只作为 prompt hint，不直接当最终答案
    - 稳定 `system + scene + preparation` prompt 已恢复走 `build_cacheable_content(...)`，继续服务 prompt cache
  - 已新增单测覆盖：
    - 命中 `邱文峰 -> 邱生峰` 时优先回文章句子
    - 不把 `reference_lines` 误当成文章原句
    - cacheable stable prompt 结构仍在
  - 已执行：
    - `python3 -m unittest discover livekit_agent/tests -v`
    - `sudo docker compose build livekit-agent`
    - `sudo docker compose up -d --force-recreate livekit-agent`
  - 最新运行态已确认：
    - `livekit-agent` 新 worker 已重新注册

- 沟通工作台这轮又继续按“文字实时辅助优先”收了一层：
  - `livekit_agent` 现在默认把 `communication_workspace` 视为 text-first surface
  - 沟通页仍会产出最终 assistant transcript / correction 结果，但不再触发 TTS 代播
  - `caption_mode` 仍保留原有“强制跳过 TTS”逻辑；训练页等非沟通页 surface 不受这次收口影响
  - 已执行：
    - `python3 -m unittest discover livekit_agent/tests -v`
    - `sudo docker compose build livekit-agent`
    - `sudo docker compose up -d --force-recreate livekit-agent`
  - 最新运行态已确认：
    - `livekit-agent` 新 worker 已重新注册
    - 新日志关键字会变成 `LiveKit text-first reply active, skipping TTS ... surface=communication_workspace`

- 训练页这轮也把 prepared-expression 拆句逻辑补到了“全文覆盖”：
  - 前端 `buildPreparedExpressionPracticeExercises(...)` 现在优先使用 `document_content`，不再只依赖 `sections[].anchor_line + practice_lines`
  - 拆句规则已经改成：
    - 先按文章原文顺序走
    - 优先依据标点切分
    - 目标长度控制在 `5-15` 字
    - 不漏掉文章里未被现有 `sections` 摘出来的段落
  - `sections` 现在只负责给拆出来的句子补 metadata：
    - 命中现有 section 时继续复用原 section title / summary / anchor / tags
    - 没命中的段落会走前端 synthetic section metadata，而不是直接丢句
  - 训练页当前段落锚点也已改成优先读取 exercise 自带 `preparedExpressionAnchorLine`，避免 synthetic section 时 UI 掉锚点
  - 已执行：
    - `cd frontend && node --import ./test/register-runtime-test-hooks.mjs --test --experimental-strip-types "src/lib/training/prepared-expression-practice.test.ts"`
    - `cd frontend && npm run build`
    - `python3 -m unittest livekit_agent.tests.test_assistant_runtime -v`

- dataset 导出这轮又补了一层“最小训练集”收口：
  - backend 新增脚本 `backend/scripts/export_audio_target_dataset.ts`
  - `npm run export:audio-target -- --email <email> --include-pending --output-dir <dir>` 现在会：
    - 从 OSS 下载音频到服务器本地 `audio/`
    - 生成只包含 `audio` 和 `target` 的 `samples.jsonl`
  - 同时顺手修掉了 `export_dataset_manifest.ts` / `export_audio_target_dataset.ts` 里 `dotenv` 载入顺序问题：
    - 之前脚本会在 `.env` 生效前导入 `datasetExportService`
    - 导致某些环境下错误导出 `0` 条
  - 已用账号 `2307294809@qq.com` 做过 smoke：
    - 输出目录 `/tmp/voxflame-audio-target-smoke`
    - 结构为 `audio/*.webm + samples.jsonl`
    - `samples.jsonl` 每行仅有 `audio` 与 `target`

- prepared-expression / correction runtime 这轮又继续从“热词驱动”往“全文准备稿 + 训练句对驱动”收了一层：
  - backend `prepared-expression` 资产与 workspace snapshot 现在新增并正式携带：
    - `document_content`
    - `reference_lines`
    - `training_pairs`
  - `PreparedExpressionSummaryService` 现在不再把“ASR 热词包”当主目标，而是开始把训练样本压成：
    - 准备稿参考原句
    - `目标句 -> 系统听到` 训练句对
    - 只服务 correction 的短摘要
  - `rtc-orchestration` 与 `livekit-session` 已改成把这些字段注入 `LiveKitPreparationContext` / session metadata
  - `livekit_agent` 的 `PreparationContextPack` / prompt 也已切换到：
    - 准备稿全文
    - 训练句对
    - 最近几轮上下文
    - 当前轮 ASR
  - `assistant_runtime` 当前已开始把稳定前缀合并成单条 cacheable system message，按阿里云 DashScope explicit context cache 方式复用准备稿前缀
  - 当前心智已经明确：
    - `6000` 字级演讲稿不需要先做语义摘要才能上 correction
    - 真正需要控的是“历史对话窗口”，不是把准备稿本身缩得过头
- correction 失败路径这轮也做了关键收口：
  - `livekit_agent` 已移除 `timeout -> raw ASR` 这条伪成功 fallback
  - 现在只有真正拿到 `qwen3.6` correction 时才会发布 assistant transcript
  - 如果 DashScope 超时或失败，会改发 `error` envelope，前端会退出 `isThinking`，不再把原始 ASR 冒充成最终结果
  - `DashScopeChatClient` 现在使用 `DASHSCOPE_TIMEOUT_SECONDS` 作为真实 HTTP 超时，并把 `latency_ms / prompt_tokens / cached_tokens / completion_tokens` 打进日志，方便后面量真实延迟和 cache 命中
- 为了先压超时，这轮又把现役 `livekit-agent` correction 模型临时从 `qwen3.6-plus` 切到了 `qwen3.5-flash`
  - 只重建了 `livekit-agent`
  - 运行中容器已确认：`DASHSCOPE_LLM_MODEL=qwen3.5-flash`
  - 这轮目标是先看实际超时和体感延迟是否显著下降，再决定是否回到 `qwen3.6-plus` 或做双层模型策略
- 训练页 / 记忆页这轮也做了同步减法：
  - 纠错总结卡片已从“热词 / ASR 热词包”改成“训练句对 / 高频误听 / 下一轮重点”
  - 页面文案已尽量避免再暗示“qwen3-asr 有热词注入能力”
- 这轮本地验证已通过：
  - `cd backend && npm run build`
  - `cd livekit_agent && python3 -m unittest discover tests -v`
  - `cd frontend && npm run build`
- Docker 构建链这轮还额外查清了两个工程性问题：
  - 根 `.env` 之前把 `LIVEKIT_AGENT_BASE_IMAGE` 设成了 `voxflame-agent-livekit-agent:latest`，会让 `livekit_agent/Dockerfile` 自引用基础镜像并在重建时触发 `max depth exceeded`
  - `frontend/` 与 `backend/` 之前没有 `.dockerignore`，导致构建上下文会把 `frontend/node_modules`、`frontend/.next`、`backend/node_modules` 一起传给 Docker；现在已补上 `.dockerignore`，后续重建不该再重复传这些大目录

### 2026-04-08

- `speech mode` 第一轮 owner/context 收口已经继续往前推：
  - backend 已新增 `prepared expression asset` 读写与总结接口：
    - `GET /api/memory/workspace/:userId/prepared-expression`
    - `PUT /api/memory/workspace/:userId/prepared-expression`
    - `POST /api/memory/workspace/:userId/prepared-expression/summarize`
  - `PreparedExpressionSummaryService` 已落地：
    - 默认先走 heuristic fallback
    - 有 `DASHSCOPE_API_KEY` 时可用 `qwen3.5-plus` 一类小模型做 periodic/manual summary
  - `workspace snapshot.preparation` / `prepared_expression` 现在都已开始正式携带：
    - `hotwords`
    - `asr_hotword_entries`
    - `rehearsal_summary`
    - `next_focus`
- LiveKit 上下文组装这轮已经更贴近官方 best practice：
  - backend `rtc-orchestration` 现已把 `asr_hotword_entries` 注入 `LiveKitPreparationContext`
  - `livekit_agent` 现在会从 session metadata / dispatch metadata 读出：
    - `hotwords`
    - `asr_hotword_entries`
    - `risky_terms`
    - `common_confusions`
    - `fallback_phrases`
  - `CommunicationAssistantRuntime` 已改成：
    - 稳定 `system + scene + preparation` 前缀
    - 小窗口 recent history
    - 当前轮单独 prompt
    - 优先按热词/风险词做“最小必要纠错”，而不是大幅改写
- 训练页和记忆页已经开始真正闭环：
  - 记忆页现在新增了 `重要表达 owner` 区：
    - 可上传/粘贴 `.md/.txt`
    - 可保存准备稿
    - 可一键总结热词/规律/保底句
  - 训练页在 `prepared_expression` 模式下，达到周期门槛后会自动触发 `periodic_auto` summary
  - 这意味着 `prepared expression -> rehearsal -> summary -> workspace snapshot -> LiveKit context` 第一条闭环已经成立
- 4/8 这轮又做了一次“减法型收口”，把训练页/记忆页和后台链路继续压成更贴近现场展示的最小闭环：
  - 训练页现在只保留：
    - `准备内容 owner`
    - `拆句训练`
    - `录音`
    - `标签 / 目标句 / 系统听到 / 保存状态`
    - `每 50 句更新一次的纠错总结`
  - 记忆页现在只保留：
    - `准备内容 owner`
    - `自定义热词`
    - `训练总结 / 高频误听 / ASR 热词包`
  - 训练页逐句大模型点评链路已从前端、RTC capability、livekit_agent data contract 里退出主链
    - 不再请求 `training_feedback_request / training_coach_request`
    - `training_coach_feedback` 不再是现役消息类型
  - prepared-expression auto summary 的节奏已改成 `50` 句，不再按更短频率刷训练摘要
  - backend 不再在没有用户准备内容时回退到默认 `speech` prepared-expression 资产，避免 runtime 被硬编码场景偷偷带偏
  - `livekit_agent` 的旧逐句 training extension runtime 已从代码、配置、测试和 README 收口移除
    - 不再保留 `TrainingCoachRuntime`
    - 不再保留 `DASHSCOPE_TRAINING_EXTENSION_*` 配置
    - `docker-compose` 也不再传这类旧 env
  - 腾讯云公网部署这轮也已经做了完整重建验证：
    - 已执行 `sudo docker compose --profile https build --no-cache frontend backend livekit-agent`
    - 已执行 `sudo docker compose --profile https up -d --force-recreate livekit-server backend frontend livekit-agent caddy`
    - `frontend / backend / livekit-agent / caddy` 日志均已确认跑在新版本
  - 前端公网登录跳转 bug 也已修复：
    - 未登录访问 `/contribute` / `/memory` 不再跳到 `localhost:3100`
    - `middleware` 与 `auth/callback` 现在会按 `x-forwarded-host / x-forwarded-proto` 组装外部 origin
  - 已用新注册账号 `voxflame.e2e.20260408152550@example.com` 做一次真实公网 smoke：
    - 无需额外邮箱验证即可注册
    - 训练页已显示新的最小闭环布局
    - 记忆页已显示新的空态与 `自定义热词 / 训练总结` 布局
    - 新账号不会再读到默认 `speech.md` 内容
- PWA 也已经重新收口：
  - frontend `next.config.js` 现在默认生产构建启用 PWA，不再要求额外显式开关才生成 `sw.js`
  - `VOXFLAME_ENABLE_PWA_DEV=1` 时可以显式允许 localhost 保留 service worker / install prompt
  - 默认本地开发仍会清掉 localhost runtime cache，避免被旧 SW 干扰
- 这一轮验证已通过：
  - `cd frontend && npm run build`
  - `cd backend && npm run build`
  - `python3 -m unittest discover livekit_agent/tests -v`
- 这轮又把现场字幕主链往“可长时运行”方向收了一层：
  - 前端继续复用现有 `字幕辅助 / 全屏字幕模式`，不再新造页面
  - `ChatInterface` 现在进入字幕模式时会发 `caption_mode_update`
  - `livekit_agent` 已开始感知字幕模式：
    - 字幕模式下改用更保守的 caption prompt
    - 会跳过 TTS，把资源优先留给 `ASR + correction`
  - `livekit_agent` 的最终 transcript 处理已改为异步入队、单 worker 串行调度
    - 不再在 ASR 事件循环里直接等待 correction/TTS 完成
    - 避免长句/慢链路把后续 ASR 事件拖住
  - 沟通页当前启动会话时已显式申请 `1800s` 长会话
  - 全屏字幕模式现在会显示：
    - 当前字幕
    - 最近字幕
    - `识别中 / 正在整理本句...`
  - 前端消息列表已加上限裁剪，避免长时会话把状态无限堆大
- 这轮验证也已通过：
  - `cd frontend && npm run build`
  - `cd livekit_agent && python3 -m unittest discover tests -v`
- HTTPS RTC 这轮又继续往前收了一层，关键结论已经更清楚：
  - backend 的 localhost 修复之外，又确认并修掉了两个 livekit-server 级根因：
    - `docker-compose` 之前把 `LIVEKIT_TURN_TLS_PORT` 直接注入容器环境，即使脚本里“关闭了 TURN/TLS”，`livekit-server v1.10.1` 仍会把它当成原生 TURN/TLS 配置并直接报 `TURN domain required`
    - `livekit.public.yaml` 在当前腾讯云单公网 IP 预览形态下，继续使用 `rtc.use_external_ip: true` 会让 LiveKit 在 `7882` 上自做 STUN 探测，随后又因同端口二次绑定出现 `listen udp ...:7882: bind: address already in use`
  - 当前已落地的修复：
    - `docker-compose.yml` 与 `infra/livekit/start-livekit.sh` 现已改成使用 `VOXFLAME_LIVEKIT_TURN_*` 变量，由脚本生成 TURN 配置，不再把半配置的 TURN/TLS 参数直接暴露给 `livekit-server`
    - `infra/livekit/livekit.public.yaml` 现已改成显式 `rtc.node_ip: 111.230.35.89` 并关闭 `rtc.use_external_ip`
    - `.env` / `.env.example` 已同步切到新的 `VOXFLAME_LIVEKIT_TURN_*` 变量名
  - 服务器侧最新实况：
    - `livekit-server` 已能稳定监听 `7880/tcp`、`7881/tcp`、`7882/udp`、`3478/udp`
    - 浏览器在公网 HTTPS 页面拿到的 ICE server 现在只剩 `turn:111.230.35.89:3478?transport=udp`，不再出现错误的 `turns:...:443`
  - 剩余 blocker 也已经更具体：
    - 浏览器仍停在 `checking / connecting`，页面继续提示“当前网络没能建立实时语音连接”
    - 本机 `ufw` 未启用、`iptables INPUT ACCEPT`，所以当前更像是腾讯云安全组或用户上游网络还没放通 `3478/udp + 7882/udp + 7881/tcp`
    - 当前预览形态没有开启 TURN/TLS，因此更严格的公司网 / 校园网 / VPN 下仍可能失败；长期正式方案仍是独立 `turn` 域名 + 证书 + TURN/TLS 或单独公网 IP / L4 路由
- 4/8 晚上这条公网 HTTPS RTC 还又补了一步最终收口：
  - 腾讯云防火墙已确认放开：
    - `80/tcp`
    - `443/tcp`
    - `7881/tcp`
    - `7882/udp`
    - `3478/udp`
  - 之后又定位到第二个 runtime 问题：
    - `livekit-agent` 在 `livekit-server` 尚未稳定时启动失败，旧 worker 一直没有重新注册，所以 `livekit-server` 日志里持续出现 `not dispatching agent job since no worker is available`
  - 当前已手动重启 `livekit-agent`
  - 最新日志已经出现：
    - `registered worker`
    - `participant active`
    - `mediaTrack published`
  - 这说明公网 HTTPS 下至少当前主链已经能完成：
    - 浏览器成功入房
    - UDP 连接建立
    - 麦克风音轨发布
    - worker 注册回 LiveKit
- 4/8 这轮又把“能连房但没转录”的假成功收了一层，按 LiveKit 官方 `worker registration -> dispatch -> job join room` 心智补了双保险：
  - backend 现在会在 `POST /api/rtc/session/start` 前先探测 LiveKit Agents 官方默认私有 health endpoint：
    - `LIVEKIT_AGENT_HEALTH_URL`，默认 `http://livekit-agent:8081/`
    - 如果 `livekit-agent` 进程都没活着，backend 会直接返回 `503`，不再发出空会话 token
  - frontend 现在不再把“浏览器已经连上 LiveKit room”当作真正 ready：
    - `livekit_agent` 进房后发出的 `session_init_ack` 现在被前端显式等待
    - 只有收到了 `session_init_ack`，页面才进入 `isConnected`
    - 如果没等到 ack，会明确判成失败，而不是显示“已连接”
  - 这层还补了一个一次性自动重试：
    - 如果第一轮因为 worker 刚恢复、dispatch 时机太早而错过 init ack，前端会自动拉一轮新会话重试一次
  - 同时把 `system_init / update_voice_profile` 的发送时机后移到 init ack 之后，避免 agent 还没真正进房时控制消息被提前丢掉
  - 还顺手修掉了一个重连隐患：
    - `startRtcRuntimeConnection` 失败路径现在会清理 `clientRef / sessionRef / rtmClientRef`
    - 避免一次失败后 hook 误以为“会话还在”，从而挡住下一次重连

### 2026-04-07

- 腾讯云大陆机公网 HTTPS 预览链路已补齐：
  - 已新增 [TENCENT_CLOUD_MAINLAND_DEPLOY_CHECKLIST_2026-04-07.md](/home/ubuntu/VoxFlame-Agent/docs/TENCENT_CLOUD_MAINLAND_DEPLOY_CHECKLIST_2026-04-07.md)
  - `docker-compose.yml` 现已支持 `https` profile 下的 `Caddy` 入口
  - `Caddy` 当前为 `host network`，避免 raw IP HTTPS 在 Docker bridge 下因无 SNI 选不到证书
  - 当前公网 HTTPS 预览入口已实测可打开：`https://111.230.35.89`
  - 同域 `/api/rtc/health` 也已经通过 HTTPS 命中 backend，并按预期返回 `401 Unauthorized`
- 这轮部署改造还补了两层基础设施收口：
  - backend `LiveKitConfigService` 现在支持 `VOXFLAME_PUBLIC_BASE_URL`
  - 新增 `infra/livekit/start-livekit.sh`，避免 compose shell 解析问题把 `livekit-server` 启动命令拆坏
- 当前大陆正式上线判断也已更具体：
  - `sslip.io` 这类未备案临时域名在腾讯云大陆机上会撞备案拦截，不适合继续作为正式路线
  - 这台机器已经成功签到 `Let's Encrypt` 的公网 IP 证书
  - 正式品牌入口仍然建议切回自有备案域名，而不是长期停留在 IP 入口
- 当前 deployment 变量组合已固定为：
  - `VOXFLAME_PUBLIC_HOST=111.230.35.89`
  - `VOXFLAME_PUBLIC_BASE_URL=https://111.230.35.89`
  - `LIVEKIT_BROWSER_URL=wss://111.230.35.89`
  - `LIVEKIT_CONFIG_FILE=./infra/livekit/livekit.public.yaml`
  - `LIVEKIT_SERVER_DEV_MODE=0`

- 沟通页展示进一步收口：
  - 已移除 `表达对照`
  - 前端状态树中的 `currentDualLine / DualLineSubtitle` 也已删除
  - 当前沟通页只保留最终可直接说出的结果，不再把“机器听到的 / 修正前后对照”作为主展示
- `livekit_agent` 沟通 fallback 已进一步收口：
  - 不再输出“现在先按当前沟通场景继续 / 我先帮你把这句话往前推进”这类前缀
  - 如果 DashScope reply generation 超时，会直接回退到用户原句或最小 fallback
- 当前慢的主因已经重新钉死：
  - 不是磁盘空间
  - 不是阿里云额度
  - 主要是 `ASR final -> DashScope correction/reply -> TTS` 的串行等待
  - 其中 `DashScope reply generation timed out` 已在 worker 日志里反复出现
- 这轮已经补了一层减时延策略：
  - `CommunicationAssistantRuntime` 现在会按输入长度使用自适应 reply timeout
  - 短句不会再傻等完整超时上限
  - 更长表达仍保留更宽容的窗口
- 最新验证已通过：
  - `cd livekit_agent && python3 -m unittest discover tests -v`
  - `cd frontend && npm test`
  - `cd frontend && npm run build`
  - `bash scripts/check_ai_docs.sh`
  - `sudo docker compose up -d --build --no-deps frontend livekit-agent`

### 2026-04-06

- 4/6 日记分析已经正式并入主文档：
  - [VOXFLAME_PRODUCT_PRD_2026-03-24.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_PRODUCT_PRD_2026-03-24.md) 已新增 future mobile、shared contract、`context assembly` 和“多 agent 不进现场主链”的中长期判断
  - [AI_ENGINEERING_SYSTEM.md](/home/ubuntu/VoxFlame-Agent/docs/AI_ENGINEERING_SYSTEM.md) 已新增 `synthesis-only`、`context assembly`、并发安全分级和拒绝追踪等协作体系升级点
- 当前新的短期工程判断已经固定：
  - 最值得先共享的不是框架，而是：
    - `session metadata`
    - `dispatch metadata`
    - `training_result`
    - `session_compaction`
    - `workspace snapshot.preparation`
    - `important-expression / prepared-expression` 视图
  - 当前 backend 继续以现有 `Express + TypeScript` 为主，不为 5 天专项临时发起大迁移
  - `context assembly` 已被明确成当前 runtime 下一步必须补的层：
    - `workspace snapshot` 继续做 durable owner
    - correction / rewrite / live assist 应逐步改为吃“最小必要准备”
    - 不直接灌原始 transcript / 全量训练流水
- 一个新的多端判断也已补清：
  - future mobile / desktop companion 应优先复用现有 web 主链稳定下来的 contract
  - future mobile 如需 companion app，可优先考虑贴近现有 TypeScript 心智的路线，但现在不抢主线资源
- 一个新的 agent 边界也已补清：
  - 多 agent / fork / swarm 更适合研发协作、离线分析和后台重任务
  - 不适合直接进入构音障碍用户的现场语音主链
  - 现场 runtime 仍以 `低延迟 / 可预测 / 可打断 / 不越权` 为第一目标

### 2026-04-05

- 当前最重要的产品目标已经从“继续泛化替代 TEN”切到“5 天内验证 VoxFlame 是否真的能帮助构音障碍用户完成高压、长时、结构化表达”。
- 当时的专项执行结论后续已经并回 `PRD / .tasks/current.md`，不再单独维护独立执行文档。
- 外部参考分析已经并入主文档，当前最值得借的长处已经写实：
  - `OpenClaw`: memory source-of-truth 与 context assembly 分离、自动 memory flush、hybrid recall
  - `VibeVoice`: 长时 transcript / hotword / 结构化长音频目标意识
  - `Lime`: 中文优先、信息优先、工作台按页面职责定宽度和层级
  - `CLEAR-VOX-MODEL`: 个体差异优先、LLM N-best rerank、规律/热词比流水更重要
- 一个关键产品边界也已经收清：
  - 所有页面都应该继续变得更直白、更简单
  - `记忆页` 做准备内容的 owner
  - `沟通页` 只显示当前场景的最小必要准备
  - 不把赌注压在“现场 agent 即兴发挥”上，而是先准备好规律、热词、保底句，再让 agent 稳定发挥
- 这份计划把当前主线重新收成：
  - `structured rehearsal mode`
  - `long-form live assist mode`
  - `review mode`
- 其中一个关键纠偏已经明确：
  - 记忆页不是训练复盘页
  - 它应该是 `用户全面画像 + 常见场景 + 即将面对场景的准备页`
  - training result 只是输入，不是页面本体
  - 这页未来应服务：
    - 用户自己上台前准备
    - agent runtime
    - LLM correction 的结构化上下文
- 关键判断已写实：
  - 这 5 天不能把成败押在“数据再录一点、微调马上显著变强”上
  - 数据录入和标注准确仍然重要，但定位是长期增益，不是短期关键路径
  - 短期更该依赖：
    - LiveKit self-hosted realtime loop
    - DashScope / Qwen-first 的现役 ASR / rewrite / TTS
    - 现有 `workspace snapshot / profile_bundle / session_review / expression_kit`
  - 记忆系统不应重做，而应继续沿现有 durable owner 做 `prepared-expression / pattern-extraction` 派生体验
  - 记忆最重要的是记住规律/特点，最高效地表达给：
    - 用户自己
    - agent runtime
    - LLM correction
- 这轮还新增了一份专门针对 LiveKit 的记忆 best practices 收口：
  - [VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md](/home/ubuntu/VoxFlame-Agent/docs/VOXFLAME_LIVEKIT_MEMORY_BEST_PRACTICES_2026-04-05.md)
  - 它把 `userdata / chat_ctx / participant attributes / session report / Qdrant / Redis` 各自该放哪一层写清楚了
  - 当前最重要的结论是：
    - `userdata` 应该承接 typed session working state
    - `chat_ctx` 应只承接当前任务最小必要准备
    - `session report` 应该作为 session-close compaction 输入
    - `Qdrant` 应作为 recall 增强层接回，而不是 durable truth
- 一个新的执行判断已经固定：
  - 在今天这种 vibe coding 时代，`5 天做出一套真正够用的记忆系统和产品收口` 并不算离谱
  - 真正的难点不是编码速度，而是我们能不能把：
    - 模型链路如何分工
    - agent 在现场到底做什么
    - 记忆该记什么、怎么压缩、怎么被现场调用
    这几件事判断准
- 当前代码现状已经足以承接这条新主线：
  - 训练结果已能沿 `training_result -> training_profile_summary -> workspace snapshot` 链沉淀
  - 记忆页本身已经具备“上台前准备页”的雏形
  - 这轮又继续收口了一层：
    - backend `workspaceSnapshot.preparation` 新增 `profile_summary / listener_guidance`
    - memory 页顶部现在先展示“我的表达画像”，再展示“当前重要表达准备”和“最近复盘”
    - 页面本体更接近“用户画像 + 当前准备 owner”，而不是训练复盘页
  - 沟通页已有 LiveKit communication / TTS / interrupt / turn-taking 基线
  - 旧执行面的辅助工具链也在继续收尾：
    - `frontend/Dockerfile` 已删除 `NEXT_PUBLIC_ENABLE_LIVEKIT_TRANSPORT`
    - `frontend/README.md` 已切到 `livekit_agent/README.md`
    - `Qwen ASR/TTS live smoke` 现在默认直打 `voxflame-livekit-agent`
    - 两条 smoke 已在现役 `livekit-agent` 容器上通过
- 当前最值得做的第一刀也已明确：
  - 把 [speech.md](/home/ubuntu/VoxFlame-Agent/speech.md) 作为第一份验证样本结构化成 `prepared expression` 资产
  - 为训练页加 `pattern extraction + structured output`
  - 记忆页顶部新增“当前重要表达准备”模块，并突出热词/规律而不是复盘流水
- 这轮还把 LiveKit 当前能力判断写实了：
  - 已成立：
    - ASR
    - correction-style transcript
    - TTS
    - basic interrupt / turn-taking
    - training feedback 最小 contract
  - 仍未补齐：
    - 更完整的 session-close compaction
    - 更稳的 interruption / barge-in policy
    - 会话内 speaker differentiation
    - LiveKit 官方 `audio_input / APM` 接入下更保守的 audio processing strategy
- 另一个关键工程判断也已钉死：
  - 当前 `QWEN_ASR_BARGE_IN_MIN_SPEECH_MS=220` 只是工程默认起始值，不是官方推荐定值
  - 下一步要尽量向 LiveKit 官方 primitives 收口：
    - `turn_detection / interruptions / endpointing`
    - `manual / hybrid turn control`
    - `room_options.audio_input` / APM
    - `session.userdata / participant attributes`
  - 即使继续使用 `DashScope / Qwen-first` provider，也应该把 agent/session/audio 结构尽量按官方最佳实践补完整
- `session.userdata + PreparationContextPack` 第一片也已经落地：
  - `livekit_agent` 现已拥有 session-local typed state owner
  - session start 会构建最小 `PreparationContextPack`
  - communication rewrite 已开始读取这层准备信息
  - backend 也已经会把 `workspace snapshot.preparation` 注入 session metadata / dispatch metadata
- `session-close compaction` 第一片也已经落地：
  - session end 时现在会自动生成 `session_compaction`
  - 当前会把 `fallback phrases / risky terms / pronunciation patterns / support strategies / hotwords / interruption telemetry`
    压成结构化语义记忆
  - frontend / backend 的 `memory growth -> workspace snapshot` 已开始消费这层 compact memory
  - 这项剩余工作收口到：
    - 更强的 pattern extraction
    - recall policy
    - participant attributes 配合的低频共享状态
- 输入电平与收音质量反馈第一片也已经落地：
  - 沟通页和训练页现在都会给出实时收音质量提示
  - 当前会提示：
    - `声音偏小`
    - `收音稳定`
    - `声音过冲`
  - 这一步主要服务高压场景下的即时自检，不等于已经接上 LiveKit 官方 `audio_input / APM`
  - 下一步仍然要继续往：
    - `room_options.audio_input`
    - Python RTC APM
    - 更细的输入 telemetry
    收口
- LiveKit Python RTC `AudioProcessingModule` 第一片也已经落地：
  - `livekit_agent` 现在会在 agent 订阅到的房间麦克风帧上先过 LiveKit 官方 APM
  - 当前采用保守配置：
    - `noise_suppression=true`
    - `high_pass_filter=true`
    - `echo_cancellation=false`
    - `auto_gain_control=false`
  - 这样做是因为：
    - 现在处理的是远端订阅音频，不是本地全双工采集
    - 对构音障碍用户先避免过激增益和误伤发音特征
  - 这一步不等于 `room_options.audio_input` 已完成
  - 下一步继续收口到：
    - `room_options.audio_input`
    - 更细的 audio telemetry
    - 会话内 speaker differentiation
- server-side audio telemetry 第一片也已经落地：
  - `livekit_agent` 现在会正式发出：
    - `normalized_level`
    - `peak_level`
    - `clipping_detected`
    - `apm_enabled`
    - `reason`
  - frontend 会把这些信号写进当前 session metadata
  - `session-close compaction` 也已开始吸收这层信号
  - 这让后续能更可靠地区分：
    - 发音问题
    - 收音问题
- 2026-04-06 新查清一个导致“已连接但助手不工作”的真因：
  - 不是前端假连接
  - 是 `livekit-agent` 在收到真实麦克风音频后，把不满足 10ms 整数帧要求的 PCM 直接送进了 LiveKit Python RTC `AudioProcessingModule`
  - Rust APM 层会直接 panic：`slice must have a multiple of 10ms worth of samples`
  - 已在 [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 改成：
    - 先把单声道 PCM 累积成严格 10ms chunk
    - 再逐块 `process_stream`
    - 剩余不足 10ms 的尾部保存在 `_apm_remainder`
  - 对应单测已补到 [livekit_agent/tests/test_asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/tests/test_asr_runtime.py)
  - `livekit-agent` 容器已重建并重新注册 worker
- 这轮又把“记忆页和训练画像的产品语义”收紧了一层：
  - `memory growth / workspace snapshot / memory page` 不再继续围绕：
    - 声母
    - 韵母
    - 声调
    - 重点音节
    长栏目展开
  - 现役产品语义开始统一为：
    - `热词`
    - `用户发音规律`
    - `场景总结`
  - 对应代码面已经同步：
    - `memory growth` 改成产出 `speech_patterns`
    - 记忆页改成围绕用户发音规律和场景总结展示
    - 前端剩余的 `mandarin-pinyin.ts` 已删除
    - `session-types / session-runtime.test` 里的旧 pair 字段也已清理
- 这轮第一刀已经开始补 `interruption / barge-in policy`：
  - `speech_started` 不再直接打断 TTS
  - 现在改成“有门槛的 barge-in”
  - 用户持续说够 `QWEN_ASR_BARGE_IN_MIN_SPEECH_MS`（当前默认 `220ms`）后，才会触发真正的 `barge_in_triggered`
  - 前端也只在 `barge_in_triggered` 时收起 `isSpeaking / isThinking`
  - 这一步是为了减少短促噪声、咳嗽和误触造成的误打断，更适合构音障碍用户
- 前端训练语料和关键词原则也已开始继续收口：
  - 现役训练语料 JSON 里的 `pinyin` 已物理删除
  - `mandarin-training-data/index.ts` 现在从真实 corpus 自动派生训练类别，不再手写一套 category 列表绑定前端
  - 未被页面消费的 `sources.ts` 与 source 相关静态结构已删除
  - 训练链不再默认写空 `keywords`
  - `livekit_agent` 训练反馈现在也不再用 category 兜底硬塞宽泛关键词
  - 新的产品/前端原则已经固定：
    - 不保留没有意义的空字段和空占位
    - 关键词要少、准、对记忆/检索/准备真的有帮助
    - 前端展示优先来自真实 corpus、结构化 feedback、workspace snapshot 和可解释规则

### 2026-04-03

- `localhost:3000` 已成为 LiveKit communication 的单口入口：
  - [frontend/proxy-server.cjs](/home/ubuntu/VoxFlame-Agent/frontend/proxy-server.cjs) 统一承接页面、API 和 `/rtc/*` signaling websocket
  - 浏览器不再需要额外手动转发 `7880`
  - 真实请求已确认 `executionBackend: "livekit"`，不是 TEN 回退
- `self-hosted LiveKit RTC` 已经真实可用：
  - backend 会签发 LiveKit token/dispatch metadata
  - `livekit-agent` 已在 Docker 中注册并接 job
  - communication 文字改写与 TTS 音频回放已跑通
- 真实 voice smoke 的根因已查清一刀并修复：
  - 用户麦克风轨此前以 `source: UNKNOWN` 发布
  - worker 订阅的是 `SOURCE_MICROPHONE`
  - 已在 [frontend/src/lib/realtime-audio/session-audio.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-audio.ts) 改为显式 `publishTrack(..., { source: Track.Source.Microphone })`
- `livekit_agent` 现在已具备最小 `ASR -> correction-style transcript metadata -> assistant reply -> TTS` 骨架：
  - [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 已接入 `DashScope realtime ASR`
  - [livekit_agent/app.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/app.py) 现在会在语音路径上回发带 `metadata.type = "correction"` 和 `metadata.original` 的 assistant transcript
  - [livekit_agent/data_contract.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/data_contract.py) 已支持这套 correction metadata
- `livekit_agent` 已接入第一版 server-side `VAD / 自动收句`：
  - 当前在 [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 内使用轻量 RMS 检测
  - 持续静音后会自动 `commit`
  - 仍保留手动 `end_audio` 作为双保险
- `livekit_agent` 已开始发出最小 `voice_profile_updated` 信号：
  - correction reply 后会估算 `clarity_score`
  - [frontend/src/lib/realtime-audio/session-runtime.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-runtime.ts) 会把它并入当前 session metadata
  - 这一步先给后续 `session_review / memory parity` 铺真实数据面
- `livekit_agent` 已开始具备最小 turn-taking：
  - [livekit_agent/asr_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/asr_runtime.py) 会回调 `speech_started / speech_stopped`
  - [livekit_agent/tts_runtime.py](/home/ubuntu/VoxFlame-Agent/livekit_agent/tts_runtime.py) 现支持 interrupt 当前 TTS
  - [frontend/src/lib/realtime-audio/session-messages.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-messages.ts) 会消费 `speech_activity`，更快反映用户打断
- LiveKit 沟通会话现在会正式走向 `session review / memory`：
  - [frontend/src/lib/realtime-audio/session-runtime.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-runtime.ts) 在连接时补齐 `surface / scene / executionBackend / transportProvider` 等 session metadata
  - [frontend/src/hooks/useRtcAgentSession.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/hooks/useRtcAgentSession.ts) 在断开时会 `memoryService.endSession()`
  - 这意味着 LiveKit communication 不再只是当场对话，而会开始沉淀进现有 workspace owner
- 沟通页执行面现在也已显式切到 LiveKit：
  - [frontend/src/components/chat/ChatInterface.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/components/chat/ChatInterface.tsx) 已指定 `executionBackend: 'livekit'`
  - [frontend/src/lib/config.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/config.ts) 默认执行面已从 `agora_ten` 收口到 `livekit`
  - [frontend/src/lib/realtime-audio/session-execution.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-execution.ts) 的 LiveKit transport gate 已删除，不再要求 `NEXT_PUBLIC_ENABLE_LIVEKIT_TRANSPORT`
  - [backend/src/services/rtc-orchestration.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/rtc-orchestration.service.ts) 现已完全收口到 `livekit`，不再保留 `agora_ten` 分支
  - [frontend/src/lib/realtime-audio/agora-transport.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/agora-transport.ts) 已删除，前端 `Agora` npm 依赖也已移除
  - [docker-compose.yml](/home/ubuntu/VoxFlame-Agent/docker-compose.yml) 已移除 `ten-agent` service；旧 orphan 容器也已通过 `docker compose down --remove-orphans` 清掉
- 这部分后来已继续收口：
  - 训练页现役链路不再依赖 `training_feedback_request`
  - 只保留每次录音的 `training_result` 写入、50 句总结回流和 `voice_profile_updated` 信号
  - 逐句 AI 教练 runtime 已在 2026-04-08 从现役代码路径移除
- backend 的 `workspace snapshot` 现在开始更像真正的训练复盘：
  - [backend/src/services/supabase.service.ts](/home/ubuntu/VoxFlame-Agent/backend/src/services/supabase.service.ts) 会把 `articulationTips / frequentConfusions / recentTraining summary` 写进 `profile_bundle`
  - `session_review` 在没有稳定沟通会话时，也会优先用最新训练摘要生成 headline/summary
  - 如果最近一次会话本身就是训练，也会直接用训练摘要覆盖更泛的“最近一次会话”描述
- 语义上已经不该再把 `livekit_agent` 描述成“只会 text stub”：
  - communication text rewrite 已真实使用 DashScope chat/completions
  - TTS 已在 LiveKit 房间发布音轨
  - ASR 已接线，但还需要继续拿真实麦克风 smoke 把 final transcript 完全打透

### 2026-04-02

- LiveKit migration 的判断已经重新写实：
  - 节点 1：部署好 LiveKit 服务，已完成第一版开发基座
  - 节点 2：补齐现役 TEN 执行链功能并跑通全链路，进行中
  - 节点 3：完全删除 TEN + Agora，尚未开始
- `livekit_agent` 不再走 OpenAI realtime stub，而是 `DashScope/Qwen-first` communication loop。
- worker 注册失败的旧 blocker 已解决，当前 blocker 已切到 `ASR / correction / training feedback / memory tooling parity`。

## 下一步

1. 先把 `session-local typed memory / context assembly / session-close compaction` 补成稳定的 owner 链。
2. 训练页和记忆页继续围绕 `prepared-expression / important-expression / 高频句` 做统一句子录入入口，而不是继续长新 surface。
3. 数据录入和标注优先补齐 review queue、质量门槛和硬指标调查，不再只按样本数量推进。
   - 句子录入后默认自动跑“一句一音对应目标句”的校验，不先做独立 annotation UI
   - 只有自动校验命中风险的样本才进入复核
4. 记忆页继续收成“用户画像 + 当前准备 owner + 会后压缩结果”。
5. 再回到 10 分钟级别和公网条件下的真实沟通 smoke，验证这些沉淀是否真实影响现场纠错结果。
6. 本地验证入口继续收口到 `http://localhost:3000`：
   - 前端本地开发与 Docker 入口重新统一为单一 `3000` origin
   - 不再维护额外本地 HTTPS 入口，避免双 origin 带来的登录与跳转漂移
7. 本地登录和 RTC 鉴权稳定性已继续补强：
   - [frontend/src/lib/realtime-audio/session-bootstrap.ts](/home/ubuntu/VoxFlame-Agent/frontend/src/lib/realtime-audio/session-bootstrap.ts) 不再优先发送可能过期的 `accessToken`，而是优先向 Supabase 取最新 token
   - [frontend/src/app/(auth)/login/page.tsx](/home/ubuntu/VoxFlame-Agent/frontend/src/app/(auth)/login/page.tsx) 登录成功后会先确认 fresh session，再做整页跳转，减少 client-only replace 和 server middleware 脱节

## 当前验证基线

- `cd frontend && npm run build`
- `cd backend && npm run build`
- `cd livekit_agent && python3 -m unittest discover tests -v`
- `python3 -m py_compile livekit_agent/app.py livekit_agent/asr_runtime.py livekit_agent/data_contract.py`
- `bash scripts/check_ai_docs.sh`
- `cd frontend && npm test`
- Docker 真实验证优先看：
  - `sudo docker compose logs -f livekit-agent`
  - 关键字：`LiveKit ASR first audio frame`、`LiveKit end_audio received`、`LiveKit ASR final transcript`

### 2026-04-07

- 沟通页展示又收口了一层：前端已移除 `表达对照`，用户界面只保留最终可直接说出去的结果。
- `livekit_agent` fallback 文案已收掉“现在先按当前沟通场景继续 / 我先帮你把这句话往前推进”这类铺垫，超时回退时也只回真正要说的话。
- 当前慢的主因已经查清：不是额度或磁盘问题，而是 `ASR final -> DashScope correction/reply -> TTS` 这段串行链路里，纠错/改写调用经常超时回退。
